# 開発タスク表

## 完了済み

| タスク | 内容 |
|---|---|
| カードドラッグの不具合修正 | 相手スロットのカードを奪える/自分の詠唱中カードを詠唱進捗ごと複製できる不具合を修正(`card_node.gd`) |
| エフェクト演出(単発) | 詠唱完了時に`projectile`が相手スロットへ飛ぶように実装(`slot.gd` / `battle_effects.gd`) |
| エフェクト演出(相殺) | 同時発動時、双方から中間地点へ飛んで衝突するように実装 |
| resolve_pairの勝敗判定修正 | シーンツリーの並び順依存をやめ、`is_player`を明示的に見て判定するよう修正 |
| 相打ち時のアビリティ発動 | 相打ちでも両方のアビリティが発動するよう修正(必殺技カウントには含めない) |
| シールドまわりの修正 | 同時発動時の二重ダメージ修正、ダメージ最低0固定。「カードが無くなったらシールド消滅」は一度実装したが不具合が出たため撤回し、現状維持に決定 |

## 未着手

### 演出・アニメーション
| タスク | 内容 |
|---|---|
| 必殺技の専用アニメーション | 必殺技カードに専用の演出を追加 |
| アビリティごとのエフェクト | インパクト/残骸/スパーク/ミラージュ/シールドなど、アビリティ種類ごとの専用演出(projectile以外) |

### 戦闘・システム
| タスク | 内容 |
|---|---|
| AI強化 | 敵AIの行動をもっと「戦っている感じ」が出るように改善 |
| 速度操作キー | 戦闘中、Space:一時停止、E:倍速アップ(1x→1.5x→2x)、Q:倍速ダウン |

### UI・メニュー
| タスク | 内容 |
|---|---|
| 全シーン共通のESCメニュー | ESCキーで一時停止し、「タイトルに戻る」「続ける」などを選べるメニューを全シーンに追加 |
| タイトル画面の仮ボタン修正 | 「ストーリー」「設定」ボタンが仮パスのままなのを修正(設定画面は`start-meny`内に実装済みのものを接続できそう) |
| 続きから始める機能 | メニューにセーブ/ロードで途中から再開できる機能を追加 |

### 報酬・カード
| タスク | 内容 |
|---|---|
| 特殊カード報酬の実装 | 報酬画面の「特殊カード(準備中)」を実際に機能させる |
| 報酬画面のカード選択肢を統合 | 「相手のカード」「特殊カード」を1つの選択肢にまとめ、所持していない全カードからランダム3枚→1枚選択に変更 |
| レリックにアイコンを設定 | 各レリックの`.tres`に画像を割り当てる(コードではなく素材設定の作業) |

### ドキュメント整理(`docs/combat_spec.md`のTODO)
| タスク | 内容 |
|---|---|
| カード配置ルール | コスト・詠唱数計算などのルールを言語化 |
| アビリティ仕様 | 各アビリティの発動条件・効果量を整理 |
| レリック仕様 | 各レリックの効果を整理 |
| 必殺技解放条件 | 属性ごとの解放条件を整理 |
