[gd_scene format=3 uid="uid://cebu0u1sqbf8j"]

[ext_resource type="Texture2D" uid="uid://jl4j50q7hg21" path="res://fire_rain/textures/vertical2.png" id="1_bb4a2"]
[ext_resource type="Texture2D" uid="uid://dgcg20a7m7m70" path="res://fire_rain/textures/swirl1.png" id="2_u53yx"]
[ext_resource type="ArrayMesh" uid="uid://dskpak0netsgg" path="res://fire_rain/meshes/projectile1.res" id="13_eu8jq"]
[ext_resource type="Texture2D" uid="uid://xyb4fhaxrwrf" path="res://fire_rain/textures/gauissan1.png" id="13_u53yx"]
[ext_resource type="Texture2D" uid="uid://c41ikgxubcgsb" path="res://fire_rain/textures/trail1.png" id="14_rxfon"]
[ext_resource type="Texture2D" uid="uid://y0q8oebw66jb" path="res://fire_rain/textures/radial1.png" id="16_pe8ma"]
[ext_resource type="Texture2D" uid="uid://b2jwghyu815d0" path="res://fire_rain/textures/vein1.png" id="17_u53yx"]
[ext_resource type="Texture2D" uid="uid://c3ocug82j06pd" path="res://fire_rain/textures/glow1.png" id="23_3dayl"]
[ext_resource type="Texture2D" uid="uid://cnhvh8xff1ou4" path="res://fire_rain/textures/embers.png" id="26_fxwkx"]
[ext_resource type="Texture2D" uid="uid://bumtnp8qv7a08" path="res://fire_rain/textures/impact1_dissolve.png" id="30_6be7q"]
[ext_resource type="Texture2D" uid="uid://bxvb3g1em3ruw" path="res://fire_rain/textures/impact1.png" id="31_w4r8r"]
[ext_resource type="Texture2D" uid="uid://dm7odagwm6wy5" path="res://fire_rain/textures/flare.png" id="32_jcfxp"]
[ext_resource type="Texture2D" uid="uid://bbuw40h4mwwhw" path="res://fire_rain/textures/radial2.png" id="33_3jlxe"]
[ext_resource type="Texture2D" uid="uid://damwfgm6bxvs3" path="res://fire_rain/textures/radial3.png" id="34_2h2m6"]
[ext_resource type="Texture2D" uid="uid://deu75v2kwem8r" path="res://fire_rain/textures/axial1.png" id="36_j7fib"]
[ext_resource type="Texture2D" uid="uid://cs7ftjdolmnep" path="res://fire_rain/textures/vertical4.png" id="37_y7i7n"]
[ext_resource type="Texture2D" uid="uid://ca64plpteoehu" path="res://fire_rain/textures/vertical3.png" id="38_qmhta"]
[ext_resource type="ArrayMesh" uid="uid://r0xtnsspb8us" path="res://fire_rain/meshes/circle1.res" id="40_7vxbc"]
[ext_resource type="Texture2D" uid="uid://bhx217hkqtxl8" path="res://fire_rain/textures/crater1.png" id="41_jluke"]
[ext_resource type="ArrayMesh" uid="uid://74v7ftet10u4" path="res://fire_rain/meshes/debris1.res" id="45_jlx0x"]

[sub_resource type="GDScript" id="GDScript_u53yx"]
script/source = "@tool
extends Node3D

var list := [
	[ \"Random Seed\", [
		{ \"node_path\": \"MainEmitters/Core\", \"property\": \"seed\" },
		{ \"node_path\": \"MainEmitters/Trail\", \"property\": \"seed\" },
		{ \"node_path\": \"MainEmitters/Glow\", \"property\": \"seed\" },
		{ \"node_path\": \"MainEmitters/SubSparks\", \"property\": \"seed\" },
		{ \"node_path\": \"MainEmitters/SubFlare\", \"property\": \"seed\" },
		{ \"node_path\": \"MainEmitters/SubGlow\", \"property\": \"seed\" },
		{ \"node_path\": \"MainEmitters/SubEmbers\", \"property\": \"seed\" },
		{ \"node_path\": \"MainEmitters/SubSmoke\", \"property\": \"seed\" },
		{ \"node_path\": \"MainEmitters/SubCrater\", \"property\": \"seed\" },
		{ \"node_path\": \"MainEmitters/SubDebris\", \"property\": \"seed\" },
	]],
	[ \"Radius\", [
		{ \"node_path\": \"MainEmitters/Core\", \"object_path\": \"process_material\", \"property\": \"shader_parameter/m1_u_radius\" },
		{ \"node_path\": \"MainEmitters/Trail\", \"object_path\": \"process_material\", \"property\": \"shader_parameter/m1_u_radius\" },
		{ \"node_path\": \"MainEmitters/Glow\", \"object_path\": \"process_material\", \"property\": \"shader_parameter/m1_u_radius\" },
		{ \"node_path\": \"MainEmitters/SubSparks\", \"object_path\": \"process_material\", \"property\": \"shader_parameter/m1_u_radius\" },
		{ \"node_path\": \"MainEmitters/SubFlare\", \"object_path\": \"process_material\", \"property\": \"shader_parameter/m1_u_radius\" },
		{ \"node_path\": \"MainEmitters/SubGlow\", \"object_path\": \"process_material\", \"property\": \"shader_parameter/m1_u_radius\" },
		{ \"node_path\": \"MainEmitters/SubEmbers\", \"object_path\": \"process_material\", \"property\": \"shader_parameter/m1_u_radius\" },
		{ \"node_path\": \"MainEmitters/SubSmoke\", \"object_path\": \"process_material\", \"property\": \"shader_parameter/m1_u_radius\" },
		{ \"node_path\": \"MainEmitters/SubCrater\", \"object_path\": \"process_material\", \"property\": \"shader_parameter/m1_u_radius\" },
		{ \"node_path\": \"MainEmitters/SubDebris\", \"object_path\": \"process_material\", \"property\": \"shader_parameter/m1_u_radius\" },
	]],
	[ \"Main Color\", [
		{ \"node_path\": \"MainEmitters/Glow\", \"object_path\": \"process_material\", \"property\": \"shader_parameter/u_m3_rgb\" },
		{ \"node_path\": \"SubEmitters/Sparks\", \"object_path\": \"process_material\", \"property\": \"shader_parameter/u_m4_rgb\" },
		{ \"node_path\": \"SubEmitters/EmbersProjectile\", \"object_path\": \"process_material\", \"property\": \"shader_parameter/u_m2_rgb\" },
		{ \"node_path\": \"SubEmitters/EmbersImpact\", \"object_path\": \"process_material\", \"property\": \"shader_parameter/u_m3_rgb\" },
		{ \"node_path\": \"SubEmitters/GroundImpact\", \"object_path\": \"process_material\", \"property\": \"shader_parameter/u_m0_rgb\" },
		{ \"node_path\": \"SubEmitters/Flare\", \"object_path\": \"process_material\", \"property\": \"shader_parameter/u_m0_rgb\" },
		{ \"node_path\": \"SubEmitters/Glow\", \"object_path\": \"process_material\", \"property\": \"shader_parameter/u_m0_rgb\" },
		{ \"node_path\": \"SubEmitters/Shockwave\", \"object_path\": \"process_material\", \"property\": \"shader_parameter/u_m1_rgb\" },
	]],
	[ \"Fire Color\", [
		{ \"node_path\": \"MainEmitters/Core\", \"object_path\": \"material_override\", \"property\": \"shader_parameter/gradient\" },
	]],
	[ \"Trail Color\", [
		{ \"node_path\": \"MainEmitters/Trail\", \"object_path\": \"draw_pass_1\", \"sub_object_path\": \"material\", \"property\": \"shader_parameter/gradient\" },
	]],
	[ \"Smoke Color\", [
		{ \"node_path\": \"SubEmitters/Smoke\", \"object_path\": \"process_material\", \"property\": \"shader_parameter/u_m2_rgb\" }
	]],
	[ \"Crater Color\", [
		{ \"node_path\": \"SubEmitters/Crater\", \"object_path\": \"process_material\", \"property\": \"shader_parameter/u_m1_rgb\" },
	]],
	[ \"Debris Color\", [
		{ \"node_path\": \"SubEmitters/Debris\", \"object_path\": \"process_material\", \"property\": \"shader_parameter/u_m8_rgb\" },
	]],
]

func _ready() -> void:
	if Engine.is_editor_hint():
		EditorInterface.get_inspector().property_selected.connect(on_property_selected)
	else:
		get_tree().create_timer(3.0).timeout.connect(queue_free)
		for n in $MainEmitters.get_children():
			start(n)

func start(node: GPUParticles3D) -> void:
	node.restart(true)

func on_property_selected(property: String) -> void:
	var obj := EditorInterface.get_inspector().get_edited_object()
	if !obj:
		return
	if Input.is_key_pressed(KEY_ALT) and Input.is_key_pressed(KEY_CTRL):
		for c in get_children():
			var r := find_property_in_node(obj, c, \"\")
			if !r.is_empty():
				DisplayServer.clipboard_set(
					\"{ \\\"node_path\\\": \\\"%s\\\", \\\"object_path\\\": \\\"%s\\\", \\\"property\\\": \\\"%s\\\" }\" % [
						r.node_path, r.object_path, property
					])

func _get_property_list() -> Array[Dictionary]:
	if !is_inside_tree():
		return []
	var l: Array[Dictionary]
	for group in list:
		for prop in group[1]:
			var node := get_node(prop.node_path)
			if !node:
				print(\"Node not found %s\" % prop.node_path)
				continue
			var obj: Object = node
			if prop.has(\"object_path\"):
				obj = node.get(prop.object_path)
				if !obj:
					print(\"Object not found %s\" % prop.object_path)
					continue
				if prop.has(\"sub_object_path\"):
					obj = obj.get(prop.sub_object_path)
					if !obj:
						print(\"Sub-object not found %s\" % prop.sub_object_path)
						continue
			var prop_list = obj.get_property_list()
			var done := false
			for p in prop_list:
				if p.name == prop.property:
					var property = p.duplicate()
					property.name = group[0]
					l.push_back(property)
					done = true
					break
			if done:
				break
	return l

func _get(property: StringName) -> Variant:
	if !is_inside_tree():
		return null
	var r := find_property(property)
	if !r.is_empty():
		return r.object.get(r.property)
	else:
		return null

func _set(property: StringName, value: Variant) -> bool:
	if !is_inside_tree():
		return false
	for group in list:
		if group[0] != property:
			continue
		for prop in group[1]:
			var node := get_node(prop.node_path)
			if !node:
				continue
			var obj: Object = node
			if prop.has(\"object_path\"):
				obj = node.get(prop.object_path)
				if !obj:
					continue
				if prop.has(\"sub_object_path\"):
					obj = obj.get(prop.sub_object_path)
					if !obj:
						continue
			obj.set(prop.property, value)
	return false

func find_property(property: StringName) -> Dictionary:
	for group in list:
		if property != group[0]:
			continue
		for prop in group[1]:
			var node := get_node(prop.node_path)
			if !node:
				continue
			var obj: Object = node
			if prop.has(\"object_path\"):
				obj = node.get(prop.object_path)
				if !obj:
					continue
				if prop.has(\"sub_object_path\"):
					obj = obj.get(prop.sub_object_path)
					if !obj:
						continue
			return { \"node\": node, \"object\": obj, \"property\": prop.property }
	return {}

func find_property_in_node(object: Object, node: Node, node_path: String) -> Dictionary:
	node_path += node.name
	if object == node:
		return { \"node_path\": node_path, \"object_path\": \"\" }
	var l := node.get_property_list()
	for prop in l:
		var value = node.get(prop[\"name\"])
		match typeof(value):
			TYPE_OBJECT:
				if !value or value is Node:
					continue
				var r := find_property_in_object(object, value, node_path, prop[\"name\"], [])
				if !r.is_empty():
					return r
	for c in node.get_children():
		var r := find_property_in_node(object, c, node_path + \"/\")
		if !r.is_empty():
			return r
	return {}

func find_property_in_object(object: Object, obj: Object, node_path: String, object_path: String, stack: Array[Object]) -> Dictionary:
	if stack.has(obj):
		return {}
	if object == obj:
		return { \"node_path\": node_path, \"object_path\": object_path }
	stack.push_back(obj)
	var l := obj.get_property_list()
	for prop in l:
		var value = obj.get(prop[\"name\"])
		match typeof(value):
			TYPE_OBJECT:
				if !value or value == obj:
					continue
				var r := find_property_in_object(object, value, node_path, object_path + \":\" + prop[\"name\"], stack)
				if !r.is_empty():
					return r
	return {}
"

[sub_resource type="Shader" id="Shader_8lb58"]
code = "shader_type spatial;
render_mode unshaded, cull_disabled;

uniform float opacity = 1.0;
uniform float emission = 1.0;

uniform sampler2D tex_1 : source_color, filter_linear_mipmap, repeat_disable;
uniform vec2 tiling_1 = vec2(1.0);
uniform vec2 offset_1 = vec2(0.0);
uniform vec2 speed_1 = vec2(0.0);

uniform sampler2D tex_2 : source_color, filter_linear_mipmap, repeat_enable;
uniform vec2 tiling_2 = vec2(1.0);
uniform vec2 offset_2 = vec2(0.0);
uniform vec2 speed_2 = vec2(0.0);

uniform sampler2D gradient : source_color, repeat_disable;

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}


varying flat uint instance_id;


void vertex()
{
	instance_id = uint(INSTANCE_ID);
}

void fragment()
{
	uint seed = instance_id;
	vec4 tex_sampled_1 = texture(tex_1, UV + speed_1 * tiling_1 * TIME);
	vec4 tex_sampled_2 = texture(tex_2, UV + speed_2 * tiling_2 * TIME);
	ALPHA = COLOR.a;
	ALPHA = ALPHA * tex_sampled_1.r;
	//ALPHA = max(smoothstep(0.99, 1.0, ALPHA), ALPHA * tex_sampled_2.r);
	ALPHA = ALPHA * tex_sampled_2.r;
	ALPHA = clamp(ALPHA * opacity, 0.0, 1.0);
	
	float feather = 0.01;
	float threshold = 0.2;
	ALBEDO = COLOR.rgb * emission * texture(gradient, vec2(tex_sampled_2.r)).rgb;
	
	ALPHA = smoothstep(threshold, threshold + feather, ALPHA);
}


"

[sub_resource type="Gradient" id="Gradient_iu8ql"]
offsets = PackedFloat32Array(0, 0.5, 0.9533333)
colors = PackedColorArray(0.51, 0.19584, 0.08160001, 1, 0.85, 0.48379168, 0.3825, 1, 0.87, 0.609, 0.2175, 1)

[sub_resource type="GradientTexture1D" id="GradientTexture1D_8lb58"]
gradient = SubResource("Gradient_iu8ql")

[sub_resource type="ShaderMaterial" id="ShaderMaterial_6vuv3"]
render_priority = 1
shader = SubResource("Shader_8lb58")
shader_parameter/opacity = 1.0
shader_parameter/emission = 6.0
shader_parameter/tex_1 = ExtResource("1_bb4a2")
shader_parameter/tiling_1 = Vector2(1, 1)
shader_parameter/offset_1 = Vector2(0, 0)
shader_parameter/speed_1 = Vector2(0, 0)
shader_parameter/tex_2 = ExtResource("2_u53yx")
shader_parameter/tiling_2 = Vector2(3, 3)
shader_parameter/offset_2 = Vector2(0, 0)
shader_parameter/speed_2 = Vector2(0.1, -0.3)
shader_parameter/gradient = SubResource("GradientTexture1D_8lb58")

[sub_resource type="Shader" id="Shader_u53yx"]
code = "shader_type particles;
render_mode disable_velocity;

const float DEG_TO_RAD = PI / 180.0;
const uint AMOUNT = uint(24);

struct Particle {
	uint seed;
	float delta;
	float lifetime;
	uint index;
	uint number;
	float age;
	float normalized_age;
	float normalized_index;
	float normalized_number;
	vec3 initial_position;
	vec3 initial_scale;
	vec4 initial_color;
	vec3 initial_velocity;
	float radial_velocity;
	vec3 orbital_axis;
	float initial_orbital_speed;
	vec3 spawn_direction;
	vec3 position;
	vec3 scale;
	vec4 color;
	vec3 velocity;
	vec3 orbital_velocity;
	float orbital_speed;
	vec3 rotation_axis;
	float initial_rotation;
	float rotation;
	mat3 orientation;
	vec3 force;
	float animation_offset;
	float animation_speed;
	mat4 emission_transform;
	mat4 previous_transform;
	bool just_spawned;
	bool collided;
	vec3 collision_normal;
	float collision_depth;
};

float remap(float value, float input_min, float input_max, float output_min, float output_max) {
	float v = clamp(value, input_min, input_max);
	return (v - input_min) / (input_max - input_min) * (output_max - output_min) + output_min;
}

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}

uint hash(uint x) {
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = (x >> uint(16)) ^ x;
	return x;
}

vec3 normalize_or_zero(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 0.0, 0.0);
}

vec3 normalize_or_up(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 1.0, 0.0);
}

vec4 quat(vec3 axis, float angle) {
  float s = sin(angle / 2.0);
  return vec4(axis.x * s, axis.y * s, axis.z * s, cos(angle / 2.0));
}

vec4 quat_rotation_to(vec3 from, vec3 to) {
	vec3 xunit = vec3(1.0, 0.0, 0.0);
	vec3 yunit = vec3(0.0, 1.0, 0.0);
	float dot = dot(from, to);
	if (dot < -0.999999) {
	    vec3 axis = cross(xunit, from);
	    if (length(axis) < 0.000001) {
	    	axis = cross(yunit, from);
	    }
	    axis = normalize(axis);
	    return quat(axis, PI);
	} else if (dot > 0.999999) {
		return vec4(0.0, 0.0, 0.0, 1.0);
	} else {
		vec3 axis = cross(from, to);
		return normalize(vec4(axis, 1.0 + dot));
	}
}

vec3 rotated(vec3 v, vec3 axis, float angle) {
  vec4 q = quat(axis, angle);
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

vec3 rotated(vec3 v, vec4 q) {
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

mat3 rotated(mat3 m, vec4 q) {
  return mat3(rotated(m[0], q), rotated(m[1], q), rotated(m[2], q));
}

float normalized_by_amount(uint n) {
	return float(n) / float(max(AMOUNT, 1u));
}
void update_orbital_velocity(inout Particle particle, mat4 emission_transform, mat4 transform) {
	if (abs(particle.orbital_speed) < 0.01 || particle.delta < 0.001) { return; }
	vec3 local_pos = (inverse(emission_transform) * transform[3]).xyz;
	vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
	particle.position = (emission_transform * vec4(next, 0.0)).xyz;
	particle.orbital_velocity = (next - local_pos) / particle.delta;
}

void apply_radial_velocity(inout Particle particle) {
	vec3 d = particle.position;
	float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
	particle.position += normalize_or_zero(d) * mag * sign(particle.radial_velocity);
}

void m0_initialize_velocity(inout Particle particle) {
	float v_x = rand_range(particle.seed, 0.000000, 0.000000);
	float v_y = rand_range(particle.seed, -30.000000, -30.000000);
	float v_z = rand_range(particle.seed, 5.000000, 8.000000);
	vec3 v = vec3(v_x, v_y, v_z);
	particle.initial_velocity = v;
}

uniform float m1_u_radius = 20.0;

void m1_shape_location_ring(inout Particle particle) {
	float r = rand_range(particle.seed, 0.000000, 1.000000);
	float u = rand_range(particle.seed, 0.000000, 1.000000);
	float v = rand_range(particle.seed, 0.000000, 1.000000);
	vec3 h = vec3(1.000000, 0.000000, 0.000000);
	vec3 up = vec3(0.000000, 1.000000, 0.000000);
	h = rotated(h, up, u * 360.0 * DEG_TO_RAD) * vec3(1.000000, 1.000000, 1.000000);
	float radius_edge = tan(0.000000 * DEG_TO_RAD) * 0.010000;
	vec3 offset = vec3(0.000000, 0.000000, 0.000000);
	particle.spawn_direction = normalize(h * r * (m1_u_radius + radius_edge) + 0.010000 * up) + offset;
	particle.initial_position = h * r * (m1_u_radius + radius_edge * v) + v * 0.010000 * up + offset;
}

void m2_initialize_size(inout Particle particle) {
	vec3 s = vec3(0.200000, 1.000000, 0.200000);
	particle.initial_scale = s;
}

void m3_align(inout Particle particle) {
	vec3 up = vec3(0.0, 1.0, 0.0);
	vec3 v = normalize(particle.velocity + particle.orbital_velocity + normalize(particle.position) * particle.radial_velocity);
	vec4 q = quat_rotation_to(up, v);
	mat3 m = mat3(1.0);
	particle.orientation = rotated(m, q);
}

void m4_collision(inout Particle particle) {
	if (particle.collided) {
		float collision_response = dot(particle.collision_normal, particle.velocity);
		float slide_to_bounce_trigger = step(2.0 / clamp(0.000000 + 1.0, 1.0, 2.0), abs(collision_response));
		particle.position += particle.collision_normal * particle.collision_depth;
		// Remove all components of velocity that are not tangential to COLLISION_NORMAL.
		particle.velocity -= particle.collision_normal * collision_response;
		// Apply friction only to velocity across the surface (effectively decouples friction and bounce behavior).
		particle.velocity = mix(particle.velocity, vec3(0.0), clamp(1.000000, 0.0, 1.0));
		// Add bounce velocity to velocity.
		particle.velocity -= particle.collision_normal * collision_response * (0.000000 * slide_to_bounce_trigger);
	}
}

uniform sampler2D u_m5_scale : repeat_disable;

void m5_scale_size(inout Particle particle) {
	vec3 s = vec3(texture(u_m5_scale, vec2(particle.normalized_age)).r);
	particle.scale *= s;
}


void initialize(inout Particle particle) {
	particle.initial_position = vec3(0.0);
	particle.initial_color = vec4(1.0);
	particle.initial_scale = vec3(1.0);
	particle.initial_velocity = vec3(0.0);
	particle.initial_orbital_speed = 0.0;
	particle.spawn_direction = vec3(0.0);
	particle.animation_offset = 0.0;
	particle.animation_speed = 1.0;
	particle.rotation_axis = vec3(1.0, 0.0, 0.0);
	particle.initial_rotation = 0.0;
	particle.radial_velocity = 0.0;
	particle.orbital_axis = vec3(0.0, 1.0, 0.0);
	particle.orbital_velocity = vec3(0.0);
	m0_initialize_velocity(particle);
	m1_shape_location_ring(particle);
	m2_initialize_size(particle);
	particle.scale = particle.initial_scale;
	particle.color = particle.initial_color;
	particle.orbital_speed = particle.initial_orbital_speed;
}

void start() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = vec3(0.0);
	initialize(particle);
	if (rand(particle.seed) > AMOUNT_RATIO) {
		ACTIVE = false;
	}
	if (RESTART_CUSTOM) {
		CUSTOM = vec4(0.0);
		CUSTOM.x = particle.initial_rotation;
	}
	if (RESTART_COLOR) {
		COLOR = particle.initial_color;
	}
	if (RESTART_ROT_SCALE) {
		TRANSFORM[0].xyz = rotated(vec3(1.0, 0.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.x;
		TRANSFORM[1].xyz = rotated(vec3(0.0, 1.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.y;
		TRANSFORM[2].xyz = rotated(vec3(0.0, 0.0, 1.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.z;
	}
	if (RESTART_POSITION) {
		TRANSFORM[3].xyz = particle.initial_position;
		TRANSFORM = EMISSION_TRANSFORM * TRANSFORM;
	}
	if (RESTART_VELOCITY) {
		VELOCITY = (EMISSION_TRANSFORM * vec4(particle.initial_velocity, 0.0)).xyz;
		VELOCITY += EMITTER_VELOCITY * 0.0;//inherit_emitter_velocity_ratio;
	}
}

void process() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.delta = DELTA;
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = TRANSFORM[3].xyz;
	particle.velocity = VELOCITY;
	particle.orientation = mat3(
		normalize(TRANSFORM[0].xyz),
		normalize(TRANSFORM[1].xyz),
		normalize(TRANSFORM[2].xyz));
	particle.force = vec3(0.0);
	particle.emission_transform = EMISSION_TRANSFORM;
	particle.previous_transform = TRANSFORM;
	particle.rotation = CUSTOM.x;
	particle.just_spawned = CUSTOM.y == 0.0;
	particle.collided = COLLIDED;
	particle.collision_normal = COLLISION_NORMAL;
	particle.collision_depth = COLLISION_DEPTH;
	initialize(particle);
	CUSTOM.y += DELTA / particle.lifetime;
	CUSTOM.y = mix(CUSTOM.y, 1.0, INTERPOLATE_TO_END);
	CUSTOM.y = clamp(CUSTOM.y, 0.0, 1.0);
	particle.age = CUSTOM.y * particle.lifetime;
	particle.normalized_age = CUSTOM.y;
	m3_align(particle);
	m4_collision(particle);
	m5_scale_size(particle);
	if (abs(particle.orbital_speed) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
		particle.orbital_velocity = (next - local_pos) / particle.delta;
	}
	if (abs(particle.radial_velocity) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 d = local_pos;
		float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
		vec3 next = local_pos + normalize_or_zero(d) * mag * sign(particle.radial_velocity);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
	}
	particle.position += particle.velocity * particle.delta;
	VELOCITY = particle.velocity;
	TRANSFORM[0].xyz = particle.orientation[0] * particle.scale.x;
	TRANSFORM[1].xyz = particle.orientation[1] * particle.scale.y;
	TRANSFORM[2].xyz = particle.orientation[2] * particle.scale.z;
	TRANSFORM[3].xyz = particle.position;
	COLOR = particle.color;
	CUSTOM.x = particle.rotation;
	
	if (particle.collided) {
		ACTIVE = false;
	}
	{
		vec3 velocity = (particle.position - particle.previous_transform[3].xyz);
		vec3 dir = normalize_or_zero(velocity);
		CUSTOM.w += length(velocity);
		float step = CUSTOM.w / 1.000000;
		float interval_from = particle.age - DELTA;
		float interval_rem = 0.030000 - mod(interval_from, 0.030000);
		if (DELTA >= interval_rem) {
			for (int i = 0; i < 1; i++) {
				mat4 m = mat4(vec4(1.0, 0.0, 0.0, 0.0), vec4(0.0, 1.0, 0.0, 0.0), vec4(0.0, 0.0, 1.0, 0.0), vec4(-dir * step * float(i), 1.0));
				emit_subparticle(m * TRANSFORM, particle.velocity, vec4(0.0), vec4(0.0), FLAG_EMIT_POSITION | FLAG_EMIT_ROT_SCALE | FLAG_EMIT_VELOCITY);
			}
			CUSTOM.w = 0.0;
		}
	}
	if (particle.age >= LIFETIME) { ACTIVE = false;  }
}

"

[sub_resource type="Curve" id="Curve_62cbi"]
_data = [Vector2(0, 0), 0.0, 12.418159, 0, 0, Vector2(0.14527026, 1), 0.0, 0.0, 0, 0]
point_count = 2

[sub_resource type="CurveTexture" id="CurveTexture_j8f57"]
curve = SubResource("Curve_62cbi")
metadata/uniform_name = "u_m5_scale"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_21ym8"]
shader = SubResource("Shader_u53yx")
shader_parameter/m1_u_radius = 8.0
shader_parameter/u_m5_scale = SubResource("CurveTexture_j8f57")

[sub_resource type="Shader" id="Shader_8agcu"]
code = "shader_type particles;
render_mode disable_velocity;

const float DEG_TO_RAD = PI / 180.0;
const uint AMOUNT = uint(24);

struct Particle {
	uint seed;
	float delta;
	float lifetime;
	uint index;
	uint number;
	float age;
	float normalized_age;
	float normalized_index;
	float normalized_number;
	vec3 initial_position;
	vec3 initial_scale;
	vec4 initial_color;
	vec3 initial_velocity;
	float radial_velocity;
	vec3 orbital_axis;
	float initial_orbital_speed;
	vec3 spawn_direction;
	vec3 position;
	vec3 scale;
	vec4 color;
	vec3 velocity;
	vec3 orbital_velocity;
	float orbital_speed;
	vec3 rotation_axis;
	float initial_rotation;
	float rotation;
	mat3 orientation;
	vec3 force;
	float animation_offset;
	float animation_speed;
	mat4 emission_transform;
	mat4 previous_transform;
	bool just_spawned;
	bool collided;
	vec3 collision_normal;
	float collision_depth;
};

float remap(float value, float input_min, float input_max, float output_min, float output_max) {
	float v = clamp(value, input_min, input_max);
	return (v - input_min) / (input_max - input_min) * (output_max - output_min) + output_min;
}

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}

uint hash(uint x) {
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = (x >> uint(16)) ^ x;
	return x;
}

vec3 normalize_or_zero(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 0.0, 0.0);
}

vec3 normalize_or_up(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 1.0, 0.0);
}

vec4 quat(vec3 axis, float angle) {
  float s = sin(angle / 2.0);
  return vec4(axis.x * s, axis.y * s, axis.z * s, cos(angle / 2.0));
}

vec4 quat_rotation_to(vec3 from, vec3 to) {
	vec3 xunit = vec3(1.0, 0.0, 0.0);
	vec3 yunit = vec3(0.0, 1.0, 0.0);
	float dot = dot(from, to);
	if (dot < -0.999999) {
	    vec3 axis = cross(xunit, from);
	    if (length(axis) < 0.000001) {
	    	axis = cross(yunit, from);
	    }
	    axis = normalize(axis);
	    return quat(axis, PI);
	} else if (dot > 0.999999) {
		return vec4(0.0, 0.0, 0.0, 1.0);
	} else {
		vec3 axis = cross(from, to);
		return normalize(vec4(axis, 1.0 + dot));
	}
}

vec3 rotated(vec3 v, vec3 axis, float angle) {
  vec4 q = quat(axis, angle);
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

vec3 rotated(vec3 v, vec4 q) {
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

mat3 rotated(mat3 m, vec4 q) {
  return mat3(rotated(m[0], q), rotated(m[1], q), rotated(m[2], q));
}

float normalized_by_amount(uint n) {
	return float(n) / float(max(AMOUNT, 1u));
}
void update_orbital_velocity(inout Particle particle, mat4 emission_transform, mat4 transform) {
	if (abs(particle.orbital_speed) < 0.01 || particle.delta < 0.001) { return; }
	vec3 local_pos = (inverse(emission_transform) * transform[3]).xyz;
	vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
	particle.position = (emission_transform * vec4(next, 0.0)).xyz;
	particle.orbital_velocity = (next - local_pos) / particle.delta;
}

void apply_radial_velocity(inout Particle particle) {
	vec3 d = particle.position;
	float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
	particle.position += normalize_or_zero(d) * mag * sign(particle.radial_velocity);
}

void m0_initialize_velocity(inout Particle particle) {
	float v_x = rand_range(particle.seed, 0.000000, 0.000000);
	float v_y = rand_range(particle.seed, -30.000000, -30.000000);
	float v_z = rand_range(particle.seed, 5.000000, 8.000000);
	vec3 v = vec3(v_x, v_y, v_z);
	particle.initial_velocity = v;
}

uniform float m1_u_radius = 20.0;

void m1_shape_location_ring(inout Particle particle) {
	float r = rand_range(particle.seed, 0.000000, 1.000000);
	float u = rand_range(particle.seed, 0.000000, 1.000000);
	float v = rand_range(particle.seed, 0.000000, 1.000000);
	vec3 h = vec3(1.000000, 0.000000, 0.000000);
	vec3 up = vec3(0.000000, 1.000000, 0.000000);
	h = rotated(h, up, u * 360.0 * DEG_TO_RAD) * vec3(1.000000, 1.000000, 1.000000);
	float radius_edge = tan(0.000000 * DEG_TO_RAD) * 0.010000;
	vec3 offset = vec3(0.000000, 0.000000, 0.000000);
	particle.spawn_direction = normalize(h * r * (m1_u_radius + radius_edge) + 0.010000 * up) + offset;
	particle.initial_position = h * r * (m1_u_radius + radius_edge * v) + v * 0.010000 * up + offset;
}

void m2_align(inout Particle particle) {
	vec3 up = vec3(0.0, 1.0, 0.0);
	vec3 v = normalize(particle.velocity + particle.orbital_velocity + normalize(particle.position) * particle.radial_velocity);
	vec4 q = quat_rotation_to(up, v);
	mat3 m = mat3(1.0);
	particle.orientation = rotated(m, q);
}

void m3_collision(inout Particle particle) {
	if (particle.collided) {
		float collision_response = dot(particle.collision_normal, particle.velocity);
		float slide_to_bounce_trigger = step(2.0 / clamp(0.000000 + 1.0, 1.0, 2.0), abs(collision_response));
		particle.position += particle.collision_normal * particle.collision_depth;
		// Remove all components of velocity that are not tangential to COLLISION_NORMAL.
		particle.velocity -= particle.collision_normal * collision_response;
		// Apply friction only to velocity across the surface (effectively decouples friction and bounce behavior).
		particle.velocity = mix(particle.velocity, vec3(0.0), clamp(1.000000, 0.0, 1.0));
		// Add bounce velocity to velocity.
		particle.velocity -= particle.collision_normal * collision_response * (0.000000 * slide_to_bounce_trigger);
	}
}


void initialize(inout Particle particle) {
	particle.initial_position = vec3(0.0);
	particle.initial_color = vec4(1.0);
	particle.initial_scale = vec3(1.0);
	particle.initial_velocity = vec3(0.0);
	particle.initial_orbital_speed = 0.0;
	particle.spawn_direction = vec3(0.0);
	particle.animation_offset = 0.0;
	particle.animation_speed = 1.0;
	particle.rotation_axis = vec3(1.0, 0.0, 0.0);
	particle.initial_rotation = 0.0;
	particle.radial_velocity = 0.0;
	particle.orbital_axis = vec3(0.0, 1.0, 0.0);
	particle.orbital_velocity = vec3(0.0);
	m0_initialize_velocity(particle);
	m1_shape_location_ring(particle);
	particle.scale = particle.initial_scale;
	particle.color = particle.initial_color;
	particle.orbital_speed = particle.initial_orbital_speed;
}

void start() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = vec3(0.0);
	initialize(particle);
	if (rand(particle.seed) > AMOUNT_RATIO) {
		ACTIVE = false;
	}
	if (RESTART_CUSTOM) {
		CUSTOM = vec4(0.0);
		CUSTOM.x = particle.initial_rotation;
	}
	if (RESTART_COLOR) {
		COLOR = particle.initial_color;
	}
	if (RESTART_ROT_SCALE) {
		TRANSFORM[0].xyz = rotated(vec3(1.0, 0.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.x;
		TRANSFORM[1].xyz = rotated(vec3(0.0, 1.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.y;
		TRANSFORM[2].xyz = rotated(vec3(0.0, 0.0, 1.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.z;
	}
	if (RESTART_POSITION) {
		TRANSFORM[3].xyz = particle.initial_position;
		TRANSFORM = EMISSION_TRANSFORM * TRANSFORM;
	}
	if (RESTART_VELOCITY) {
		VELOCITY = (EMISSION_TRANSFORM * vec4(particle.initial_velocity, 0.0)).xyz;
		VELOCITY += EMITTER_VELOCITY * 0.0;//inherit_emitter_velocity_ratio;
	}
}

void process() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.delta = DELTA;
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = TRANSFORM[3].xyz;
	particle.velocity = VELOCITY;
	particle.orientation = mat3(
		normalize(TRANSFORM[0].xyz),
		normalize(TRANSFORM[1].xyz),
		normalize(TRANSFORM[2].xyz));
	particle.force = vec3(0.0);
	particle.emission_transform = EMISSION_TRANSFORM;
	particle.previous_transform = TRANSFORM;
	particle.rotation = CUSTOM.x;
	particle.just_spawned = CUSTOM.y == 0.0;
	particle.collided = COLLIDED;
	particle.collision_normal = COLLISION_NORMAL;
	particle.collision_depth = COLLISION_DEPTH;
	initialize(particle);
	CUSTOM.y += DELTA / particle.lifetime;
	CUSTOM.y = mix(CUSTOM.y, 1.0, INTERPOLATE_TO_END);
	CUSTOM.y = clamp(CUSTOM.y, 0.0, 1.0);
	particle.age = CUSTOM.y * particle.lifetime;
	particle.normalized_age = CUSTOM.y;
	m2_align(particle);
	m3_collision(particle);
	if (abs(particle.orbital_speed) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
		particle.orbital_velocity = (next - local_pos) / particle.delta;
	}
	if (abs(particle.radial_velocity) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 d = local_pos;
		float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
		vec3 next = local_pos + normalize_or_zero(d) * mag * sign(particle.radial_velocity);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
	}
	particle.position += particle.velocity * particle.delta;
	VELOCITY = particle.velocity;
	TRANSFORM[0].xyz = particle.orientation[0] * particle.scale.x;
	TRANSFORM[1].xyz = particle.orientation[1] * particle.scale.y;
	TRANSFORM[2].xyz = particle.orientation[2] * particle.scale.z;
	TRANSFORM[3].xyz = particle.position;
	COLOR = particle.color;
	CUSTOM.x = particle.rotation;
	
	if (particle.age >= LIFETIME) { ACTIVE = false;  }
}

"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_xjhfn"]
shader = SubResource("Shader_8agcu")
shader_parameter/m1_u_radius = 8.0

[sub_resource type="Curve" id="Curve_bda6n"]
_data = [Vector2(0, 1), 0.0, -0.6670468, 0, 1, Vector2(1, 0.3329532), -0.6670468, 0.0, 1, 0]
point_count = 2

[sub_resource type="Shader" id="Shader_1c2q6"]
code = "shader_type spatial;
render_mode unshaded, particle_trails, cull_disabled, blend_add;

uniform float opacity = 1.0;
uniform float emission = 1.0;

uniform sampler2D gradient : source_color, repeat_disable;

uniform sampler2D tex_1 : source_color, filter_linear_mipmap, repeat_disable;
uniform vec2 tiling_1 = vec2(1.0);
uniform vec2 offset_1 = vec2(0.0);
uniform vec2 speed_1 = vec2(0.0);

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}


varying flat uint instance_id;


void vertex()
{
	instance_id = uint(INSTANCE_ID);
	vec3 right = normalize(cross(MODEL_MATRIX[1].xyz, MAIN_CAM_INV_VIEW_MATRIX[2].xyz));
	vec3 front = normalize(cross(right, MODEL_MATRIX[1].xyz));
	mat4 model = mat4(
		vec4(right, 0.0),
		vec4(normalize(MODEL_MATRIX[1].xyz), 0.0),
		vec4(front, 0.0),
		MODEL_MATRIX[3]);
	model = model * mat4(
		vec4(cos(INSTANCE_CUSTOM.x), -sin(INSTANCE_CUSTOM.x), 0.0, 0.0),
		vec4(sin(INSTANCE_CUSTOM.x), cos(INSTANCE_CUSTOM.x), 0.0, 0.0),
		vec4(0.0, 0.0, 1.0, 0.0),
		vec4(0.0, 0.0, 0.0, 1.0));
    model = model * mat4(
		vec4(length(MODEL_MATRIX[0].xyz), 0.0, 0.0, 0.0),
		vec4(0.0, length(MODEL_MATRIX[1].xyz), 0.0, 0.0),
		vec4(0.0, 0.0, length(MODEL_MATRIX[2].xyz), 0.0),
		vec4(0.0, 0.0, 0.0, 1.0));
	MODELVIEW_MATRIX = VIEW_MATRIX * model;
	MODELVIEW_NORMAL_MATRIX = mat3(MODELVIEW_MATRIX);
}

void fragment()
{
	uint seed = instance_id;
	vec4 tex_sampled_1 = texture(tex_1, UV + speed_1 * tiling_1 * TIME);
	ALPHA = COLOR.a;
	ALPHA = ALPHA * tex_sampled_1.r;
	ALPHA = clamp(ALPHA * opacity, 0.0, 1.0);
	ALBEDO = COLOR.rgb * emission;
	ALBEDO *= tex_sampled_1.rgb;
	ALBEDO = texture(gradient, vec2(ALPHA)).rgb * emission;
	
}


"

[sub_resource type="Gradient" id="Gradient_aycuk"]
colors = PackedColorArray(0.2, 0.10613334, 0.024000002, 1, 1, 0.7776667, 0.42000002, 1)

[sub_resource type="GradientTexture1D" id="GradientTexture1D_iu8ql"]
gradient = SubResource("Gradient_aycuk")

[sub_resource type="ShaderMaterial" id="ShaderMaterial_8lb58"]
render_priority = 0
shader = SubResource("Shader_1c2q6")
shader_parameter/opacity = 1.0
shader_parameter/emission = 6.0
shader_parameter/gradient = SubResource("GradientTexture1D_iu8ql")
shader_parameter/tex_1 = ExtResource("14_rxfon")
shader_parameter/tiling_1 = Vector2(1, 1)
shader_parameter/offset_1 = Vector2(0, 0)
shader_parameter/speed_1 = Vector2(0, 0)

[sub_resource type="RibbonTrailMesh" id="RibbonTrailMesh_gk6rx"]
material = SubResource("ShaderMaterial_8lb58")
shape = 0
size = 0.3
curve = SubResource("Curve_bda6n")

[sub_resource type="Shader" id="Shader_jgkmw"]
code = "shader_type particles;
render_mode disable_velocity;

const float DEG_TO_RAD = PI / 180.0;
const uint AMOUNT = uint(24);

struct Particle {
	uint seed;
	float delta;
	float lifetime;
	uint index;
	uint number;
	float age;
	float normalized_age;
	float normalized_index;
	float normalized_number;
	vec3 initial_position;
	vec3 initial_scale;
	vec4 initial_color;
	vec3 initial_velocity;
	float radial_velocity;
	vec3 orbital_axis;
	float initial_orbital_speed;
	vec3 spawn_direction;
	vec3 position;
	vec3 scale;
	vec4 color;
	vec3 velocity;
	vec3 orbital_velocity;
	float orbital_speed;
	vec3 rotation_axis;
	float initial_rotation;
	float rotation;
	mat3 orientation;
	vec3 force;
	float animation_offset;
	float animation_speed;
	mat4 emission_transform;
	mat4 previous_transform;
	bool just_spawned;
	bool collided;
	vec3 collision_normal;
	float collision_depth;
};

float remap(float value, float input_min, float input_max, float output_min, float output_max) {
	float v = clamp(value, input_min, input_max);
	return (v - input_min) / (input_max - input_min) * (output_max - output_min) + output_min;
}

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}

uint hash(uint x) {
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = (x >> uint(16)) ^ x;
	return x;
}

vec3 normalize_or_zero(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 0.0, 0.0);
}

vec3 normalize_or_up(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 1.0, 0.0);
}

vec4 quat(vec3 axis, float angle) {
  float s = sin(angle / 2.0);
  return vec4(axis.x * s, axis.y * s, axis.z * s, cos(angle / 2.0));
}

vec4 quat_rotation_to(vec3 from, vec3 to) {
	vec3 xunit = vec3(1.0, 0.0, 0.0);
	vec3 yunit = vec3(0.0, 1.0, 0.0);
	float dot = dot(from, to);
	if (dot < -0.999999) {
	    vec3 axis = cross(xunit, from);
	    if (length(axis) < 0.000001) {
	    	axis = cross(yunit, from);
	    }
	    axis = normalize(axis);
	    return quat(axis, PI);
	} else if (dot > 0.999999) {
		return vec4(0.0, 0.0, 0.0, 1.0);
	} else {
		vec3 axis = cross(from, to);
		return normalize(vec4(axis, 1.0 + dot));
	}
}

vec3 rotated(vec3 v, vec3 axis, float angle) {
  vec4 q = quat(axis, angle);
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

vec3 rotated(vec3 v, vec4 q) {
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

mat3 rotated(mat3 m, vec4 q) {
  return mat3(rotated(m[0], q), rotated(m[1], q), rotated(m[2], q));
}

float normalized_by_amount(uint n) {
	return float(n) / float(max(AMOUNT, 1u));
}
void update_orbital_velocity(inout Particle particle, mat4 emission_transform, mat4 transform) {
	if (abs(particle.orbital_speed) < 0.01 || particle.delta < 0.001) { return; }
	vec3 local_pos = (inverse(emission_transform) * transform[3]).xyz;
	vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
	particle.position = (emission_transform * vec4(next, 0.0)).xyz;
	particle.orbital_velocity = (next - local_pos) / particle.delta;
}

void apply_radial_velocity(inout Particle particle) {
	vec3 d = particle.position;
	float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
	particle.position += normalize_or_zero(d) * mag * sign(particle.radial_velocity);
}

void m0_initialize_velocity(inout Particle particle) {
	float v_x = rand_range(particle.seed, 0.000000, 0.000000);
	float v_y = rand_range(particle.seed, -30.000000, -30.000000);
	float v_z = rand_range(particle.seed, 5.000000, 8.000000);
	vec3 v = vec3(v_x, v_y, v_z);
	particle.initial_velocity = v;
}
uniform float m1_u_radius = 20.0;

void m1_shape_location_ring(inout Particle particle) {
	float r = rand_range(particle.seed, 0.000000, 1.000000);
	float u = rand_range(particle.seed, 0.000000, 1.000000);
	float v = rand_range(particle.seed, 0.000000, 1.000000);
	vec3 h = vec3(1.000000, 0.000000, 0.000000);
	vec3 up = vec3(0.000000, 1.000000, 0.000000);
	h = rotated(h, up, u * 360.0 * DEG_TO_RAD) * vec3(1.000000, 1.000000, 1.000000);
	float radius_edge = tan(0.000000 * DEG_TO_RAD) * 0.010000;
	vec3 offset = vec3(0.000000, 0.000000, 0.000000);
	particle.spawn_direction = normalize(h * r * (m1_u_radius + radius_edge) + 0.010000 * up) + offset;
	particle.initial_position = h * r * (m1_u_radius + radius_edge * v) + v * 0.010000 * up + offset;
}

void m2_initialize_size(inout Particle particle) {
	vec3 s = vec3(1.000000, 4.000000, 1.000000);
	particle.initial_scale = s;
}

uniform vec4 u_m3_rgb : source_color;

void m3_initialize_color(inout Particle particle) {
	vec4 c = u_m3_rgb;
	float a = 1.0;
	particle.initial_color = vec4(c.rgb, a);
}

void m4_align(inout Particle particle) {
	vec3 up = vec3(0.0, 1.0, 0.0);
	vec3 v = normalize(particle.velocity + particle.orbital_velocity + normalize(particle.position) * particle.radial_velocity);
	vec4 q = quat_rotation_to(up, v);
	mat3 m = mat3(1.0);
	particle.orientation = rotated(m, q);
}

void m5_collision(inout Particle particle) {
	if (particle.collided) {
		float collision_response = dot(particle.collision_normal, particle.velocity);
		float slide_to_bounce_trigger = step(2.0 / clamp(0.000000 + 1.0, 1.0, 2.0), abs(collision_response));
		particle.position += particle.collision_normal * particle.collision_depth;
		// Remove all components of velocity that are not tangential to COLLISION_NORMAL.
		particle.velocity -= particle.collision_normal * collision_response;
		// Apply friction only to velocity across the surface (effectively decouples friction and bounce behavior).
		particle.velocity = mix(particle.velocity, vec3(0.0), clamp(1.000000, 0.0, 1.0));
		// Add bounce velocity to velocity.
		particle.velocity -= particle.collision_normal * collision_response * (0.000000 * slide_to_bounce_trigger);
	}
}

uniform sampler2D u_m6_scale : repeat_disable;

void m6_scale_size(inout Particle particle) {
	vec3 s = vec3(texture(u_m6_scale, vec2(particle.normalized_age)).r);
	particle.scale *= s;
}


void initialize(inout Particle particle) {
	particle.initial_position = vec3(0.0);
	particle.initial_color = vec4(1.0);
	particle.initial_scale = vec3(1.0);
	particle.initial_velocity = vec3(0.0);
	particle.initial_orbital_speed = 0.0;
	particle.spawn_direction = vec3(0.0);
	particle.animation_offset = 0.0;
	particle.animation_speed = 1.0;
	particle.rotation_axis = vec3(1.0, 0.0, 0.0);
	particle.initial_rotation = 0.0;
	particle.radial_velocity = 0.0;
	particle.orbital_axis = vec3(0.0, 1.0, 0.0);
	particle.orbital_velocity = vec3(0.0);
	m0_initialize_velocity(particle);
	m1_shape_location_ring(particle);
	m2_initialize_size(particle);
	m3_initialize_color(particle);
	particle.scale = particle.initial_scale;
	particle.color = particle.initial_color;
	particle.orbital_speed = particle.initial_orbital_speed;
}

void start() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = vec3(0.0);
	initialize(particle);
	if (rand(particle.seed) > AMOUNT_RATIO) {
		ACTIVE = false;
	}
	if (RESTART_CUSTOM) {
		CUSTOM = vec4(0.0);
		CUSTOM.x = particle.initial_rotation;
	}
	if (RESTART_COLOR) {
		COLOR = particle.initial_color;
	}
	if (RESTART_ROT_SCALE) {
		TRANSFORM[0].xyz = rotated(vec3(1.0, 0.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.x;
		TRANSFORM[1].xyz = rotated(vec3(0.0, 1.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.y;
		TRANSFORM[2].xyz = rotated(vec3(0.0, 0.0, 1.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.z;
	}
	if (RESTART_POSITION) {
		TRANSFORM[3].xyz = particle.initial_position;
		TRANSFORM = EMISSION_TRANSFORM * TRANSFORM;
	}
	if (RESTART_VELOCITY) {
		VELOCITY = (EMISSION_TRANSFORM * vec4(particle.initial_velocity, 0.0)).xyz;
		VELOCITY += EMITTER_VELOCITY * 0.0;//inherit_emitter_velocity_ratio;
	}
}

void process() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.delta = DELTA;
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = TRANSFORM[3].xyz;
	particle.velocity = VELOCITY;
	particle.orientation = mat3(
		normalize(TRANSFORM[0].xyz),
		normalize(TRANSFORM[1].xyz),
		normalize(TRANSFORM[2].xyz));
	particle.force = vec3(0.0);
	particle.emission_transform = EMISSION_TRANSFORM;
	particle.previous_transform = TRANSFORM;
	particle.rotation = CUSTOM.x;
	particle.just_spawned = CUSTOM.y == 0.0;
	particle.collided = COLLIDED;
	particle.collision_normal = COLLISION_NORMAL;
	particle.collision_depth = COLLISION_DEPTH;
	initialize(particle);
	CUSTOM.y += DELTA / particle.lifetime;
	CUSTOM.y = mix(CUSTOM.y, 1.0, INTERPOLATE_TO_END);
	CUSTOM.y = clamp(CUSTOM.y, 0.0, 1.0);
	particle.age = CUSTOM.y * particle.lifetime;
	particle.normalized_age = CUSTOM.y;
	m4_align(particle);
	m5_collision(particle);
	m6_scale_size(particle);
	if (abs(particle.orbital_speed) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
		particle.orbital_velocity = (next - local_pos) / particle.delta;
	}
	if (abs(particle.radial_velocity) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 d = local_pos;
		float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
		vec3 next = local_pos + normalize_or_zero(d) * mag * sign(particle.radial_velocity);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
	}
	particle.position += particle.velocity * particle.delta;
	VELOCITY = particle.velocity;
	TRANSFORM[0].xyz = particle.orientation[0] * particle.scale.x;
	TRANSFORM[1].xyz = particle.orientation[1] * particle.scale.y;
	TRANSFORM[2].xyz = particle.orientation[2] * particle.scale.z;
	TRANSFORM[3].xyz = particle.position;
	COLOR = particle.color;
	CUSTOM.x = particle.rotation;
	
	if (particle.collided) {
		ACTIVE = false;
	}
	if (particle.collided && CUSTOM.w == 0.0) {
		CUSTOM.w = 1.0;
		for (int i = 0; i < 1; i++) { emit_subparticle(TRANSFORM, vec3(0.0), vec4(0.0), vec4(0.0), FLAG_EMIT_POSITION); }
	}
	if (particle.age >= LIFETIME) { ACTIVE = false;  }
}

"

[sub_resource type="Curve" id="Curve_q516c"]
_data = [Vector2(0, 0), 0.0, 9.751309, 0, 0, Vector2(0.16554053, 1), 0.0, 0.0, 0, 0]
point_count = 2

[sub_resource type="CurveTexture" id="CurveTexture_lidet"]
curve = SubResource("Curve_q516c")
metadata/uniform_name = "u_m6_scale"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_xjchu"]
shader = SubResource("Shader_jgkmw")
shader_parameter/m1_u_radius = 8.0
shader_parameter/u_m3_rgb = Color(0.94, 0.74855334, 0.49819997, 1)
shader_parameter/u_m6_scale = SubResource("CurveTexture_lidet")

[sub_resource type="Shader" id="Shader_ow1oc"]
code = "shader_type spatial;
render_mode unshaded, blend_add, cull_disabled;

uniform float opacity = 1.0;
uniform float emission = 1.0;

uniform sampler2D tex_1 : source_color, filter_linear_mipmap, repeat_disable;
uniform vec2 tiling_1 = vec2(1.0);
uniform vec2 offset_1 = vec2(0.0);
uniform vec2 speed_1 = vec2(0.0);

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}


varying flat uint instance_id;


void vertex()
{
	instance_id = uint(INSTANCE_ID);
	vec3 up = normalize(MODEL_MATRIX[1].xyz);
	vec3 right = normalize(cross(up, MAIN_CAM_INV_VIEW_MATRIX[2].xyz));
	vec3 front = normalize(cross(right, up));
	MODELVIEW_MATRIX = VIEW_MATRIX * mat4(
		vec4(right, 0.0),
		vec4(up, 0.0),
		vec4(front, 0.0),
		MODEL_MATRIX[3]);
	MODELVIEW_MATRIX = MODELVIEW_MATRIX * mat4(
			vec4(length(MODEL_MATRIX[0].xyz), 0.0, 0.0, 0.0),
			vec4(0.0, length(MODEL_MATRIX[1].xyz), 0.0, 0.0),
			vec4(0.0, 0.0, length(MODEL_MATRIX[2].xyz), 0.0),
			vec4(0.0, 0.0, 0.0, 1.0));
	MODELVIEW_NORMAL_MATRIX = mat3(MODELVIEW_MATRIX);
}

void fragment()
{
	uint seed = instance_id;
	vec4 tex_sampled_1 = texture(tex_1, UV + speed_1 * tiling_1 * TIME);
	ALPHA = COLOR.a;
	ALPHA = ALPHA * tex_sampled_1.r;
	ALPHA = clamp(ALPHA * opacity, 0.0, 1.0);
	ALBEDO = COLOR.rgb * emission;
	ALBEDO *= tex_sampled_1.rgb;
	
}


"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_ljiev"]
render_priority = 0
shader = SubResource("Shader_ow1oc")
shader_parameter/opacity = 1.0
shader_parameter/emission = 2.0
shader_parameter/tex_1 = ExtResource("16_pe8ma")
shader_parameter/tiling_1 = Vector2(1, 1)
shader_parameter/offset_1 = Vector2(0, 0)
shader_parameter/speed_1 = Vector2(0, 0)

[sub_resource type="QuadMesh" id="QuadMesh_83lj5"]
material = SubResource("ShaderMaterial_ljiev")
center_offset = Vector3(0, -0.035, 0)

[sub_resource type="Shader" id="Shader_so1j8"]
code = "shader_type particles;
render_mode disable_velocity;

const float DEG_TO_RAD = PI / 180.0;
const uint AMOUNT = uint(24);

struct Particle {
	uint seed;
	float delta;
	float lifetime;
	uint index;
	uint number;
	float age;
	float normalized_age;
	float normalized_index;
	float normalized_number;
	vec3 initial_position;
	vec3 initial_scale;
	vec4 initial_color;
	vec3 initial_velocity;
	float radial_velocity;
	vec3 orbital_axis;
	float initial_orbital_speed;
	vec3 spawn_direction;
	vec3 position;
	vec3 scale;
	vec4 color;
	vec3 velocity;
	vec3 orbital_velocity;
	float orbital_speed;
	vec3 rotation_axis;
	float initial_rotation;
	float rotation;
	mat3 orientation;
	vec3 force;
	float animation_offset;
	float animation_speed;
	mat4 emission_transform;
	mat4 previous_transform;
	bool just_spawned;
	bool collided;
	vec3 collision_normal;
	float collision_depth;
};

float remap(float value, float input_min, float input_max, float output_min, float output_max) {
	float v = clamp(value, input_min, input_max);
	return (v - input_min) / (input_max - input_min) * (output_max - output_min) + output_min;
}

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}

uint hash(uint x) {
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = (x >> uint(16)) ^ x;
	return x;
}

vec3 normalize_or_zero(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 0.0, 0.0);
}

vec3 normalize_or_up(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 1.0, 0.0);
}

vec4 quat(vec3 axis, float angle) {
  float s = sin(angle / 2.0);
  return vec4(axis.x * s, axis.y * s, axis.z * s, cos(angle / 2.0));
}

vec4 quat_rotation_to(vec3 from, vec3 to) {
	vec3 xunit = vec3(1.0, 0.0, 0.0);
	vec3 yunit = vec3(0.0, 1.0, 0.0);
	float dot = dot(from, to);
	if (dot < -0.999999) {
	    vec3 axis = cross(xunit, from);
	    if (length(axis) < 0.000001) {
	    	axis = cross(yunit, from);
	    }
	    axis = normalize(axis);
	    return quat(axis, PI);
	} else if (dot > 0.999999) {
		return vec4(0.0, 0.0, 0.0, 1.0);
	} else {
		vec3 axis = cross(from, to);
		return normalize(vec4(axis, 1.0 + dot));
	}
}

vec3 rotated(vec3 v, vec3 axis, float angle) {
  vec4 q = quat(axis, angle);
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

vec3 rotated(vec3 v, vec4 q) {
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

mat3 rotated(mat3 m, vec4 q) {
  return mat3(rotated(m[0], q), rotated(m[1], q), rotated(m[2], q));
}

float normalized_by_amount(uint n) {
	return float(n) / float(max(AMOUNT, 1u));
}
void update_orbital_velocity(inout Particle particle, mat4 emission_transform, mat4 transform) {
	if (abs(particle.orbital_speed) < 0.01 || particle.delta < 0.001) { return; }
	vec3 local_pos = (inverse(emission_transform) * transform[3]).xyz;
	vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
	particle.position = (emission_transform * vec4(next, 0.0)).xyz;
	particle.orbital_velocity = (next - local_pos) / particle.delta;
}

void apply_radial_velocity(inout Particle particle) {
	vec3 d = particle.position;
	float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
	particle.position += normalize_or_zero(d) * mag * sign(particle.radial_velocity);
}

void m0_initialize_velocity(inout Particle particle) {
	float v_x = rand_range(particle.seed, 0.000000, 1.000000);
	float v_y = rand_range(particle.seed, -30.000000, -30.000000);
	float v_z = rand_range(particle.seed, 5.000000, 8.000000);
	vec3 v = vec3(v_x, v_y, v_z);
	particle.initial_velocity = v;
}
uniform float m1_u_radius = 20.0;

void m1_shape_location_ring(inout Particle particle) {
	float r = rand_range(particle.seed, 0.000000, 1.000000);
	float u = rand_range(particle.seed, 0.000000, 1.000000);
	float v = rand_range(particle.seed, 0.000000, 1.000000);
	vec3 h = vec3(1.000000, 0.000000, 0.000000);
	vec3 up = vec3(0.000000, 1.000000, 0.000000);
	h = rotated(h, up, u * 360.0 * DEG_TO_RAD) * vec3(1.000000, 1.000000, 1.000000);
	float radius_edge = tan(0.000000 * DEG_TO_RAD) * 0.010000;
	vec3 offset = vec3(0.000000, 0.000000, 0.000000);
	particle.spawn_direction = normalize(h * r * (m1_u_radius + radius_edge) + 0.010000 * up) + offset;
	particle.initial_position = h * r * (m1_u_radius + radius_edge * v) + v * 0.010000 * up + offset;
}

void m2_collision(inout Particle particle) {
	if (particle.collided) {
		float collision_response = dot(particle.collision_normal, particle.velocity);
		float slide_to_bounce_trigger = step(2.0 / clamp(0.000000 + 1.0, 1.0, 2.0), abs(collision_response));
		particle.position += particle.collision_normal * particle.collision_depth;
		// Remove all components of velocity that are not tangential to COLLISION_NORMAL.
		particle.velocity -= particle.collision_normal * collision_response;
		// Apply friction only to velocity across the surface (effectively decouples friction and bounce behavior).
		particle.velocity = mix(particle.velocity, vec3(0.0), clamp(1.000000, 0.0, 1.0));
		// Add bounce velocity to velocity.
		particle.velocity -= particle.collision_normal * collision_response * (0.000000 * slide_to_bounce_trigger);
	}
}


void initialize(inout Particle particle) {
	particle.initial_position = vec3(0.0);
	particle.initial_color = vec4(1.0);
	particle.initial_scale = vec3(1.0);
	particle.initial_velocity = vec3(0.0);
	particle.initial_orbital_speed = 0.0;
	particle.spawn_direction = vec3(0.0);
	particle.animation_offset = 0.0;
	particle.animation_speed = 1.0;
	particle.rotation_axis = vec3(1.0, 0.0, 0.0);
	particle.initial_rotation = 0.0;
	particle.radial_velocity = 0.0;
	particle.orbital_axis = vec3(0.0, 1.0, 0.0);
	particle.orbital_velocity = vec3(0.0);
	m0_initialize_velocity(particle);
	m1_shape_location_ring(particle);
	particle.scale = particle.initial_scale;
	particle.color = particle.initial_color;
	particle.orbital_speed = particle.initial_orbital_speed;
}

void start() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = vec3(0.0);
	initialize(particle);
	if (rand(particle.seed) > AMOUNT_RATIO) {
		ACTIVE = false;
	}
	if (RESTART_CUSTOM) {
		CUSTOM = vec4(0.0);
		CUSTOM.x = particle.initial_rotation;
	}
	if (RESTART_COLOR) {
		COLOR = particle.initial_color;
	}
	if (RESTART_ROT_SCALE) {
		TRANSFORM[0].xyz = rotated(vec3(1.0, 0.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.x;
		TRANSFORM[1].xyz = rotated(vec3(0.0, 1.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.y;
		TRANSFORM[2].xyz = rotated(vec3(0.0, 0.0, 1.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.z;
	}
	if (RESTART_POSITION) {
		TRANSFORM[3].xyz = particle.initial_position;
		TRANSFORM = EMISSION_TRANSFORM * TRANSFORM;
	}
	if (RESTART_VELOCITY) {
		VELOCITY = (EMISSION_TRANSFORM * vec4(particle.initial_velocity, 0.0)).xyz;
		VELOCITY += EMITTER_VELOCITY * 0.0;//inherit_emitter_velocity_ratio;
	}
}

void process() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.delta = DELTA;
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = TRANSFORM[3].xyz;
	particle.velocity = VELOCITY;
	particle.orientation = mat3(
		normalize(TRANSFORM[0].xyz),
		normalize(TRANSFORM[1].xyz),
		normalize(TRANSFORM[2].xyz));
	particle.force = vec3(0.0);
	particle.emission_transform = EMISSION_TRANSFORM;
	particle.previous_transform = TRANSFORM;
	particle.rotation = CUSTOM.x;
	particle.just_spawned = CUSTOM.y == 0.0;
	particle.collided = COLLIDED;
	particle.collision_normal = COLLISION_NORMAL;
	particle.collision_depth = COLLISION_DEPTH;
	initialize(particle);
	CUSTOM.y += DELTA / particle.lifetime;
	CUSTOM.y = mix(CUSTOM.y, 1.0, INTERPOLATE_TO_END);
	CUSTOM.y = clamp(CUSTOM.y, 0.0, 1.0);
	particle.age = CUSTOM.y * particle.lifetime;
	particle.normalized_age = CUSTOM.y;
	m2_collision(particle);
	if (abs(particle.orbital_speed) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
		particle.orbital_velocity = (next - local_pos) / particle.delta;
	}
	if (abs(particle.radial_velocity) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 d = local_pos;
		float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
		vec3 next = local_pos + normalize_or_zero(d) * mag * sign(particle.radial_velocity);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
	}
	particle.position += particle.velocity * particle.delta;
	VELOCITY = particle.velocity;
	TRANSFORM[0].xyz = particle.orientation[0] * particle.scale.x;
	TRANSFORM[1].xyz = particle.orientation[1] * particle.scale.y;
	TRANSFORM[2].xyz = particle.orientation[2] * particle.scale.z;
	TRANSFORM[3].xyz = particle.position;
	COLOR = particle.color;
	CUSTOM.x = particle.rotation;
	
	if (particle.collided) {
		ACTIVE = false;
		for (int i = 0; i < 6; i++) emit_subparticle(TRANSFORM, vec3(0.0), vec4(0.0), vec4(0.0), FLAG_EMIT_POSITION);
	}
	if (particle.age >= LIFETIME) { ACTIVE = false; 		for (int i = 0; i < 6; i++) emit_subparticle(TRANSFORM, vec3(0.0), vec4(0.0), vec4(0.0), FLAG_EMIT_POSITION);
 }
}

"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_ry011"]
shader = SubResource("Shader_so1j8")
shader_parameter/m1_u_radius = 8.0

[sub_resource type="Shader" id="Shader_3lnob"]
code = "shader_type particles;
render_mode disable_velocity;

const float DEG_TO_RAD = PI / 180.0;
const uint AMOUNT = uint(24);

struct Particle {
	uint seed;
	float delta;
	float lifetime;
	uint index;
	uint number;
	float age;
	float normalized_age;
	float normalized_index;
	float normalized_number;
	vec3 initial_position;
	vec3 initial_scale;
	vec4 initial_color;
	vec3 initial_velocity;
	float radial_velocity;
	vec3 orbital_axis;
	float initial_orbital_speed;
	vec3 spawn_direction;
	vec3 position;
	vec3 scale;
	vec4 color;
	vec3 velocity;
	vec3 orbital_velocity;
	float orbital_speed;
	vec3 rotation_axis;
	float initial_rotation;
	float rotation;
	mat3 orientation;
	vec3 force;
	float animation_offset;
	float animation_speed;
	mat4 emission_transform;
	mat4 previous_transform;
	bool just_spawned;
	bool collided;
	vec3 collision_normal;
	float collision_depth;
};

float remap(float value, float input_min, float input_max, float output_min, float output_max) {
	float v = clamp(value, input_min, input_max);
	return (v - input_min) / (input_max - input_min) * (output_max - output_min) + output_min;
}

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}

uint hash(uint x) {
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = (x >> uint(16)) ^ x;
	return x;
}

vec3 normalize_or_zero(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 0.0, 0.0);
}

vec3 normalize_or_up(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 1.0, 0.0);
}

vec4 quat(vec3 axis, float angle) {
  float s = sin(angle / 2.0);
  return vec4(axis.x * s, axis.y * s, axis.z * s, cos(angle / 2.0));
}

vec4 quat_rotation_to(vec3 from, vec3 to) {
	vec3 xunit = vec3(1.0, 0.0, 0.0);
	vec3 yunit = vec3(0.0, 1.0, 0.0);
	float dot = dot(from, to);
	if (dot < -0.999999) {
	    vec3 axis = cross(xunit, from);
	    if (length(axis) < 0.000001) {
	    	axis = cross(yunit, from);
	    }
	    axis = normalize(axis);
	    return quat(axis, PI);
	} else if (dot > 0.999999) {
		return vec4(0.0, 0.0, 0.0, 1.0);
	} else {
		vec3 axis = cross(from, to);
		return normalize(vec4(axis, 1.0 + dot));
	}
}

vec3 rotated(vec3 v, vec3 axis, float angle) {
  vec4 q = quat(axis, angle);
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

vec3 rotated(vec3 v, vec4 q) {
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

mat3 rotated(mat3 m, vec4 q) {
  return mat3(rotated(m[0], q), rotated(m[1], q), rotated(m[2], q));
}

float normalized_by_amount(uint n) {
	return float(n) / float(max(AMOUNT, 1u));
}
void update_orbital_velocity(inout Particle particle, mat4 emission_transform, mat4 transform) {
	if (abs(particle.orbital_speed) < 0.01 || particle.delta < 0.001) { return; }
	vec3 local_pos = (inverse(emission_transform) * transform[3]).xyz;
	vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
	particle.position = (emission_transform * vec4(next, 0.0)).xyz;
	particle.orbital_velocity = (next - local_pos) / particle.delta;
}

void apply_radial_velocity(inout Particle particle) {
	vec3 d = particle.position;
	float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
	particle.position += normalize_or_zero(d) * mag * sign(particle.radial_velocity);
}

void m0_initialize_velocity(inout Particle particle) {
	float v_x = rand_range(particle.seed, 0.000000, 0.000000);
	float v_y = rand_range(particle.seed, -30.000000, -30.000000);
	float v_z = rand_range(particle.seed, 5.000000, 8.000000);
	vec3 v = vec3(v_x, v_y, v_z);
	particle.initial_velocity = v;
}
uniform float m1_u_radius = 20.0;

void m1_shape_location_ring(inout Particle particle) {
	float r = rand_range(particle.seed, 0.000000, 1.000000);
	float u = rand_range(particle.seed, 0.000000, 1.000000);
	float v = rand_range(particle.seed, 0.000000, 1.000000);
	vec3 h = vec3(1.000000, 0.000000, 0.000000);
	vec3 up = vec3(0.000000, 1.000000, 0.000000);
	h = rotated(h, up, u * 360.0 * DEG_TO_RAD) * vec3(1.000000, 1.000000, 1.000000);
	float radius_edge = tan(0.000000 * DEG_TO_RAD) * 0.010000;
	vec3 offset = vec3(0.000000, 0.000000, 0.000000);
	particle.spawn_direction = normalize(h * r * (m1_u_radius + radius_edge) + 0.010000 * up) + offset;
	particle.initial_position = h * r * (m1_u_radius + radius_edge * v) + v * 0.010000 * up + offset;
}

void m2_collision(inout Particle particle) {
	if (particle.collided) {
		float collision_response = dot(particle.collision_normal, particle.velocity);
		float slide_to_bounce_trigger = step(2.0 / clamp(0.000000 + 1.0, 1.0, 2.0), abs(collision_response));
		particle.position += particle.collision_normal * particle.collision_depth;
		// Remove all components of velocity that are not tangential to COLLISION_NORMAL.
		particle.velocity -= particle.collision_normal * collision_response;
		// Apply friction only to velocity across the surface (effectively decouples friction and bounce behavior).
		particle.velocity = mix(particle.velocity, vec3(0.0), clamp(1.000000, 0.0, 1.0));
		// Add bounce velocity to velocity.
		particle.velocity -= particle.collision_normal * collision_response * (0.000000 * slide_to_bounce_trigger);
	}
}


void initialize(inout Particle particle) {
	particle.initial_position = vec3(0.0);
	particle.initial_color = vec4(1.0);
	particle.initial_scale = vec3(1.0);
	particle.initial_velocity = vec3(0.0);
	particle.initial_orbital_speed = 0.0;
	particle.spawn_direction = vec3(0.0);
	particle.animation_offset = 0.0;
	particle.animation_speed = 1.0;
	particle.rotation_axis = vec3(1.0, 0.0, 0.0);
	particle.initial_rotation = 0.0;
	particle.radial_velocity = 0.0;
	particle.orbital_axis = vec3(0.0, 1.0, 0.0);
	particle.orbital_velocity = vec3(0.0);
	m0_initialize_velocity(particle);
	m1_shape_location_ring(particle);
	particle.scale = particle.initial_scale;
	particle.color = particle.initial_color;
	particle.orbital_speed = particle.initial_orbital_speed;
}

void start() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = vec3(0.0);
	initialize(particle);
	if (rand(particle.seed) > AMOUNT_RATIO) {
		ACTIVE = false;
	}
	if (RESTART_CUSTOM) {
		CUSTOM = vec4(0.0);
		CUSTOM.x = particle.initial_rotation;
	}
	if (RESTART_COLOR) {
		COLOR = particle.initial_color;
	}
	if (RESTART_ROT_SCALE) {
		TRANSFORM[0].xyz = rotated(vec3(1.0, 0.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.x;
		TRANSFORM[1].xyz = rotated(vec3(0.0, 1.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.y;
		TRANSFORM[2].xyz = rotated(vec3(0.0, 0.0, 1.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.z;
	}
	if (RESTART_POSITION) {
		TRANSFORM[3].xyz = particle.initial_position;
		TRANSFORM = EMISSION_TRANSFORM * TRANSFORM;
	}
	if (RESTART_VELOCITY) {
		VELOCITY = (EMISSION_TRANSFORM * vec4(particle.initial_velocity, 0.0)).xyz;
		VELOCITY += EMITTER_VELOCITY * 0.0;//inherit_emitter_velocity_ratio;
	}
}

void process() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.delta = DELTA;
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = TRANSFORM[3].xyz;
	particle.velocity = VELOCITY;
	particle.orientation = mat3(
		normalize(TRANSFORM[0].xyz),
		normalize(TRANSFORM[1].xyz),
		normalize(TRANSFORM[2].xyz));
	particle.force = vec3(0.0);
	particle.emission_transform = EMISSION_TRANSFORM;
	particle.previous_transform = TRANSFORM;
	particle.rotation = CUSTOM.x;
	particle.just_spawned = CUSTOM.y == 0.0;
	particle.collided = COLLIDED;
	particle.collision_normal = COLLISION_NORMAL;
	particle.collision_depth = COLLISION_DEPTH;
	initialize(particle);
	CUSTOM.y += DELTA / particle.lifetime;
	CUSTOM.y = mix(CUSTOM.y, 1.0, INTERPOLATE_TO_END);
	CUSTOM.y = clamp(CUSTOM.y, 0.0, 1.0);
	particle.age = CUSTOM.y * particle.lifetime;
	particle.normalized_age = CUSTOM.y;
	m2_collision(particle);
	if (abs(particle.orbital_speed) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
		particle.orbital_velocity = (next - local_pos) / particle.delta;
	}
	if (abs(particle.radial_velocity) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 d = local_pos;
		float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
		vec3 next = local_pos + normalize_or_zero(d) * mag * sign(particle.radial_velocity);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
	}
	particle.position += particle.velocity * particle.delta;
	VELOCITY = particle.velocity;
	TRANSFORM[0].xyz = particle.orientation[0] * particle.scale.x;
	TRANSFORM[1].xyz = particle.orientation[1] * particle.scale.y;
	TRANSFORM[2].xyz = particle.orientation[2] * particle.scale.z;
	TRANSFORM[3].xyz = particle.position;
	COLOR = particle.color;
	CUSTOM.x = particle.rotation;
	
	if (particle.collided && CUSTOM.w == 0.0) {
		CUSTOM.w = 1.0;
		for (int i = 0; i < 1; i++) { emit_subparticle(TRANSFORM, vec3(0.0), vec4(0.0), vec4(0.0), FLAG_EMIT_POSITION); }
	}
	if (particle.collided) {
		ACTIVE = false;
	}
	if (particle.age >= LIFETIME) { ACTIVE = false;  }
}

"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_t8qsk"]
shader = SubResource("Shader_3lnob")
shader_parameter/m1_u_radius = 8.0

[sub_resource type="Shader" id="Shader_tsftg"]
code = "shader_type particles;
render_mode disable_velocity;

const float DEG_TO_RAD = PI / 180.0;
const uint AMOUNT = uint(24);

struct Particle {
	uint seed;
	float delta;
	float lifetime;
	uint index;
	uint number;
	float age;
	float normalized_age;
	float normalized_index;
	float normalized_number;
	vec3 initial_position;
	vec3 initial_scale;
	vec4 initial_color;
	vec3 initial_velocity;
	float radial_velocity;
	vec3 orbital_axis;
	float initial_orbital_speed;
	vec3 spawn_direction;
	vec3 position;
	vec3 scale;
	vec4 color;
	vec3 velocity;
	vec3 orbital_velocity;
	float orbital_speed;
	vec3 rotation_axis;
	float initial_rotation;
	float rotation;
	mat3 orientation;
	vec3 force;
	float animation_offset;
	float animation_speed;
	mat4 emission_transform;
	mat4 previous_transform;
	bool just_spawned;
	bool collided;
	vec3 collision_normal;
	float collision_depth;
};

float remap(float value, float input_min, float input_max, float output_min, float output_max) {
	float v = clamp(value, input_min, input_max);
	return (v - input_min) / (input_max - input_min) * (output_max - output_min) + output_min;
}

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}

uint hash(uint x) {
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = (x >> uint(16)) ^ x;
	return x;
}

vec3 normalize_or_zero(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 0.0, 0.0);
}

vec3 normalize_or_up(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 1.0, 0.0);
}

vec4 quat(vec3 axis, float angle) {
  float s = sin(angle / 2.0);
  return vec4(axis.x * s, axis.y * s, axis.z * s, cos(angle / 2.0));
}

vec4 quat_rotation_to(vec3 from, vec3 to) {
	vec3 xunit = vec3(1.0, 0.0, 0.0);
	vec3 yunit = vec3(0.0, 1.0, 0.0);
	float dot = dot(from, to);
	if (dot < -0.999999) {
	    vec3 axis = cross(xunit, from);
	    if (length(axis) < 0.000001) {
	    	axis = cross(yunit, from);
	    }
	    axis = normalize(axis);
	    return quat(axis, PI);
	} else if (dot > 0.999999) {
		return vec4(0.0, 0.0, 0.0, 1.0);
	} else {
		vec3 axis = cross(from, to);
		return normalize(vec4(axis, 1.0 + dot));
	}
}

vec3 rotated(vec3 v, vec3 axis, float angle) {
  vec4 q = quat(axis, angle);
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

vec3 rotated(vec3 v, vec4 q) {
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

mat3 rotated(mat3 m, vec4 q) {
  return mat3(rotated(m[0], q), rotated(m[1], q), rotated(m[2], q));
}

float normalized_by_amount(uint n) {
	return float(n) / float(max(AMOUNT, 1u));
}
void update_orbital_velocity(inout Particle particle, mat4 emission_transform, mat4 transform) {
	if (abs(particle.orbital_speed) < 0.01 || particle.delta < 0.001) { return; }
	vec3 local_pos = (inverse(emission_transform) * transform[3]).xyz;
	vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
	particle.position = (emission_transform * vec4(next, 0.0)).xyz;
	particle.orbital_velocity = (next - local_pos) / particle.delta;
}

void apply_radial_velocity(inout Particle particle) {
	vec3 d = particle.position;
	float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
	particle.position += normalize_or_zero(d) * mag * sign(particle.radial_velocity);
}

void m0_initialize_velocity(inout Particle particle) {
	float v_x = rand_range(particle.seed, 0.000000, 0.000000);
	float v_y = rand_range(particle.seed, -30.000000, -30.000000);
	float v_z = rand_range(particle.seed, 5.000000, 8.000000);
	vec3 v = vec3(v_x, v_y, v_z);
	particle.initial_velocity = v;
}
uniform float m1_u_radius = 20.0;

void m1_shape_location_ring(inout Particle particle) {
	float r = rand_range(particle.seed, 0.000000, 1.000000);
	float u = rand_range(particle.seed, 0.000000, 1.000000);
	float v = rand_range(particle.seed, 0.000000, 1.000000);
	vec3 h = vec3(1.000000, 0.000000, 0.000000);
	vec3 up = vec3(0.000000, 1.000000, 0.000000);
	h = rotated(h, up, u * 360.0 * DEG_TO_RAD) * vec3(1.000000, 1.000000, 1.000000);
	float radius_edge = tan(0.000000 * DEG_TO_RAD) * 0.010000;
	vec3 offset = vec3(0.000000, 0.000000, 0.000000);
	particle.spawn_direction = normalize(h * r * (m1_u_radius + radius_edge) + 0.010000 * up) + offset;
	particle.initial_position = h * r * (m1_u_radius + radius_edge * v) + v * 0.010000 * up + offset;
}

void m2_collision(inout Particle particle) {
	if (particle.collided) {
		float collision_response = dot(particle.collision_normal, particle.velocity);
		float slide_to_bounce_trigger = step(2.0 / clamp(0.000000 + 1.0, 1.0, 2.0), abs(collision_response));
		particle.position += particle.collision_normal * particle.collision_depth;
		// Remove all components of velocity that are not tangential to COLLISION_NORMAL.
		particle.velocity -= particle.collision_normal * collision_response;
		// Apply friction only to velocity across the surface (effectively decouples friction and bounce behavior).
		particle.velocity = mix(particle.velocity, vec3(0.0), clamp(1.000000, 0.0, 1.0));
		// Add bounce velocity to velocity.
		particle.velocity -= particle.collision_normal * collision_response * (0.000000 * slide_to_bounce_trigger);
	}
}


void initialize(inout Particle particle) {
	particle.initial_position = vec3(0.0);
	particle.initial_color = vec4(1.0);
	particle.initial_scale = vec3(1.0);
	particle.initial_velocity = vec3(0.0);
	particle.initial_orbital_speed = 0.0;
	particle.spawn_direction = vec3(0.0);
	particle.animation_offset = 0.0;
	particle.animation_speed = 1.0;
	particle.rotation_axis = vec3(1.0, 0.0, 0.0);
	particle.initial_rotation = 0.0;
	particle.radial_velocity = 0.0;
	particle.orbital_axis = vec3(0.0, 1.0, 0.0);
	particle.orbital_velocity = vec3(0.0);
	m0_initialize_velocity(particle);
	m1_shape_location_ring(particle);
	particle.scale = particle.initial_scale;
	particle.color = particle.initial_color;
	particle.orbital_speed = particle.initial_orbital_speed;
}

void start() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = vec3(0.0);
	initialize(particle);
	if (rand(particle.seed) > AMOUNT_RATIO) {
		ACTIVE = false;
	}
	if (RESTART_CUSTOM) {
		CUSTOM = vec4(0.0);
		CUSTOM.x = particle.initial_rotation;
	}
	if (RESTART_COLOR) {
		COLOR = particle.initial_color;
	}
	if (RESTART_ROT_SCALE) {
		TRANSFORM[0].xyz = rotated(vec3(1.0, 0.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.x;
		TRANSFORM[1].xyz = rotated(vec3(0.0, 1.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.y;
		TRANSFORM[2].xyz = rotated(vec3(0.0, 0.0, 1.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.z;
	}
	if (RESTART_POSITION) {
		TRANSFORM[3].xyz = particle.initial_position;
		TRANSFORM = EMISSION_TRANSFORM * TRANSFORM;
	}
	if (RESTART_VELOCITY) {
		VELOCITY = (EMISSION_TRANSFORM * vec4(particle.initial_velocity, 0.0)).xyz;
		VELOCITY += EMITTER_VELOCITY * 0.0;//inherit_emitter_velocity_ratio;
	}
}

void process() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.delta = DELTA;
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = TRANSFORM[3].xyz;
	particle.velocity = VELOCITY;
	particle.orientation = mat3(
		normalize(TRANSFORM[0].xyz),
		normalize(TRANSFORM[1].xyz),
		normalize(TRANSFORM[2].xyz));
	particle.force = vec3(0.0);
	particle.emission_transform = EMISSION_TRANSFORM;
	particle.previous_transform = TRANSFORM;
	particle.rotation = CUSTOM.x;
	particle.just_spawned = CUSTOM.y == 0.0;
	particle.collided = COLLIDED;
	particle.collision_normal = COLLISION_NORMAL;
	particle.collision_depth = COLLISION_DEPTH;
	initialize(particle);
	CUSTOM.y += DELTA / particle.lifetime;
	CUSTOM.y = mix(CUSTOM.y, 1.0, INTERPOLATE_TO_END);
	CUSTOM.y = clamp(CUSTOM.y, 0.0, 1.0);
	particle.age = CUSTOM.y * particle.lifetime;
	particle.normalized_age = CUSTOM.y;
	m2_collision(particle);
	if (abs(particle.orbital_speed) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
		particle.orbital_velocity = (next - local_pos) / particle.delta;
	}
	if (abs(particle.radial_velocity) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 d = local_pos;
		float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
		vec3 next = local_pos + normalize_or_zero(d) * mag * sign(particle.radial_velocity);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
	}
	particle.position += particle.velocity * particle.delta;
	VELOCITY = particle.velocity;
	TRANSFORM[0].xyz = particle.orientation[0] * particle.scale.x;
	TRANSFORM[1].xyz = particle.orientation[1] * particle.scale.y;
	TRANSFORM[2].xyz = particle.orientation[2] * particle.scale.z;
	TRANSFORM[3].xyz = particle.position;
	COLOR = particle.color;
	CUSTOM.x = particle.rotation;
	
	if (particle.collided && CUSTOM.w == 0.0) {
		CUSTOM.w = 1.0;
		for (int i = 0; i < 1; i++) { emit_subparticle(TRANSFORM, vec3(0.0), vec4(0.0), vec4(0.0), FLAG_EMIT_POSITION); }
	}
	if (particle.collided) {
		ACTIVE = false;
	}
	if (particle.age >= LIFETIME) { ACTIVE = false;  }
}

"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_ubj0s"]
shader = SubResource("Shader_tsftg")
shader_parameter/m1_u_radius = 8.0

[sub_resource type="Shader" id="Shader_y7o3b"]
code = "shader_type particles;
render_mode disable_velocity;

const float DEG_TO_RAD = PI / 180.0;
const uint AMOUNT = uint(24);

struct Particle {
	uint seed;
	float delta;
	float lifetime;
	uint index;
	uint number;
	float age;
	float normalized_age;
	float normalized_index;
	float normalized_number;
	vec3 initial_position;
	vec3 initial_scale;
	vec4 initial_color;
	vec3 initial_velocity;
	float radial_velocity;
	vec3 orbital_axis;
	float initial_orbital_speed;
	vec3 spawn_direction;
	vec3 position;
	vec3 scale;
	vec4 color;
	vec3 velocity;
	vec3 orbital_velocity;
	float orbital_speed;
	vec3 rotation_axis;
	float initial_rotation;
	float rotation;
	mat3 orientation;
	vec3 force;
	float animation_offset;
	float animation_speed;
	mat4 emission_transform;
	mat4 previous_transform;
	bool just_spawned;
	bool collided;
	vec3 collision_normal;
	float collision_depth;
};

float remap(float value, float input_min, float input_max, float output_min, float output_max) {
	float v = clamp(value, input_min, input_max);
	return (v - input_min) / (input_max - input_min) * (output_max - output_min) + output_min;
}

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}

uint hash(uint x) {
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = (x >> uint(16)) ^ x;
	return x;
}

vec3 normalize_or_zero(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 0.0, 0.0);
}

vec3 normalize_or_up(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 1.0, 0.0);
}

vec4 quat(vec3 axis, float angle) {
  float s = sin(angle / 2.0);
  return vec4(axis.x * s, axis.y * s, axis.z * s, cos(angle / 2.0));
}

vec4 quat_rotation_to(vec3 from, vec3 to) {
	vec3 xunit = vec3(1.0, 0.0, 0.0);
	vec3 yunit = vec3(0.0, 1.0, 0.0);
	float dot = dot(from, to);
	if (dot < -0.999999) {
	    vec3 axis = cross(xunit, from);
	    if (length(axis) < 0.000001) {
	    	axis = cross(yunit, from);
	    }
	    axis = normalize(axis);
	    return quat(axis, PI);
	} else if (dot > 0.999999) {
		return vec4(0.0, 0.0, 0.0, 1.0);
	} else {
		vec3 axis = cross(from, to);
		return normalize(vec4(axis, 1.0 + dot));
	}
}

vec3 rotated(vec3 v, vec3 axis, float angle) {
  vec4 q = quat(axis, angle);
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

vec3 rotated(vec3 v, vec4 q) {
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

mat3 rotated(mat3 m, vec4 q) {
  return mat3(rotated(m[0], q), rotated(m[1], q), rotated(m[2], q));
}

float normalized_by_amount(uint n) {
	return float(n) / float(max(AMOUNT, 1u));
}
void update_orbital_velocity(inout Particle particle, mat4 emission_transform, mat4 transform) {
	if (abs(particle.orbital_speed) < 0.01 || particle.delta < 0.001) { return; }
	vec3 local_pos = (inverse(emission_transform) * transform[3]).xyz;
	vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
	particle.position = (emission_transform * vec4(next, 0.0)).xyz;
	particle.orbital_velocity = (next - local_pos) / particle.delta;
}

void apply_radial_velocity(inout Particle particle) {
	vec3 d = particle.position;
	float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
	particle.position += normalize_or_zero(d) * mag * sign(particle.radial_velocity);
}

void m0_initialize_velocity(inout Particle particle) {
	float v_x = rand_range(particle.seed, 0.000000, 0.000000);
	float v_y = rand_range(particle.seed, -30.000000, -30.000000);
	float v_z = rand_range(particle.seed, 5.000000, 8.000000);
	vec3 v = vec3(v_x, v_y, v_z);
	particle.initial_velocity = v;
}
uniform float m1_u_radius = 20.0;

void m1_shape_location_ring(inout Particle particle) {
	float r = rand_range(particle.seed, 0.000000, 1.000000);
	float u = rand_range(particle.seed, 0.000000, 1.000000);
	float v = rand_range(particle.seed, 0.000000, 1.000000);
	vec3 h = vec3(1.000000, 0.000000, 0.000000);
	vec3 up = vec3(0.000000, 1.000000, 0.000000);
	h = rotated(h, up, u * 360.0 * DEG_TO_RAD) * vec3(1.000000, 1.000000, 1.000000);
	float radius_edge = tan(0.000000 * DEG_TO_RAD) * 0.010000;
	vec3 offset = vec3(0.000000, 0.000000, 0.000000);
	particle.spawn_direction = normalize(h * r * (m1_u_radius + radius_edge) + 0.010000 * up) + offset;
	particle.initial_position = h * r * (m1_u_radius + radius_edge * v) + v * 0.010000 * up + offset;
}

void m2_collision(inout Particle particle) {
	if (particle.collided) {
		float collision_response = dot(particle.collision_normal, particle.velocity);
		float slide_to_bounce_trigger = step(2.0 / clamp(0.000000 + 1.0, 1.0, 2.0), abs(collision_response));
		particle.position += particle.collision_normal * particle.collision_depth;
		// Remove all components of velocity that are not tangential to COLLISION_NORMAL.
		particle.velocity -= particle.collision_normal * collision_response;
		// Apply friction only to velocity across the surface (effectively decouples friction and bounce behavior).
		particle.velocity = mix(particle.velocity, vec3(0.0), clamp(1.000000, 0.0, 1.0));
		// Add bounce velocity to velocity.
		particle.velocity -= particle.collision_normal * collision_response * (0.000000 * slide_to_bounce_trigger);
	}
}


void initialize(inout Particle particle) {
	particle.initial_position = vec3(0.0);
	particle.initial_color = vec4(1.0);
	particle.initial_scale = vec3(1.0);
	particle.initial_velocity = vec3(0.0);
	particle.initial_orbital_speed = 0.0;
	particle.spawn_direction = vec3(0.0);
	particle.animation_offset = 0.0;
	particle.animation_speed = 1.0;
	particle.rotation_axis = vec3(1.0, 0.0, 0.0);
	particle.initial_rotation = 0.0;
	particle.radial_velocity = 0.0;
	particle.orbital_axis = vec3(0.0, 1.0, 0.0);
	particle.orbital_velocity = vec3(0.0);
	m0_initialize_velocity(particle);
	m1_shape_location_ring(particle);
	particle.scale = particle.initial_scale;
	particle.color = particle.initial_color;
	particle.orbital_speed = particle.initial_orbital_speed;
}

void start() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = vec3(0.0);
	initialize(particle);
	if (rand(particle.seed) > AMOUNT_RATIO) {
		ACTIVE = false;
	}
	if (RESTART_CUSTOM) {
		CUSTOM = vec4(0.0);
		CUSTOM.x = particle.initial_rotation;
	}
	if (RESTART_COLOR) {
		COLOR = particle.initial_color;
	}
	if (RESTART_ROT_SCALE) {
		TRANSFORM[0].xyz = rotated(vec3(1.0, 0.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.x;
		TRANSFORM[1].xyz = rotated(vec3(0.0, 1.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.y;
		TRANSFORM[2].xyz = rotated(vec3(0.0, 0.0, 1.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.z;
	}
	if (RESTART_POSITION) {
		TRANSFORM[3].xyz = particle.initial_position;
		TRANSFORM = EMISSION_TRANSFORM * TRANSFORM;
	}
	if (RESTART_VELOCITY) {
		VELOCITY = (EMISSION_TRANSFORM * vec4(particle.initial_velocity, 0.0)).xyz;
		VELOCITY += EMITTER_VELOCITY * 0.0;//inherit_emitter_velocity_ratio;
	}
}

void process() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.delta = DELTA;
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = TRANSFORM[3].xyz;
	particle.velocity = VELOCITY;
	particle.orientation = mat3(
		normalize(TRANSFORM[0].xyz),
		normalize(TRANSFORM[1].xyz),
		normalize(TRANSFORM[2].xyz));
	particle.force = vec3(0.0);
	particle.emission_transform = EMISSION_TRANSFORM;
	particle.previous_transform = TRANSFORM;
	particle.rotation = CUSTOM.x;
	particle.just_spawned = CUSTOM.y == 0.0;
	particle.collided = COLLIDED;
	particle.collision_normal = COLLISION_NORMAL;
	particle.collision_depth = COLLISION_DEPTH;
	initialize(particle);
	CUSTOM.y += DELTA / particle.lifetime;
	CUSTOM.y = mix(CUSTOM.y, 1.0, INTERPOLATE_TO_END);
	CUSTOM.y = clamp(CUSTOM.y, 0.0, 1.0);
	particle.age = CUSTOM.y * particle.lifetime;
	particle.normalized_age = CUSTOM.y;
	m2_collision(particle);
	if (abs(particle.orbital_speed) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
		particle.orbital_velocity = (next - local_pos) / particle.delta;
	}
	if (abs(particle.radial_velocity) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 d = local_pos;
		float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
		vec3 next = local_pos + normalize_or_zero(d) * mag * sign(particle.radial_velocity);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
	}
	particle.position += particle.velocity * particle.delta;
	VELOCITY = particle.velocity;
	TRANSFORM[0].xyz = particle.orientation[0] * particle.scale.x;
	TRANSFORM[1].xyz = particle.orientation[1] * particle.scale.y;
	TRANSFORM[2].xyz = particle.orientation[2] * particle.scale.z;
	TRANSFORM[3].xyz = particle.position;
	COLOR = particle.color;
	CUSTOM.x = particle.rotation;
	
	if (particle.collided && CUSTOM.w == 0.0) {
		CUSTOM.w = 1.0;
		for (int i = 0; i < 4; i++) { emit_subparticle(TRANSFORM, vec3(0.0), vec4(0.0), vec4(0.0), FLAG_EMIT_POSITION); }
	}
	if (particle.collided) {
		ACTIVE = false;
	}
	if (particle.age >= LIFETIME) { ACTIVE = false;  }
}

"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_rxgj7"]
shader = SubResource("Shader_y7o3b")
shader_parameter/m1_u_radius = 8.0

[sub_resource type="Shader" id="Shader_uwqvu"]
code = "shader_type particles;
render_mode disable_velocity;

const float DEG_TO_RAD = PI / 180.0;
const uint AMOUNT = uint(24);

struct Particle {
	uint seed;
	float delta;
	float lifetime;
	uint index;
	uint number;
	float age;
	float normalized_age;
	float normalized_index;
	float normalized_number;
	vec3 initial_position;
	vec3 initial_scale;
	vec4 initial_color;
	vec3 initial_velocity;
	float radial_velocity;
	vec3 orbital_axis;
	float initial_orbital_speed;
	vec3 spawn_direction;
	vec3 position;
	vec3 scale;
	vec4 color;
	vec3 velocity;
	vec3 orbital_velocity;
	float orbital_speed;
	vec3 rotation_axis;
	float initial_rotation;
	float rotation;
	mat3 orientation;
	vec3 force;
	float animation_offset;
	float animation_speed;
	mat4 emission_transform;
	mat4 previous_transform;
	bool just_spawned;
	bool collided;
	vec3 collision_normal;
	float collision_depth;
};

float remap(float value, float input_min, float input_max, float output_min, float output_max) {
	float v = clamp(value, input_min, input_max);
	return (v - input_min) / (input_max - input_min) * (output_max - output_min) + output_min;
}

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}

uint hash(uint x) {
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = (x >> uint(16)) ^ x;
	return x;
}

vec3 normalize_or_zero(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 0.0, 0.0);
}

vec3 normalize_or_up(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 1.0, 0.0);
}

vec4 quat(vec3 axis, float angle) {
  float s = sin(angle / 2.0);
  return vec4(axis.x * s, axis.y * s, axis.z * s, cos(angle / 2.0));
}

vec4 quat_rotation_to(vec3 from, vec3 to) {
	vec3 xunit = vec3(1.0, 0.0, 0.0);
	vec3 yunit = vec3(0.0, 1.0, 0.0);
	float dot = dot(from, to);
	if (dot < -0.999999) {
	    vec3 axis = cross(xunit, from);
	    if (length(axis) < 0.000001) {
	    	axis = cross(yunit, from);
	    }
	    axis = normalize(axis);
	    return quat(axis, PI);
	} else if (dot > 0.999999) {
		return vec4(0.0, 0.0, 0.0, 1.0);
	} else {
		vec3 axis = cross(from, to);
		return normalize(vec4(axis, 1.0 + dot));
	}
}

vec3 rotated(vec3 v, vec3 axis, float angle) {
  vec4 q = quat(axis, angle);
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

vec3 rotated(vec3 v, vec4 q) {
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

mat3 rotated(mat3 m, vec4 q) {
  return mat3(rotated(m[0], q), rotated(m[1], q), rotated(m[2], q));
}

float normalized_by_amount(uint n) {
	return float(n) / float(max(AMOUNT, 1u));
}
void update_orbital_velocity(inout Particle particle, mat4 emission_transform, mat4 transform) {
	if (abs(particle.orbital_speed) < 0.01 || particle.delta < 0.001) { return; }
	vec3 local_pos = (inverse(emission_transform) * transform[3]).xyz;
	vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
	particle.position = (emission_transform * vec4(next, 0.0)).xyz;
	particle.orbital_velocity = (next - local_pos) / particle.delta;
}

void apply_radial_velocity(inout Particle particle) {
	vec3 d = particle.position;
	float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
	particle.position += normalize_or_zero(d) * mag * sign(particle.radial_velocity);
}

void m0_initialize_velocity(inout Particle particle) {
	float v_x = rand_range(particle.seed, 0.000000, 0.000000);
	float v_y = rand_range(particle.seed, -30.000000, -30.000000);
	float v_z = rand_range(particle.seed, 5.000000, 8.000000);
	vec3 v = vec3(v_x, v_y, v_z);
	particle.initial_velocity = v;
}
uniform float m1_u_radius = 20.0;

void m1_shape_location_ring(inout Particle particle) {
	float r = rand_range(particle.seed, 0.000000, 1.000000);
	float u = rand_range(particle.seed, 0.000000, 1.000000);
	float v = rand_range(particle.seed, 0.000000, 1.000000);
	vec3 h = vec3(1.000000, 0.000000, 0.000000);
	vec3 up = vec3(0.000000, 1.000000, 0.000000);
	h = rotated(h, up, u * 360.0 * DEG_TO_RAD) * vec3(1.000000, 1.000000, 1.000000);
	float radius_edge = tan(0.000000 * DEG_TO_RAD) * 0.010000;
	vec3 offset = vec3(0.000000, 0.000000, 0.000000);
	particle.spawn_direction = normalize(h * r * (m1_u_radius + radius_edge) + 0.010000 * up) + offset;
	particle.initial_position = h * r * (m1_u_radius + radius_edge * v) + v * 0.010000 * up + offset;
}

void m2_collision(inout Particle particle) {
	if (particle.collided) {
		float collision_response = dot(particle.collision_normal, particle.velocity);
		float slide_to_bounce_trigger = step(2.0 / clamp(0.000000 + 1.0, 1.0, 2.0), abs(collision_response));
		particle.position += particle.collision_normal * particle.collision_depth;
		// Remove all components of velocity that are not tangential to COLLISION_NORMAL.
		particle.velocity -= particle.collision_normal * collision_response;
		// Apply friction only to velocity across the surface (effectively decouples friction and bounce behavior).
		particle.velocity = mix(particle.velocity, vec3(0.0), clamp(1.000000, 0.0, 1.0));
		// Add bounce velocity to velocity.
		particle.velocity -= particle.collision_normal * collision_response * (0.000000 * slide_to_bounce_trigger);
	}
}


void initialize(inout Particle particle) {
	particle.initial_position = vec3(0.0);
	particle.initial_color = vec4(1.0);
	particle.initial_scale = vec3(1.0);
	particle.initial_velocity = vec3(0.0);
	particle.initial_orbital_speed = 0.0;
	particle.spawn_direction = vec3(0.0);
	particle.animation_offset = 0.0;
	particle.animation_speed = 1.0;
	particle.rotation_axis = vec3(1.0, 0.0, 0.0);
	particle.initial_rotation = 0.0;
	particle.radial_velocity = 0.0;
	particle.orbital_axis = vec3(0.0, 1.0, 0.0);
	particle.orbital_velocity = vec3(0.0);
	m0_initialize_velocity(particle);
	m1_shape_location_ring(particle);
	particle.scale = particle.initial_scale;
	particle.color = particle.initial_color;
	particle.orbital_speed = particle.initial_orbital_speed;
}

void start() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = vec3(0.0);
	initialize(particle);
	if (rand(particle.seed) > AMOUNT_RATIO) {
		ACTIVE = false;
	}
	if (RESTART_CUSTOM) {
		CUSTOM = vec4(0.0);
		CUSTOM.x = particle.initial_rotation;
	}
	if (RESTART_COLOR) {
		COLOR = particle.initial_color;
	}
	if (RESTART_ROT_SCALE) {
		TRANSFORM[0].xyz = rotated(vec3(1.0, 0.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.x;
		TRANSFORM[1].xyz = rotated(vec3(0.0, 1.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.y;
		TRANSFORM[2].xyz = rotated(vec3(0.0, 0.0, 1.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.z;
	}
	if (RESTART_POSITION) {
		TRANSFORM[3].xyz = particle.initial_position;
		TRANSFORM = EMISSION_TRANSFORM * TRANSFORM;
	}
	if (RESTART_VELOCITY) {
		VELOCITY = (EMISSION_TRANSFORM * vec4(particle.initial_velocity, 0.0)).xyz;
		VELOCITY += EMITTER_VELOCITY * 0.0;//inherit_emitter_velocity_ratio;
	}
}

void process() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.delta = DELTA;
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = TRANSFORM[3].xyz;
	particle.velocity = VELOCITY;
	particle.orientation = mat3(
		normalize(TRANSFORM[0].xyz),
		normalize(TRANSFORM[1].xyz),
		normalize(TRANSFORM[2].xyz));
	particle.force = vec3(0.0);
	particle.emission_transform = EMISSION_TRANSFORM;
	particle.previous_transform = TRANSFORM;
	particle.rotation = CUSTOM.x;
	particle.just_spawned = CUSTOM.y == 0.0;
	particle.collided = COLLIDED;
	particle.collision_normal = COLLISION_NORMAL;
	particle.collision_depth = COLLISION_DEPTH;
	initialize(particle);
	CUSTOM.y += DELTA / particle.lifetime;
	CUSTOM.y = mix(CUSTOM.y, 1.0, INTERPOLATE_TO_END);
	CUSTOM.y = clamp(CUSTOM.y, 0.0, 1.0);
	particle.age = CUSTOM.y * particle.lifetime;
	particle.normalized_age = CUSTOM.y;
	m2_collision(particle);
	if (abs(particle.orbital_speed) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
		particle.orbital_velocity = (next - local_pos) / particle.delta;
	}
	if (abs(particle.radial_velocity) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 d = local_pos;
		float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
		vec3 next = local_pos + normalize_or_zero(d) * mag * sign(particle.radial_velocity);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
	}
	particle.position += particle.velocity * particle.delta;
	VELOCITY = particle.velocity;
	TRANSFORM[0].xyz = particle.orientation[0] * particle.scale.x;
	TRANSFORM[1].xyz = particle.orientation[1] * particle.scale.y;
	TRANSFORM[2].xyz = particle.orientation[2] * particle.scale.z;
	TRANSFORM[3].xyz = particle.position;
	COLOR = particle.color;
	CUSTOM.x = particle.rotation;
	
	if (particle.collided && CUSTOM.w == 0.0) {
		CUSTOM.w = 1.0;
		for (int i = 0; i < 12; i++) { emit_subparticle(TRANSFORM, vec3(0.0), vec4(0.0), vec4(0.0, 0.0, max(0.0, particle.lifetime - particle.age), 0.0), FLAG_EMIT_POSITION | FLAG_EMIT_CUSTOM); }
	}
	if (particle.collided) {
		ACTIVE = false;
	}
	if (particle.age >= LIFETIME) { ACTIVE = false;  }
}

"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_kuk0d"]
shader = SubResource("Shader_uwqvu")
shader_parameter/m1_u_radius = 8.0

[sub_resource type="Shader" id="Shader_ty5rr"]
code = "shader_type particles;
render_mode disable_velocity;

const float DEG_TO_RAD = PI / 180.0;
const uint AMOUNT = uint(24);

struct Particle {
	uint seed;
	float delta;
	float lifetime;
	uint index;
	uint number;
	float age;
	float normalized_age;
	float normalized_index;
	float normalized_number;
	vec3 initial_position;
	vec3 initial_scale;
	vec4 initial_color;
	vec3 initial_velocity;
	float radial_velocity;
	vec3 orbital_axis;
	float initial_orbital_speed;
	vec3 spawn_direction;
	vec3 position;
	vec3 scale;
	vec4 color;
	vec3 velocity;
	vec3 orbital_velocity;
	float orbital_speed;
	vec3 rotation_axis;
	float initial_rotation;
	float rotation;
	mat3 orientation;
	vec3 force;
	float animation_offset;
	float animation_speed;
	mat4 emission_transform;
	mat4 previous_transform;
	bool just_spawned;
	bool collided;
	vec3 collision_normal;
	float collision_depth;
};

float remap(float value, float input_min, float input_max, float output_min, float output_max) {
	float v = clamp(value, input_min, input_max);
	return (v - input_min) / (input_max - input_min) * (output_max - output_min) + output_min;
}

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}

uint hash(uint x) {
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = (x >> uint(16)) ^ x;
	return x;
}

vec3 normalize_or_zero(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 0.0, 0.0);
}

vec3 normalize_or_up(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 1.0, 0.0);
}

vec4 quat(vec3 axis, float angle) {
  float s = sin(angle / 2.0);
  return vec4(axis.x * s, axis.y * s, axis.z * s, cos(angle / 2.0));
}

vec4 quat_rotation_to(vec3 from, vec3 to) {
	vec3 xunit = vec3(1.0, 0.0, 0.0);
	vec3 yunit = vec3(0.0, 1.0, 0.0);
	float dot = dot(from, to);
	if (dot < -0.999999) {
	    vec3 axis = cross(xunit, from);
	    if (length(axis) < 0.000001) {
	    	axis = cross(yunit, from);
	    }
	    axis = normalize(axis);
	    return quat(axis, PI);
	} else if (dot > 0.999999) {
		return vec4(0.0, 0.0, 0.0, 1.0);
	} else {
		vec3 axis = cross(from, to);
		return normalize(vec4(axis, 1.0 + dot));
	}
}

vec3 rotated(vec3 v, vec3 axis, float angle) {
  vec4 q = quat(axis, angle);
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

vec3 rotated(vec3 v, vec4 q) {
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

mat3 rotated(mat3 m, vec4 q) {
  return mat3(rotated(m[0], q), rotated(m[1], q), rotated(m[2], q));
}

float normalized_by_amount(uint n) {
	return float(n) / float(max(AMOUNT, 1u));
}
void update_orbital_velocity(inout Particle particle, mat4 emission_transform, mat4 transform) {
	if (abs(particle.orbital_speed) < 0.01 || particle.delta < 0.001) { return; }
	vec3 local_pos = (inverse(emission_transform) * transform[3]).xyz;
	vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
	particle.position = (emission_transform * vec4(next, 0.0)).xyz;
	particle.orbital_velocity = (next - local_pos) / particle.delta;
}

void apply_radial_velocity(inout Particle particle) {
	vec3 d = particle.position;
	float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
	particle.position += normalize_or_zero(d) * mag * sign(particle.radial_velocity);
}

void m0_initialize_velocity(inout Particle particle) {
	float v_x = rand_range(particle.seed, 0.000000, 0.000000);
	float v_y = rand_range(particle.seed, -30.000000, -30.000000);
	float v_z = rand_range(particle.seed, 5.000000, 8.000000);
	vec3 v = vec3(v_x, v_y, v_z);
	particle.initial_velocity = v;
}
uniform float m1_u_radius = 20.0;

void m1_shape_location_ring(inout Particle particle) {
	float r = rand_range(particle.seed, 0.000000, 1.000000);
	float u = rand_range(particle.seed, 0.000000, 1.000000);
	float v = rand_range(particle.seed, 0.000000, 1.000000);
	vec3 h = vec3(1.000000, 0.000000, 0.000000);
	vec3 up = vec3(0.000000, 1.000000, 0.000000);
	h = rotated(h, up, u * 360.0 * DEG_TO_RAD) * vec3(1.000000, 1.000000, 1.000000);
	float radius_edge = tan(0.000000 * DEG_TO_RAD) * 0.010000;
	vec3 offset = vec3(0.000000, 0.000000, 0.000000);
	particle.spawn_direction = normalize(h * r * (m1_u_radius + radius_edge) + 0.010000 * up) + offset;
	particle.initial_position = h * r * (m1_u_radius + radius_edge * v) + v * 0.010000 * up + offset;
}

void m2_collision(inout Particle particle) {
	if (particle.collided) {
		float collision_response = dot(particle.collision_normal, particle.velocity);
		float slide_to_bounce_trigger = step(2.0 / clamp(0.000000 + 1.0, 1.0, 2.0), abs(collision_response));
		particle.position += particle.collision_normal * particle.collision_depth;
		// Remove all components of velocity that are not tangential to COLLISION_NORMAL.
		particle.velocity -= particle.collision_normal * collision_response;
		// Apply friction only to velocity across the surface (effectively decouples friction and bounce behavior).
		particle.velocity = mix(particle.velocity, vec3(0.0), clamp(1.000000, 0.0, 1.0));
		// Add bounce velocity to velocity.
		particle.velocity -= particle.collision_normal * collision_response * (0.000000 * slide_to_bounce_trigger);
	}
}


void initialize(inout Particle particle) {
	particle.initial_position = vec3(0.0);
	particle.initial_color = vec4(1.0);
	particle.initial_scale = vec3(1.0);
	particle.initial_velocity = vec3(0.0);
	particle.initial_orbital_speed = 0.0;
	particle.spawn_direction = vec3(0.0);
	particle.animation_offset = 0.0;
	particle.animation_speed = 1.0;
	particle.rotation_axis = vec3(1.0, 0.0, 0.0);
	particle.initial_rotation = 0.0;
	particle.radial_velocity = 0.0;
	particle.orbital_axis = vec3(0.0, 1.0, 0.0);
	particle.orbital_velocity = vec3(0.0);
	m0_initialize_velocity(particle);
	m1_shape_location_ring(particle);
	particle.scale = particle.initial_scale;
	particle.color = particle.initial_color;
	particle.orbital_speed = particle.initial_orbital_speed;
}

void start() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = vec3(0.0);
	initialize(particle);
	if (rand(particle.seed) > AMOUNT_RATIO) {
		ACTIVE = false;
	}
	if (RESTART_CUSTOM) {
		CUSTOM = vec4(0.0);
		CUSTOM.x = particle.initial_rotation;
	}
	if (RESTART_COLOR) {
		COLOR = particle.initial_color;
	}
	if (RESTART_ROT_SCALE) {
		TRANSFORM[0].xyz = rotated(vec3(1.0, 0.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.x;
		TRANSFORM[1].xyz = rotated(vec3(0.0, 1.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.y;
		TRANSFORM[2].xyz = rotated(vec3(0.0, 0.0, 1.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.z;
	}
	if (RESTART_POSITION) {
		TRANSFORM[3].xyz = particle.initial_position;
		TRANSFORM = EMISSION_TRANSFORM * TRANSFORM;
	}
	if (RESTART_VELOCITY) {
		VELOCITY = (EMISSION_TRANSFORM * vec4(particle.initial_velocity, 0.0)).xyz;
		VELOCITY += EMITTER_VELOCITY * 0.0;//inherit_emitter_velocity_ratio;
	}
}

void process() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.delta = DELTA;
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = TRANSFORM[3].xyz;
	particle.velocity = VELOCITY;
	particle.orientation = mat3(
		normalize(TRANSFORM[0].xyz),
		normalize(TRANSFORM[1].xyz),
		normalize(TRANSFORM[2].xyz));
	particle.force = vec3(0.0);
	particle.emission_transform = EMISSION_TRANSFORM;
	particle.previous_transform = TRANSFORM;
	particle.rotation = CUSTOM.x;
	particle.just_spawned = CUSTOM.y == 0.0;
	particle.collided = COLLIDED;
	particle.collision_normal = COLLISION_NORMAL;
	particle.collision_depth = COLLISION_DEPTH;
	initialize(particle);
	CUSTOM.y += DELTA / particle.lifetime;
	CUSTOM.y = mix(CUSTOM.y, 1.0, INTERPOLATE_TO_END);
	CUSTOM.y = clamp(CUSTOM.y, 0.0, 1.0);
	particle.age = CUSTOM.y * particle.lifetime;
	particle.normalized_age = CUSTOM.y;
	m2_collision(particle);
	if (abs(particle.orbital_speed) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
		particle.orbital_velocity = (next - local_pos) / particle.delta;
	}
	if (abs(particle.radial_velocity) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 d = local_pos;
		float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
		vec3 next = local_pos + normalize_or_zero(d) * mag * sign(particle.radial_velocity);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
	}
	particle.position += particle.velocity * particle.delta;
	VELOCITY = particle.velocity;
	TRANSFORM[0].xyz = particle.orientation[0] * particle.scale.x;
	TRANSFORM[1].xyz = particle.orientation[1] * particle.scale.y;
	TRANSFORM[2].xyz = particle.orientation[2] * particle.scale.z;
	TRANSFORM[3].xyz = particle.position;
	COLOR = particle.color;
	CUSTOM.x = particle.rotation;
	
	if (particle.collided && CUSTOM.w == 0.0) {
		CUSTOM.w = 1.0;
		for (int i = 0; i < 1; i++) { emit_subparticle(TRANSFORM, vec3(0.0), vec4(0.0), vec4(0.0), FLAG_EMIT_POSITION); }
	}
	if (particle.collided) {
		ACTIVE = false;
	}
	if (particle.age >= LIFETIME) { ACTIVE = false;  }
}

"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_jkyy0"]
shader = SubResource("Shader_ty5rr")
shader_parameter/m1_u_radius = 8.0

[sub_resource type="Shader" id="Shader_4hdsn"]
code = "shader_type particles;
render_mode disable_velocity;

const float DEG_TO_RAD = PI / 180.0;
const uint AMOUNT = uint(24);

struct Particle {
	uint seed;
	float delta;
	float lifetime;
	uint index;
	uint number;
	float age;
	float normalized_age;
	float normalized_index;
	float normalized_number;
	vec3 initial_position;
	vec3 initial_scale;
	vec4 initial_color;
	vec3 initial_velocity;
	float radial_velocity;
	vec3 orbital_axis;
	float initial_orbital_speed;
	vec3 spawn_direction;
	vec3 position;
	vec3 scale;
	vec4 color;
	vec3 velocity;
	vec3 orbital_velocity;
	float orbital_speed;
	vec3 rotation_axis;
	float initial_rotation;
	float rotation;
	mat3 orientation;
	vec3 force;
	float animation_offset;
	float animation_speed;
	mat4 emission_transform;
	mat4 previous_transform;
	bool just_spawned;
	bool collided;
	vec3 collision_normal;
	float collision_depth;
};

float remap(float value, float input_min, float input_max, float output_min, float output_max) {
	float v = clamp(value, input_min, input_max);
	return (v - input_min) / (input_max - input_min) * (output_max - output_min) + output_min;
}

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}

uint hash(uint x) {
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = (x >> uint(16)) ^ x;
	return x;
}

vec3 normalize_or_zero(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 0.0, 0.0);
}

vec3 normalize_or_up(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 1.0, 0.0);
}

vec4 quat(vec3 axis, float angle) {
  float s = sin(angle / 2.0);
  return vec4(axis.x * s, axis.y * s, axis.z * s, cos(angle / 2.0));
}

vec4 quat_rotation_to(vec3 from, vec3 to) {
	vec3 xunit = vec3(1.0, 0.0, 0.0);
	vec3 yunit = vec3(0.0, 1.0, 0.0);
	float dot = dot(from, to);
	if (dot < -0.999999) {
	    vec3 axis = cross(xunit, from);
	    if (length(axis) < 0.000001) {
	    	axis = cross(yunit, from);
	    }
	    axis = normalize(axis);
	    return quat(axis, PI);
	} else if (dot > 0.999999) {
		return vec4(0.0, 0.0, 0.0, 1.0);
	} else {
		vec3 axis = cross(from, to);
		return normalize(vec4(axis, 1.0 + dot));
	}
}

vec3 rotated(vec3 v, vec3 axis, float angle) {
  vec4 q = quat(axis, angle);
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

vec3 rotated(vec3 v, vec4 q) {
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

mat3 rotated(mat3 m, vec4 q) {
  return mat3(rotated(m[0], q), rotated(m[1], q), rotated(m[2], q));
}

float normalized_by_amount(uint n) {
	return float(n) / float(max(AMOUNT, 1u));
}
void update_orbital_velocity(inout Particle particle, mat4 emission_transform, mat4 transform) {
	if (abs(particle.orbital_speed) < 0.01 || particle.delta < 0.001) { return; }
	vec3 local_pos = (inverse(emission_transform) * transform[3]).xyz;
	vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
	particle.position = (emission_transform * vec4(next, 0.0)).xyz;
	particle.orbital_velocity = (next - local_pos) / particle.delta;
}

void apply_radial_velocity(inout Particle particle) {
	vec3 d = particle.position;
	float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
	particle.position += normalize_or_zero(d) * mag * sign(particle.radial_velocity);
}

void m0_initialize_velocity(inout Particle particle) {
	float v_x = rand_range(particle.seed, 0.000000, 0.000000);
	float v_y = rand_range(particle.seed, -30.000000, -30.000000);
	float v_z = rand_range(particle.seed, 5.000000, 8.000000);
	vec3 v = vec3(v_x, v_y, v_z);
	particle.initial_velocity = v;
}
uniform float m1_u_radius = 20.0;

void m1_shape_location_ring(inout Particle particle) {
	float r = rand_range(particle.seed, 0.000000, 1.000000);
	float u = rand_range(particle.seed, 0.000000, 1.000000);
	float v = rand_range(particle.seed, 0.000000, 1.000000);
	vec3 h = vec3(1.000000, 0.000000, 0.000000);
	vec3 up = vec3(0.000000, 1.000000, 0.000000);
	h = rotated(h, up, u * 360.0 * DEG_TO_RAD) * vec3(1.000000, 1.000000, 1.000000);
	float radius_edge = tan(0.000000 * DEG_TO_RAD) * 0.010000;
	vec3 offset = vec3(0.000000, 0.000000, 0.000000);
	particle.spawn_direction = normalize(h * r * (m1_u_radius + radius_edge) + 0.010000 * up) + offset;
	particle.initial_position = h * r * (m1_u_radius + radius_edge * v) + v * 0.010000 * up + offset;
}

void m2_collision(inout Particle particle) {
	if (particle.collided) {
		float collision_response = dot(particle.collision_normal, particle.velocity);
		float slide_to_bounce_trigger = step(2.0 / clamp(0.000000 + 1.0, 1.0, 2.0), abs(collision_response));
		particle.position += particle.collision_normal * particle.collision_depth;
		// Remove all components of velocity that are not tangential to COLLISION_NORMAL.
		particle.velocity -= particle.collision_normal * collision_response;
		// Apply friction only to velocity across the surface (effectively decouples friction and bounce behavior).
		particle.velocity = mix(particle.velocity, vec3(0.0), clamp(1.000000, 0.0, 1.0));
		// Add bounce velocity to velocity.
		particle.velocity -= particle.collision_normal * collision_response * (0.000000 * slide_to_bounce_trigger);
	}
}


void initialize(inout Particle particle) {
	particle.initial_position = vec3(0.0);
	particle.initial_color = vec4(1.0);
	particle.initial_scale = vec3(1.0);
	particle.initial_velocity = vec3(0.0);
	particle.initial_orbital_speed = 0.0;
	particle.spawn_direction = vec3(0.0);
	particle.animation_offset = 0.0;
	particle.animation_speed = 1.0;
	particle.rotation_axis = vec3(1.0, 0.0, 0.0);
	particle.initial_rotation = 0.0;
	particle.radial_velocity = 0.0;
	particle.orbital_axis = vec3(0.0, 1.0, 0.0);
	particle.orbital_velocity = vec3(0.0);
	m0_initialize_velocity(particle);
	m1_shape_location_ring(particle);
	particle.scale = particle.initial_scale;
	particle.color = particle.initial_color;
	particle.orbital_speed = particle.initial_orbital_speed;
}

void start() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = vec3(0.0);
	initialize(particle);
	if (rand(particle.seed) > AMOUNT_RATIO) {
		ACTIVE = false;
	}
	if (RESTART_CUSTOM) {
		CUSTOM = vec4(0.0);
		CUSTOM.x = particle.initial_rotation;
	}
	if (RESTART_COLOR) {
		COLOR = particle.initial_color;
	}
	if (RESTART_ROT_SCALE) {
		TRANSFORM[0].xyz = rotated(vec3(1.0, 0.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.x;
		TRANSFORM[1].xyz = rotated(vec3(0.0, 1.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.y;
		TRANSFORM[2].xyz = rotated(vec3(0.0, 0.0, 1.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.z;
	}
	if (RESTART_POSITION) {
		TRANSFORM[3].xyz = particle.initial_position;
		TRANSFORM = EMISSION_TRANSFORM * TRANSFORM;
	}
	if (RESTART_VELOCITY) {
		VELOCITY = (EMISSION_TRANSFORM * vec4(particle.initial_velocity, 0.0)).xyz;
		VELOCITY += EMITTER_VELOCITY * 0.0;//inherit_emitter_velocity_ratio;
	}
}

void process() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.delta = DELTA;
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = TRANSFORM[3].xyz;
	particle.velocity = VELOCITY;
	particle.orientation = mat3(
		normalize(TRANSFORM[0].xyz),
		normalize(TRANSFORM[1].xyz),
		normalize(TRANSFORM[2].xyz));
	particle.force = vec3(0.0);
	particle.emission_transform = EMISSION_TRANSFORM;
	particle.previous_transform = TRANSFORM;
	particle.rotation = CUSTOM.x;
	particle.just_spawned = CUSTOM.y == 0.0;
	particle.collided = COLLIDED;
	particle.collision_normal = COLLISION_NORMAL;
	particle.collision_depth = COLLISION_DEPTH;
	initialize(particle);
	CUSTOM.y += DELTA / particle.lifetime;
	CUSTOM.y = mix(CUSTOM.y, 1.0, INTERPOLATE_TO_END);
	CUSTOM.y = clamp(CUSTOM.y, 0.0, 1.0);
	particle.age = CUSTOM.y * particle.lifetime;
	particle.normalized_age = CUSTOM.y;
	m2_collision(particle);
	if (abs(particle.orbital_speed) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
		particle.orbital_velocity = (next - local_pos) / particle.delta;
	}
	if (abs(particle.radial_velocity) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 d = local_pos;
		float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
		vec3 next = local_pos + normalize_or_zero(d) * mag * sign(particle.radial_velocity);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
	}
	particle.position += particle.velocity * particle.delta;
	VELOCITY = particle.velocity;
	TRANSFORM[0].xyz = particle.orientation[0] * particle.scale.x;
	TRANSFORM[1].xyz = particle.orientation[1] * particle.scale.y;
	TRANSFORM[2].xyz = particle.orientation[2] * particle.scale.z;
	TRANSFORM[3].xyz = particle.position;
	COLOR = particle.color;
	CUSTOM.x = particle.rotation;
	
	if (particle.collided && CUSTOM.w == 0.0) {
		CUSTOM.w = 1.0;
		for (int i = 0; i < 4; i++) { emit_subparticle(TRANSFORM, vec3(0.0), vec4(0.0), vec4(0.0), FLAG_EMIT_POSITION); }
	}
	if (particle.collided) {
		ACTIVE = false;
	}
	if (particle.age >= LIFETIME) { ACTIVE = false;  }
}

"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_8ogl4"]
shader = SubResource("Shader_4hdsn")
shader_parameter/m1_u_radius = 8.0

[sub_resource type="Shader" id="Shader_fi0ju"]
code = "shader_type particles;
render_mode disable_velocity;

const float DEG_TO_RAD = PI / 180.0;
const uint AMOUNT = uint(144);

struct Particle {
	uint seed;
	float delta;
	float lifetime;
	uint index;
	uint number;
	float age;
	float normalized_age;
	float normalized_index;
	float normalized_number;
	vec3 initial_position;
	vec3 initial_scale;
	vec4 initial_color;
	vec3 initial_velocity;
	float radial_velocity;
	vec3 orbital_axis;
	float initial_orbital_speed;
	vec3 spawn_direction;
	vec3 position;
	vec3 scale;
	vec4 color;
	vec3 velocity;
	vec3 orbital_velocity;
	float orbital_speed;
	vec3 rotation_axis;
	float initial_rotation;
	float rotation;
	mat3 orientation;
	vec3 force;
	float animation_offset;
	float animation_speed;
	mat4 emission_transform;
	mat4 previous_transform;
	bool just_spawned;
	bool collided;
	vec3 collision_normal;
	float collision_depth;
};

float remap(float value, float input_min, float input_max, float output_min, float output_max) {
	float v = clamp(value, input_min, input_max);
	return (v - input_min) / (input_max - input_min) * (output_max - output_min) + output_min;
}

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}

uint hash(uint x) {
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = (x >> uint(16)) ^ x;
	return x;
}

vec3 normalize_or_zero(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 0.0, 0.0);
}

vec3 normalize_or_up(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 1.0, 0.0);
}

vec4 quat(vec3 axis, float angle) {
  float s = sin(angle / 2.0);
  return vec4(axis.x * s, axis.y * s, axis.z * s, cos(angle / 2.0));
}

vec4 quat_rotation_to(vec3 from, vec3 to) {
	vec3 xunit = vec3(1.0, 0.0, 0.0);
	vec3 yunit = vec3(0.0, 1.0, 0.0);
	float dot = dot(from, to);
	if (dot < -0.999999) {
	    vec3 axis = cross(xunit, from);
	    if (length(axis) < 0.000001) {
	    	axis = cross(yunit, from);
	    }
	    axis = normalize(axis);
	    return quat(axis, PI);
	} else if (dot > 0.999999) {
		return vec4(0.0, 0.0, 0.0, 1.0);
	} else {
		vec3 axis = cross(from, to);
		return normalize(vec4(axis, 1.0 + dot));
	}
}

vec3 rotated(vec3 v, vec3 axis, float angle) {
  vec4 q = quat(axis, angle);
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

vec3 rotated(vec3 v, vec4 q) {
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

mat3 rotated(mat3 m, vec4 q) {
  return mat3(rotated(m[0], q), rotated(m[1], q), rotated(m[2], q));
}

float normalized_by_amount(uint n) {
	return float(n) / float(max(AMOUNT, 1u));
}
void update_orbital_velocity(inout Particle particle, mat4 emission_transform, mat4 transform) {
	if (abs(particle.orbital_speed) < 0.01 || particle.delta < 0.001) { return; }
	vec3 local_pos = (inverse(emission_transform) * transform[3]).xyz;
	vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
	particle.position = (emission_transform * vec4(next, 0.0)).xyz;
	particle.orbital_velocity = (next - local_pos) / particle.delta;
}

void apply_radial_velocity(inout Particle particle) {
	vec3 d = particle.position;
	float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
	particle.position += normalize_or_zero(d) * mag * sign(particle.radial_velocity);
}

void m0_shape_location_sphere(inout Particle particle) {
	float r = 1.0;
	float u = rand_range(particle.seed, 0.000000, 1.000000);
	float v = rand_range(particle.seed, 0.000000, 0.500000);
	vec3 n = vec3(1.0, 0.0, 0.0);
	n = rotated(n, vec3(0.0, 1.0, 0.0), u * 360.0 * DEG_TO_RAD);
	float h = 1.0 - 2.0 * v;
	n = normalize(n * (1.0 - abs(h)) + vec3(0.0, h, 0.0));
	particle.initial_position = n * r * 0.010000 + vec3(0.000000, 0.000000, 0.000000);
}

void m1_initialize_velocity_from_point(inout Particle particle) {
	float v = rand_range(particle.seed, 30.000000, 45.000000);
	vec3 front = vec3(0.0, 0.0, 1.0);
	vec3 to = -normalize(particle.initial_position);
	vec4 q = quat_rotation_to(front, to);
	vec3 right = rotated(vec3(1.0, 0.0, 0.0), q);
	particle.initial_velocity = normalize(particle.initial_position) * v;
}

void m2_initialize_size(inout Particle particle) {
	float s_c = rand_range(particle.seed, 0.020000, 0.050000);
	vec3 s = vec3(s_c, s_c, s_c);
	particle.initial_scale = s;
}

void m3_randomize_lifetime(inout Particle particle) {
	particle.lifetime = particle.lifetime * (1.0 - rand(particle.seed) * 0.500000);
}

uniform vec4 u_m4_rgb : source_color;

void m4_initialize_color(inout Particle particle) {
	vec4 c = u_m4_rgb;
	float a = 1.0;
	particle.initial_color = vec4(c.rgb, a);
}

void m5_drag(inout Particle particle) {
	float limit = 0.0;
	float drag = 12.0;
	float speed = length(particle.velocity);
	float excess = max(0.0, speed - limit);
	vec3 dir = normalize_or_up(particle.velocity);
	particle.velocity = dir * (speed - particle.delta * excess * max(drag, 0.0));
}

void m6_align(inout Particle particle) {
	vec3 up = vec3(0.0, 1.0, 0.0);
	vec3 v = normalize(particle.velocity + particle.orbital_velocity + normalize(particle.position) * particle.radial_velocity);
	vec4 q = quat_rotation_to(up, v);
	mat3 m = mat3(1.0);
	particle.orientation = rotated(m, q);
}

uniform sampler2D u_m7_scale : repeat_disable;

void m7_scale_size(inout Particle particle) {
	vec3 s = vec3(texture(u_m7_scale, vec2(particle.normalized_age)).r);
	particle.scale *= s;
}

uniform sampler2D u_m8_scale_x : repeat_disable;

uniform sampler2D u_m8_scale_y : repeat_disable;

uniform sampler2D u_m8_scale_z : repeat_disable;

void m8_scale_size_by_velocity(inout Particle particle) {
	float t = clamp(length(particle.velocity) / max(0.001, length(particle.initial_velocity)), 0.0, 1.0);
	vec3 s = vec3(texture(u_m8_scale_x, vec2(t)).r, texture(u_m8_scale_y, vec2(t)).r, texture(u_m8_scale_z, vec2(t)).r);
	particle.scale = particle.scale * s;
}


void initialize(inout Particle particle) {
	particle.initial_position = vec3(0.0);
	particle.initial_color = vec4(1.0);
	particle.initial_scale = vec3(1.0);
	particle.initial_velocity = vec3(0.0);
	particle.initial_orbital_speed = 0.0;
	particle.spawn_direction = vec3(0.0);
	particle.animation_offset = 0.0;
	particle.animation_speed = 1.0;
	particle.rotation_axis = vec3(1.0, 0.0, 0.0);
	particle.initial_rotation = 0.0;
	particle.radial_velocity = 0.0;
	particle.orbital_axis = vec3(0.0, 1.0, 0.0);
	particle.orbital_velocity = vec3(0.0);
	m0_shape_location_sphere(particle);
	m1_initialize_velocity_from_point(particle);
	m2_initialize_size(particle);
	m3_randomize_lifetime(particle);
	m4_initialize_color(particle);
	particle.scale = particle.initial_scale;
	particle.color = particle.initial_color;
	particle.orbital_speed = particle.initial_orbital_speed;
}

void start() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = vec3(0.0);
	initialize(particle);
	if (rand(particle.seed) > AMOUNT_RATIO) {
		ACTIVE = false;
	}
	if (RESTART_CUSTOM) {
		CUSTOM = vec4(0.0);
		CUSTOM.x = particle.initial_rotation;
	}
	if (RESTART_COLOR) {
		COLOR = particle.initial_color;
	}
	if (RESTART_ROT_SCALE) {
		TRANSFORM[0].xyz = rotated(vec3(1.0, 0.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.x;
		TRANSFORM[1].xyz = rotated(vec3(0.0, 1.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.y;
		TRANSFORM[2].xyz = rotated(vec3(0.0, 0.0, 1.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.z;
	}
	if (RESTART_POSITION) {
		TRANSFORM[3].xyz = particle.initial_position;
		TRANSFORM = EMISSION_TRANSFORM * TRANSFORM;
	}
	if (RESTART_VELOCITY) {
		VELOCITY = (EMISSION_TRANSFORM * vec4(particle.initial_velocity, 0.0)).xyz;
		VELOCITY += EMITTER_VELOCITY * 0.0;//inherit_emitter_velocity_ratio;
	}
}

void process() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.delta = DELTA;
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = TRANSFORM[3].xyz;
	particle.velocity = VELOCITY;
	particle.orientation = mat3(
		normalize(TRANSFORM[0].xyz),
		normalize(TRANSFORM[1].xyz),
		normalize(TRANSFORM[2].xyz));
	particle.force = vec3(0.0);
	particle.emission_transform = EMISSION_TRANSFORM;
	particle.previous_transform = TRANSFORM;
	particle.rotation = CUSTOM.x;
	particle.just_spawned = CUSTOM.y == 0.0;
	particle.collided = COLLIDED;
	particle.collision_normal = COLLISION_NORMAL;
	particle.collision_depth = COLLISION_DEPTH;
	initialize(particle);
	CUSTOM.y += DELTA / particle.lifetime;
	CUSTOM.y = mix(CUSTOM.y, 1.0, INTERPOLATE_TO_END);
	CUSTOM.y = clamp(CUSTOM.y, 0.0, 1.0);
	particle.age = CUSTOM.y * particle.lifetime;
	particle.normalized_age = CUSTOM.y;
	m5_drag(particle);
	m6_align(particle);
	m7_scale_size(particle);
	m8_scale_size_by_velocity(particle);
	if (abs(particle.orbital_speed) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
		particle.orbital_velocity = (next - local_pos) / particle.delta;
	}
	if (abs(particle.radial_velocity) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 d = local_pos;
		float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
		vec3 next = local_pos + normalize_or_zero(d) * mag * sign(particle.radial_velocity);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
	}
	particle.position += particle.velocity * particle.delta;
	VELOCITY = particle.velocity;
	TRANSFORM[0].xyz = particle.orientation[0] * particle.scale.x;
	TRANSFORM[1].xyz = particle.orientation[1] * particle.scale.y;
	TRANSFORM[2].xyz = particle.orientation[2] * particle.scale.z;
	TRANSFORM[3].xyz = particle.position;
	COLOR = particle.color;
	CUSTOM.x = particle.rotation;
	
   if (particle.age >= particle.lifetime) { ACTIVE = false;  }
}

"

[sub_resource type="Curve" id="Curve_r4kp1"]
_data = [Vector2(0.19932431, 1), 0.0, 0.0, 0, 0, Vector2(1, 0), -3.3191774, 0.0, 0, 0]
point_count = 2

[sub_resource type="CurveTexture" id="CurveTexture_ie6l3"]
curve = SubResource("Curve_r4kp1")
metadata/uniform_name = "u_m7_scale"

[sub_resource type="Curve" id="Curve_0wjud"]
_data = [Vector2(0, 1), 0.0, 0.0, 0, 0]
point_count = 1

[sub_resource type="CurveTexture" id="CurveTexture_x1np5"]
curve = SubResource("Curve_0wjud")
metadata/uniform_name = "u_m8_scale_x"

[sub_resource type="Curve" id="Curve_g8c3b"]
_limits = [0.0, 20.0, 0.0, 1.0]
_data = [Vector2(0, 2.0426416), 0.0, 17.95736, 0, 1, Vector2(1, 20), 17.95736, 0.0, 1, 0]
point_count = 2

[sub_resource type="CurveTexture" id="CurveTexture_bb4a2"]
curve = SubResource("Curve_g8c3b")
metadata/uniform_name = "u_m8_scale_y"

[sub_resource type="CurveTexture" id="CurveTexture_ama2p"]
curve = SubResource("Curve_0wjud")
metadata/uniform_name = "u_m8_scale_z"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_2tpp2"]
shader = SubResource("Shader_fi0ju")
shader_parameter/u_m4_rgb = Color(0.94, 0.74855334, 0.49819997, 1)
shader_parameter/u_m7_scale = SubResource("CurveTexture_ie6l3")
shader_parameter/u_m8_scale_x = SubResource("CurveTexture_x1np5")
shader_parameter/u_m8_scale_y = SubResource("CurveTexture_bb4a2")
shader_parameter/u_m8_scale_z = SubResource("CurveTexture_ama2p")

[sub_resource type="Shader" id="Shader_byb4e"]
code = "shader_type spatial;
render_mode unshaded, cull_disabled;

uniform float opacity = 1.0;
uniform float emission = 1.0;

uniform sampler2D tex_1 : source_color, filter_linear_mipmap, repeat_disable;
uniform vec2 tiling_1 = vec2(1.0);
uniform vec2 offset_1 = vec2(0.0);
uniform vec2 speed_1 = vec2(0.0);

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}


varying flat uint instance_id;


void vertex()
{
	instance_id = uint(INSTANCE_ID);
	vec3 up = normalize(MODEL_MATRIX[1].xyz);
	vec3 right = normalize(cross(up, MAIN_CAM_INV_VIEW_MATRIX[2].xyz));
	vec3 front = normalize(cross(right, up));
	MODELVIEW_MATRIX = VIEW_MATRIX * mat4(
		vec4(right, 0.0),
		vec4(up, 0.0),
		vec4(front, 0.0),
		MODEL_MATRIX[3]);
	MODELVIEW_MATRIX = MODELVIEW_MATRIX * mat4(
			vec4(length(MODEL_MATRIX[0].xyz), 0.0, 0.0, 0.0),
			vec4(0.0, length(MODEL_MATRIX[1].xyz), 0.0, 0.0),
			vec4(0.0, 0.0, length(MODEL_MATRIX[2].xyz), 0.0),
			vec4(0.0, 0.0, 0.0, 1.0));
	MODELVIEW_NORMAL_MATRIX = mat3(MODELVIEW_MATRIX);
}

void fragment()
{
	uint seed = instance_id;
	vec4 tex_sampled_1 = texture(tex_1, UV + speed_1 * tiling_1 * TIME);
	ALPHA = COLOR.a;
	ALPHA = ALPHA * tex_sampled_1.r;
	ALPHA = clamp(ALPHA * opacity, 0.0, 1.0);
	ALBEDO = COLOR.rgb * emission;
	ALBEDO *= tex_sampled_1.rgb;
	
}


"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_beqxg"]
render_priority = 0
shader = SubResource("Shader_byb4e")
shader_parameter/opacity = 1.0
shader_parameter/emission = 10.0
shader_parameter/tex_1 = ExtResource("23_3dayl")
shader_parameter/tiling_1 = Vector2(1, 1)
shader_parameter/offset_1 = Vector2(0, 0)
shader_parameter/speed_1 = Vector2(0, 0)

[sub_resource type="QuadMesh" id="QuadMesh_ai3hr"]
material = SubResource("ShaderMaterial_beqxg")
center_offset = Vector3(0, 0.5, 0)

[sub_resource type="Shader" id="Shader_hbvjr"]
code = "shader_type particles;
render_mode disable_velocity;

const float DEG_TO_RAD = PI / 180.0;
const uint AMOUNT = uint(256);

struct Particle {
	uint seed;
	float delta;
	float lifetime;
	uint index;
	uint number;
	float age;
	float normalized_age;
	float normalized_index;
	float normalized_number;
	vec3 initial_position;
	vec3 initial_scale;
	vec4 initial_color;
	vec3 initial_velocity;
	float radial_velocity;
	vec3 orbital_axis;
	float initial_orbital_speed;
	vec3 spawn_direction;
	vec3 position;
	vec3 scale;
	vec4 color;
	vec3 velocity;
	vec3 orbital_velocity;
	float orbital_speed;
	vec3 rotation_axis;
	float initial_rotation;
	float rotation;
	mat3 orientation;
	vec3 force;
	float animation_offset;
	float animation_speed;
	mat4 emission_transform;
	mat4 previous_transform;
	bool just_spawned;
	bool collided;
	vec3 collision_normal;
	float collision_depth;
};

float remap(float value, float input_min, float input_max, float output_min, float output_max) {
	float v = clamp(value, input_min, input_max);
	return (v - input_min) / (input_max - input_min) * (output_max - output_min) + output_min;
}

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}

uint hash(uint x) {
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = (x >> uint(16)) ^ x;
	return x;
}

vec3 normalize_or_zero(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 0.0, 0.0);
}

vec3 normalize_or_up(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 1.0, 0.0);
}

vec4 quat(vec3 axis, float angle) {
  float s = sin(angle / 2.0);
  return vec4(axis.x * s, axis.y * s, axis.z * s, cos(angle / 2.0));
}

vec4 quat_rotation_to(vec3 from, vec3 to) {
	vec3 xunit = vec3(1.0, 0.0, 0.0);
	vec3 yunit = vec3(0.0, 1.0, 0.0);
	float dot = dot(from, to);
	if (dot < -0.999999) {
	    vec3 axis = cross(xunit, from);
	    if (length(axis) < 0.000001) {
	    	axis = cross(yunit, from);
	    }
	    axis = normalize(axis);
	    return quat(axis, PI);
	} else if (dot > 0.999999) {
		return vec4(0.0, 0.0, 0.0, 1.0);
	} else {
		vec3 axis = cross(from, to);
		return normalize(vec4(axis, 1.0 + dot));
	}
}

vec3 rotated(vec3 v, vec3 axis, float angle) {
  vec4 q = quat(axis, angle);
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

vec3 rotated(vec3 v, vec4 q) {
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

mat3 rotated(mat3 m, vec4 q) {
  return mat3(rotated(m[0], q), rotated(m[1], q), rotated(m[2], q));
}

float normalized_by_amount(uint n) {
	return float(n) / float(max(AMOUNT, 1u));
}
void update_orbital_velocity(inout Particle particle, mat4 emission_transform, mat4 transform) {
	if (abs(particle.orbital_speed) < 0.01 || particle.delta < 0.001) { return; }
	vec3 local_pos = (inverse(emission_transform) * transform[3]).xyz;
	vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
	particle.position = (emission_transform * vec4(next, 0.0)).xyz;
	particle.orbital_velocity = (next - local_pos) / particle.delta;
}

void apply_radial_velocity(inout Particle particle) {
	vec3 d = particle.position;
	float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
	particle.position += normalize_or_zero(d) * mag * sign(particle.radial_velocity);
}

void m1_initialize_size(inout Particle particle) {
	float s_c = rand_range(particle.seed, 0.050000, 0.100000);
	vec3 s = vec3(s_c, s_c, s_c);
	particle.initial_scale = s;
}
uniform vec4 u_m2_rgb : source_color;

void m2_initialize_color(inout Particle particle) {
	vec4 c = u_m2_rgb;
	float a = 1.0;
	particle.initial_color = vec4(c.rgb, a);
}

void m3_set_sprite_animation(inout Particle particle) {
	float offset = rand_range(particle.seed, 0.000000, 3.000000);
	particle.animation_offset = offset;
	particle.animation_speed = 0.000000;
}

void m4_align(inout Particle particle) {
	vec3 up = vec3(0.0, 1.0, 0.0);
	vec3 v = normalize(particle.velocity + particle.orbital_velocity + normalize(particle.position) * particle.radial_velocity);
	vec4 q = quat_rotation_to(up, v);
	mat3 m = mat3(1.0);
	particle.orientation = rotated(m, q);
}

uniform sampler2D u_m5_scale : repeat_disable;

void m5_scale_size(inout Particle particle) {
	vec3 s = vec3(texture(u_m5_scale, vec2(particle.normalized_age)).r);
	particle.scale *= s;
}


void initialize(inout Particle particle) {
	particle.initial_position = vec3(0.0);
	particle.initial_color = vec4(1.0);
	particle.initial_scale = vec3(1.0);
	particle.initial_velocity = vec3(0.0);
	particle.initial_orbital_speed = 0.0;
	particle.spawn_direction = vec3(0.0);
	particle.animation_offset = 0.0;
	particle.animation_speed = 1.0;
	particle.rotation_axis = vec3(1.0, 0.0, 0.0);
	particle.initial_rotation = 0.0;
	particle.radial_velocity = 0.0;
	particle.orbital_axis = vec3(0.0, 1.0, 0.0);
	particle.orbital_velocity = vec3(0.0);
	m1_initialize_size(particle);
	m2_initialize_color(particle);
	particle.scale = particle.initial_scale;
	particle.color = particle.initial_color;
	particle.orbital_speed = particle.initial_orbital_speed;
}

void start() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = vec3(0.0);
	initialize(particle);
	if (rand(particle.seed) > AMOUNT_RATIO) {
		ACTIVE = false;
	}
	if (RESTART_CUSTOM) {
		CUSTOM = vec4(0.0);
		CUSTOM.x = particle.initial_rotation;
	}
	if (RESTART_COLOR) {
		COLOR = particle.initial_color;
	}
	if (RESTART_ROT_SCALE) {
		TRANSFORM[0].xyz = rotated(vec3(1.0, 0.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.x;
		TRANSFORM[1].xyz = rotated(vec3(0.0, 1.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.y;
		TRANSFORM[2].xyz = rotated(vec3(0.0, 0.0, 1.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.z;
	}
	if (RESTART_POSITION) {
		TRANSFORM[3].xyz = particle.initial_position;
		TRANSFORM = EMISSION_TRANSFORM * TRANSFORM;
	}
	if (RESTART_VELOCITY) {
		VELOCITY = (EMISSION_TRANSFORM * vec4(particle.initial_velocity, 0.0)).xyz;
		VELOCITY += EMITTER_VELOCITY * 0.0;//inherit_emitter_velocity_ratio;
	}
   {
		float v = rand_range(particle.seed, 0.000000, 0.100000);
   	VELOCITY = v * VELOCITY + (EMISSION_TRANSFORM * vec4(particle.initial_velocity, 0.0)).xyz;
   }
}

void process() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.delta = DELTA;
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = TRANSFORM[3].xyz;
	particle.velocity = VELOCITY;
	particle.orientation = mat3(
		normalize(TRANSFORM[0].xyz),
		normalize(TRANSFORM[1].xyz),
		normalize(TRANSFORM[2].xyz));
	particle.force = vec3(0.0);
	particle.emission_transform = EMISSION_TRANSFORM;
	particle.previous_transform = TRANSFORM;
	particle.rotation = CUSTOM.x;
	particle.just_spawned = CUSTOM.y == 0.0;
	particle.collided = COLLIDED;
	particle.collision_normal = COLLISION_NORMAL;
	particle.collision_depth = COLLISION_DEPTH;
	initialize(particle);
	CUSTOM.y += DELTA / particle.lifetime;
	CUSTOM.y = mix(CUSTOM.y, 1.0, INTERPOLATE_TO_END);
	CUSTOM.y = clamp(CUSTOM.y, 0.0, 1.0);
	particle.age = CUSTOM.y * particle.lifetime;
	particle.normalized_age = CUSTOM.y;
	m3_set_sprite_animation(particle);
	m4_align(particle);
	m5_scale_size(particle);
	if (abs(particle.orbital_speed) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
		particle.orbital_velocity = (next - local_pos) / particle.delta;
	}
	if (abs(particle.radial_velocity) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 d = local_pos;
		float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
		vec3 next = local_pos + normalize_or_zero(d) * mag * sign(particle.radial_velocity);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
	}
	particle.position += particle.velocity * particle.delta;
	VELOCITY = particle.velocity;
	TRANSFORM[0].xyz = particle.orientation[0] * particle.scale.x;
	TRANSFORM[1].xyz = particle.orientation[1] * particle.scale.y;
	TRANSFORM[2].xyz = particle.orientation[2] * particle.scale.z;
	TRANSFORM[3].xyz = particle.position;
	COLOR = particle.color;
	CUSTOM.x = particle.rotation;
	
	CUSTOM.z = particle.animation_offset + particle.normalized_age * particle.animation_speed;
	if (particle.age >= LIFETIME) { ACTIVE = false;  }
}

"

[sub_resource type="Curve" id="Curve_iu1sl"]
_data = [Vector2(0, 1), 0.0, 0.0, 0, 0, Vector2(1, 0), -1.9339753, 0.0, 0, 0]
point_count = 2

[sub_resource type="CurveTexture" id="CurveTexture_fxtl3"]
curve = SubResource("Curve_iu1sl")
metadata/uniform_name = "u_m5_scale"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_n8jf6"]
shader = SubResource("Shader_hbvjr")
shader_parameter/u_m2_rgb = Color(0.94, 0.74855334, 0.49819997, 1)
shader_parameter/u_m5_scale = SubResource("CurveTexture_fxtl3")

[sub_resource type="Shader" id="Shader_q516c"]
code = "shader_type spatial;
render_mode unshaded, cull_disabled, blend_add;

uniform float opacity = 1.0;
uniform float emission = 1.0;

uniform sampler2D tex_1 : source_color, filter_linear_mipmap, repeat_disable;
uniform vec2 tiling_1 = vec2(1.0);
uniform vec2 offset_1 = vec2(0.0);
uniform vec2 speed_1 = vec2(0.0);

uniform uint num_horizontal;
uniform uint num_vertical;

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}


varying flat uint instance_id;
varying float age;

void vertex()
{
	instance_id = uint(INSTANCE_ID);
	age = INSTANCE_CUSTOM.y;
	vec3 up = normalize(MODEL_MATRIX[1].xyz);
	vec3 right = normalize(cross(up, MAIN_CAM_INV_VIEW_MATRIX[2].xyz));
	vec3 front = normalize(cross(right, up));
	MODELVIEW_MATRIX = VIEW_MATRIX * mat4(
		vec4(right, 0.0),
		vec4(up, 0.0),
		vec4(front, 0.0),
		MODEL_MATRIX[3]);
	MODELVIEW_MATRIX = MODELVIEW_MATRIX * mat4(
			vec4(length(MODEL_MATRIX[0].xyz), 0.0, 0.0, 0.0),
			vec4(0.0, length(MODEL_MATRIX[1].xyz), 0.0, 0.0),
			vec4(0.0, 0.0, length(MODEL_MATRIX[2].xyz), 0.0),
			vec4(0.0, 0.0, 0.0, 1.0));
	MODELVIEW_NORMAL_MATRIX = mat3(MODELVIEW_MATRIX);
	uint h = max(num_horizontal, 1u);
	uint v = max(num_vertical, 1u);
	uint num = h * v;
	uint idx = min(uint(float(num) * INSTANCE_CUSTOM.z) % num, num - 1u);
	uint iy = idx / h;
	uint ix = idx - iy * h;
	float w = 1.0 / float(h);
	float height = 1.0 / float(v);
	UV.x = float(ix) * w + UV.x * w;
	UV.y = float(iy) * height + UV.y * height;
}

void fragment()
{
	uint seed = instance_id;
	vec4 tex_sampled_1 = texture(tex_1, UV + speed_1 * tiling_1 * TIME);
	ALPHA = COLOR.a;
	ALPHA = ALPHA * tex_sampled_1.r;
	ALPHA = clamp(ALPHA * opacity, 0.0, 1.0);
	ALBEDO = COLOR.rgb * emission;
	
}


"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_unylw"]
render_priority = 0
shader = SubResource("Shader_q516c")
shader_parameter/opacity = 1.0
shader_parameter/emission = 10.0
shader_parameter/tex_1 = ExtResource("26_fxwkx")
shader_parameter/tiling_1 = Vector2(1, 1)
shader_parameter/offset_1 = Vector2(0, 0)
shader_parameter/speed_1 = Vector2(0, 0)
shader_parameter/num_horizontal = 2
shader_parameter/num_vertical = 2

[sub_resource type="QuadMesh" id="QuadMesh_bdfux"]
material = SubResource("ShaderMaterial_unylw")

[sub_resource type="Shader" id="Shader_fl3cn"]
code = "shader_type particles;
render_mode disable_velocity;

const float DEG_TO_RAD = PI / 180.0;
const uint AMOUNT = uint(96);

struct Particle {
	uint seed;
	float delta;
	float lifetime;
	uint index;
	uint number;
	float age;
	float normalized_age;
	float normalized_index;
	float normalized_number;
	vec3 initial_position;
	vec3 initial_scale;
	vec4 initial_color;
	vec3 initial_velocity;
	float radial_velocity;
	vec3 orbital_axis;
	float initial_orbital_speed;
	vec3 spawn_direction;
	vec3 position;
	vec3 scale;
	vec4 color;
	vec3 velocity;
	vec3 orbital_velocity;
	float orbital_speed;
	vec3 rotation_axis;
	float initial_rotation;
	float rotation;
	mat3 orientation;
	vec3 force;
	float animation_offset;
	float animation_speed;
	mat4 emission_transform;
	mat4 previous_transform;
	bool just_spawned;
	bool collided;
	vec3 collision_normal;
	float collision_depth;
};

float remap(float value, float input_min, float input_max, float output_min, float output_max) {
	float v = clamp(value, input_min, input_max);
	return (v - input_min) / (input_max - input_min) * (output_max - output_min) + output_min;
}

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}

uint hash(uint x) {
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = (x >> uint(16)) ^ x;
	return x;
}

vec3 normalize_or_zero(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 0.0, 0.0);
}

vec3 normalize_or_up(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 1.0, 0.0);
}

vec4 quat(vec3 axis, float angle) {
  float s = sin(angle / 2.0);
  return vec4(axis.x * s, axis.y * s, axis.z * s, cos(angle / 2.0));
}

vec4 quat_rotation_to(vec3 from, vec3 to) {
	vec3 xunit = vec3(1.0, 0.0, 0.0);
	vec3 yunit = vec3(0.0, 1.0, 0.0);
	float dot = dot(from, to);
	if (dot < -0.999999) {
	    vec3 axis = cross(xunit, from);
	    if (length(axis) < 0.000001) {
	    	axis = cross(yunit, from);
	    }
	    axis = normalize(axis);
	    return quat(axis, PI);
	} else if (dot > 0.999999) {
		return vec4(0.0, 0.0, 0.0, 1.0);
	} else {
		vec3 axis = cross(from, to);
		return normalize(vec4(axis, 1.0 + dot));
	}
}

vec3 rotated(vec3 v, vec3 axis, float angle) {
  vec4 q = quat(axis, angle);
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

vec3 rotated(vec3 v, vec4 q) {
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

mat3 rotated(mat3 m, vec4 q) {
  return mat3(rotated(m[0], q), rotated(m[1], q), rotated(m[2], q));
}

float normalized_by_amount(uint n) {
	return float(n) / float(max(AMOUNT, 1u));
}
void update_orbital_velocity(inout Particle particle, mat4 emission_transform, mat4 transform) {
	if (abs(particle.orbital_speed) < 0.01 || particle.delta < 0.001) { return; }
	vec3 local_pos = (inverse(emission_transform) * transform[3]).xyz;
	vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
	particle.position = (emission_transform * vec4(next, 0.0)).xyz;
	particle.orbital_velocity = (next - local_pos) / particle.delta;
}

void apply_radial_velocity(inout Particle particle) {
	vec3 d = particle.position;
	float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
	particle.position += normalize_or_zero(d) * mag * sign(particle.radial_velocity);
}

void m0_shape_location_sphere(inout Particle particle) {
	float r = 1.0;
	float u = rand_range(particle.seed, 0.000000, 1.000000);
	float v = rand_range(particle.seed, 0.000000, 0.500000);
	vec3 n = vec3(1.0, 0.0, 0.0);
	n = rotated(n, vec3(0.0, 1.0, 0.0), u * 360.0 * DEG_TO_RAD);
	float h = 1.0 - 2.0 * v;
	n = normalize(n * (1.0 - abs(h)) + vec3(0.0, h, 0.0));
	particle.initial_position = n * r * 0.100000 + vec3(0.000000, 0.000000, 0.000000);
}

void m1_initialize_velocity_from_point(inout Particle particle) {
	float v = rand_range(particle.seed, 5.000000, 15.000000);
	vec3 front = vec3(0.0, 0.0, 1.0);
	vec3 to = -normalize(particle.initial_position);
	vec4 q = quat_rotation_to(front, to);
	vec3 right = rotated(vec3(1.0, 0.0, 0.0), q);
	particle.initial_velocity = normalize(particle.initial_position) * v;
}

void m2_initialize_size(inout Particle particle) {
	float s_c = rand_range(particle.seed, 0.050000, 0.100000);
	vec3 s = vec3(s_c, s_c, s_c);
	particle.initial_scale = s;
}

uniform vec4 u_m3_rgb : source_color;

void m3_initialize_color(inout Particle particle) {
	vec4 c = u_m3_rgb;
	float a = 1.0;
	particle.initial_color = vec4(c.rgb, a);
}

uniform sampler2D u_m4_influence_scale : repeat_disable;

// Functions for 3D noise / turbulence.
vec4 grad(vec4 p) {
	p = fract(vec4(
			dot(p, vec4(0.143081, 0.001724, 0.280166, 0.262771)),
			dot(p, vec4(0.645401, -0.047791, -0.146698, 0.595016)),
			dot(p, vec4(-0.499665, -0.095734, 0.425674, -0.207367)),
			dot(p, vec4(-0.013596, -0.848588, 0.423736, 0.17044))));
	return fract((p.xyzw * p.yzwx) * 2365.952041) * 2.0 - 1.0;
}

float noise(vec4 coord) {
	// Domain rotation to improve the look of XYZ slices + animation patterns.
	coord = vec4(
			coord.xyz + dot(coord, vec4(vec3(-0.1666667), -0.5)),
			dot(coord, vec4(0.5)));
	vec4 base = floor(coord), delta = coord - base;
	vec4 grad_0000 = grad(base + vec4(0.0, 0.0, 0.0, 0.0)), grad_1000 = grad(base + vec4(1.0, 0.0, 0.0, 0.0));
	vec4 grad_0100 = grad(base + vec4(0.0, 1.0, 0.0, 0.0)), grad_1100 = grad(base + vec4(1.0, 1.0, 0.0, 0.0));
	vec4 grad_0010 = grad(base + vec4(0.0, 0.0, 1.0, 0.0)), grad_1010 = grad(base + vec4(1.0, 0.0, 1.0, 0.0));
	vec4 grad_0110 = grad(base + vec4(0.0, 1.0, 1.0, 0.0)), grad_1110 = grad(base + vec4(1.0, 1.0, 1.0, 0.0));
	vec4 grad_0001 = grad(base + vec4(0.0, 0.0, 0.0, 1.0)), grad_1001 = grad(base + vec4(1.0, 0.0, 0.0, 1.0));
	vec4 grad_0101 = grad(base + vec4(0.0, 1.0, 0.0, 1.0)), grad_1101 = grad(base + vec4(1.0, 1.0, 0.0, 1.0));
	vec4 grad_0011 = grad(base + vec4(0.0, 0.0, 1.0, 1.0)), grad_1011 = grad(base + vec4(1.0, 0.0, 1.0, 1.0));
	vec4 grad_0111 = grad(base + vec4(0.0, 1.0, 1.0, 1.0)), grad_1111 = grad(base + vec4(1.0, 1.0, 1.0, 1.0));
	vec4 result_0123 = vec4(
			dot(delta - vec4(0.0, 0.0, 0.0, 0.0), grad_0000), dot(delta - vec4(1.0, 0.0, 0.0, 0.0), grad_1000),
			dot(delta - vec4(0.0, 1.0, 0.0, 0.0), grad_0100), dot(delta - vec4(1.0, 1.0, 0.0, 0.0), grad_1100));
	vec4 result_4567 = vec4(
			dot(delta - vec4(0.0, 0.0, 1.0, 0.0), grad_0010), dot(delta - vec4(1.0, 0.0, 1.0, 0.0), grad_1010),
			dot(delta - vec4(0.0, 1.0, 1.0, 0.0), grad_0110), dot(delta - vec4(1.0, 1.0, 1.0, 0.0), grad_1110));
	vec4 result_89AB = vec4(
			dot(delta - vec4(0.0, 0.0, 0.0, 1.0), grad_0001), dot(delta - vec4(1.0, 0.0, 0.0, 1.0), grad_1001),
			dot(delta - vec4(0.0, 1.0, 0.0, 1.0), grad_0101), dot(delta - vec4(1.0, 1.0, 0.0, 1.0), grad_1101));
	vec4 result_CDEF = vec4(
			dot(delta - vec4(0.0, 0.0, 1.0, 1.0), grad_0011), dot(delta - vec4(1.0, 0.0, 1.0, 1.0), grad_1011),
			dot(delta - vec4(0.0, 1.0, 1.0, 1.0), grad_0111), dot(delta - vec4(1.0, 1.0, 1.0, 1.0), grad_1111));
	vec4 fade = delta * delta * delta * (10.0 + delta * (-15.0 + delta * 6.0));
	vec4 result_W0 = mix(result_0123, result_89AB, fade.w), result_W1 = mix(result_4567, result_CDEF, fade.w);
	vec4 result_WZ = mix(result_W0, result_W1, fade.z);
	vec2 result_WZY = mix(result_WZ.xy, result_WZ.zw, fade.y);
	return mix(result_WZY.x, result_WZY.y, fade.x);
}

// Curl 3D and three-noise function with friendly permission by Isaac Cohen.
// Modified to accept 4D noise.
vec3 noise_3x(vec4 p) {
	float s = noise(p);
	float s1 = noise(p + vec4(vec3(0.0), 1.7320508 * 2048.333333));
	float s2 = noise(p - vec4(vec3(0.0), 1.7320508 * 2048.333333));
	vec3 c = vec3(s, s1, s2);
	return c;
}

vec3 curl_3d(vec4 p, float c) {
	float epsilon = 0.001 + c;
	vec4 dx = vec4(epsilon, 0.0, 0.0, 0.0);
	vec4 dy = vec4(0.0, epsilon, 0.0, 0.0);
	vec4 dz = vec4(0.0, 0.0, epsilon, 0.0);
	vec3 x0 = noise_3x(p - dx).xyz;
	vec3 x1 = noise_3x(p + dx).xyz;
	vec3 y0 = noise_3x(p - dy).xyz;
	vec3 y1 = noise_3x(p + dy).xyz;
	vec3 z0 = noise_3x(p - dz).xyz;
	vec3 z1 = noise_3x(p + dz).xyz;
	float x = (y1.z - y0.z) - (z1.y - z0.y);
	float y = (z1.x - z0.x) - (x1.z - x0.z);
	float z = (x1.y - x0.y) - (y1.x - y0.x);
	return normalize(vec3(x, y, z));
}

vec3 get_noise_direction(vec3 pos, float t) {
	float adj_contrast = max((1.000000 - 1.0), 0.0) * 70.0;
	vec4 noise_time = t * vec4(1.000000, 1.000000, 1.000000, 0.200000);
	vec4 noise_pos = vec4(pos * 0.500000, 0.0);
	vec3 noise_direction = curl_3d(noise_pos + noise_time, adj_contrast);
	noise_direction = mix(0.9 * noise_direction, noise_direction, 1.000000 - 9.0);
	return noise_direction;
}

void m4_noise(inout Particle particle) {
	float adjust = texture(u_m4_influence_scale, vec2(particle.normalized_age)).r;
	float initial_influence = rand_range(particle.seed, 0.100000, 1.000000);
	float influence = initial_influence * adjust;
	vec3 noise_direction = normalize(get_noise_direction(particle.position, particle.normalized_age));
	particle.velocity = mix(particle.velocity, noise_direction * length(particle.velocity) * (1.0 + (1.0 - influence) * 0.2), influence);
}

void m5_drag(inout Particle particle) {
	float limit = 1.0;
	float drag = 6.0;
	float speed = length(particle.velocity);
	float excess = max(0.0, speed - limit);
	vec3 dir = normalize_or_up(particle.velocity);
	particle.velocity = dir * (speed - particle.delta * excess * max(drag, 0.0));
}

void m6_align(inout Particle particle) {
	vec3 up = vec3(0.0, 1.0, 0.0);
	vec3 v = normalize(particle.velocity + particle.orbital_velocity + normalize(particle.position) * particle.radial_velocity);
	vec4 q = quat_rotation_to(up, v);
	mat3 m = mat3(1.0);
	particle.orientation = rotated(m, q);
}

uniform sampler2D u_m7_scale : repeat_disable;

void m7_scale_size(inout Particle particle) {
	vec3 s = vec3(texture(u_m7_scale, vec2(particle.normalized_age)).r);
	particle.scale *= s;
}


void initialize(inout Particle particle) {
	particle.initial_position = vec3(0.0);
	particle.initial_color = vec4(1.0);
	particle.initial_scale = vec3(1.0);
	particle.initial_velocity = vec3(0.0);
	particle.initial_orbital_speed = 0.0;
	particle.spawn_direction = vec3(0.0);
	particle.animation_offset = 0.0;
	particle.animation_speed = 1.0;
	particle.rotation_axis = vec3(1.0, 0.0, 0.0);
	particle.initial_rotation = 0.0;
	particle.radial_velocity = 0.0;
	particle.orbital_axis = vec3(0.0, 1.0, 0.0);
	particle.orbital_velocity = vec3(0.0);
	m0_shape_location_sphere(particle);
	m1_initialize_velocity_from_point(particle);
	m2_initialize_size(particle);
	m3_initialize_color(particle);
	particle.scale = particle.initial_scale;
	particle.color = particle.initial_color;
	particle.orbital_speed = particle.initial_orbital_speed;
}

void start() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = vec3(0.0);
	initialize(particle);
	if (rand(particle.seed) > AMOUNT_RATIO) {
		ACTIVE = false;
	}
	if (RESTART_CUSTOM) {
		CUSTOM = vec4(0.0);
		CUSTOM.x = particle.initial_rotation;
	}
	if (RESTART_COLOR) {
		COLOR = particle.initial_color;
	}
	if (RESTART_ROT_SCALE) {
		TRANSFORM[0].xyz = rotated(vec3(1.0, 0.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.x;
		TRANSFORM[1].xyz = rotated(vec3(0.0, 1.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.y;
		TRANSFORM[2].xyz = rotated(vec3(0.0, 0.0, 1.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.z;
	}
	if (RESTART_POSITION) {
		TRANSFORM[3].xyz = particle.initial_position;
		TRANSFORM = EMISSION_TRANSFORM * TRANSFORM;
	}
	if (RESTART_VELOCITY) {
		VELOCITY = (EMISSION_TRANSFORM * vec4(particle.initial_velocity, 0.0)).xyz;
		VELOCITY += EMITTER_VELOCITY * 0.0;//inherit_emitter_velocity_ratio;
	}
}

void process() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.delta = DELTA;
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = TRANSFORM[3].xyz;
	particle.velocity = VELOCITY;
	particle.orientation = mat3(
		normalize(TRANSFORM[0].xyz),
		normalize(TRANSFORM[1].xyz),
		normalize(TRANSFORM[2].xyz));
	particle.force = vec3(0.0);
	particle.emission_transform = EMISSION_TRANSFORM;
	particle.previous_transform = TRANSFORM;
	particle.rotation = CUSTOM.x;
	particle.just_spawned = CUSTOM.y == 0.0;
	particle.collided = COLLIDED;
	particle.collision_normal = COLLISION_NORMAL;
	particle.collision_depth = COLLISION_DEPTH;
	initialize(particle);
	CUSTOM.y += DELTA / particle.lifetime;
	CUSTOM.y = mix(CUSTOM.y, 1.0, INTERPOLATE_TO_END);
	CUSTOM.y = clamp(CUSTOM.y, 0.0, 1.0);
	particle.age = CUSTOM.y * particle.lifetime;
	particle.normalized_age = CUSTOM.y;
	m4_noise(particle);
	m5_drag(particle);
	m6_align(particle);
	m7_scale_size(particle);
	if (abs(particle.orbital_speed) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
		particle.orbital_velocity = (next - local_pos) / particle.delta;
	}
	if (abs(particle.radial_velocity) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 d = local_pos;
		float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
		vec3 next = local_pos + normalize_or_zero(d) * mag * sign(particle.radial_velocity);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
	}
	particle.position += particle.velocity * particle.delta;
	VELOCITY = particle.velocity;
	TRANSFORM[0].xyz = particle.orientation[0] * particle.scale.x;
	TRANSFORM[1].xyz = particle.orientation[1] * particle.scale.y;
	TRANSFORM[2].xyz = particle.orientation[2] * particle.scale.z;
	TRANSFORM[3].xyz = particle.position;
	COLOR = particle.color;
	CUSTOM.x = particle.rotation;
	
	if (particle.age >= LIFETIME) { ACTIVE = false;  }
}

"

[sub_resource type="Curve" id="Curve_gpvoe"]
_data = [Vector2(0.27702704, 0), 0.0, 2.4353778, 0, 0, Vector2(0.4493243, 0.65130675), 4.631362, -3.3368812, 0, 0, Vector2(0.6925675, 0), 0.0, 0.0, 0, 0]
point_count = 3

[sub_resource type="CurveTexture" id="CurveTexture_o0k8m"]
curve = SubResource("Curve_gpvoe")
metadata/uniform_name = "u_m4_influence_scale"

[sub_resource type="Curve" id="Curve_boerj"]
_data = [Vector2(0, 0), 0.0, 9.105417, 0, 0, Vector2(0.19932431, 1), 0.0, 0.0, 0, 0, Vector2(0.96283776, 0), -1.7147402, 0.0, 0, 0]
point_count = 3

[sub_resource type="CurveTexture" id="CurveTexture_4s0vd"]
curve = SubResource("Curve_boerj")
metadata/uniform_name = "u_m7_scale"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_irr66"]
shader = SubResource("Shader_fl3cn")
shader_parameter/u_m3_rgb = Color(0.94, 0.74855334, 0.49819997, 1)
shader_parameter/u_m4_influence_scale = SubResource("CurveTexture_o0k8m")
shader_parameter/u_m7_scale = SubResource("CurveTexture_4s0vd")

[sub_resource type="Shader" id="Shader_pr0us"]
code = "shader_type spatial;
render_mode unshaded, cull_disabled;

uniform float opacity = 1.0;
uniform float emission = 1.0;

uniform sampler2D tex_1 : source_color, filter_linear_mipmap, repeat_disable;
uniform vec2 tiling_1 = vec2(1.0);
uniform vec2 offset_1 = vec2(0.0);
uniform vec2 speed_1 = vec2(0.0);
uniform uint num_horizontal;
uniform uint num_vertical;

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}


varying flat uint instance_id;


void vertex()
{
	instance_id = uint(INSTANCE_ID);
	vec3 up = normalize(MODEL_MATRIX[1].xyz);
	vec3 right = normalize(cross(up, MAIN_CAM_INV_VIEW_MATRIX[2].xyz));
	vec3 front = normalize(cross(right, up));
	MODELVIEW_MATRIX = VIEW_MATRIX * mat4(
		vec4(right, 0.0),
		vec4(up, 0.0),
		vec4(front, 0.0),
		MODEL_MATRIX[3]);
	MODELVIEW_MATRIX = MODELVIEW_MATRIX * mat4(
			vec4(length(MODEL_MATRIX[0].xyz), 0.0, 0.0, 0.0),
			vec4(0.0, length(MODEL_MATRIX[1].xyz), 0.0, 0.0),
			vec4(0.0, 0.0, length(MODEL_MATRIX[2].xyz), 0.0),
			vec4(0.0, 0.0, 0.0, 1.0));
	MODELVIEW_NORMAL_MATRIX = mat3(MODELVIEW_MATRIX);
	uint h = max(num_horizontal, 1u);
	uint v = max(num_vertical, 1u);
	uint num = h * v;
	uint idx = min(uint(float(num) * INSTANCE_CUSTOM.z) % num, num - 1u);
	uint iy = idx / h;
	uint ix = idx - iy * h;
	float w = 1.0 / float(h);
	float height = 1.0 / float(v);
	UV.x = float(ix) * w + UV.x * w;
	UV.y = float(iy) * height + UV.y * height;
}

void fragment()
{
	uint seed = instance_id;
	vec4 tex_sampled_1 = texture(tex_1, UV + speed_1 * tiling_1 * TIME);
	ALPHA = COLOR.a;
	ALPHA = ALPHA * tex_sampled_1.r;
	ALPHA = clamp(ALPHA * opacity, 0.0, 1.0);
	ALBEDO = COLOR.rgb * emission;
	
}


"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_os3lh"]
render_priority = 0
shader = SubResource("Shader_pr0us")
shader_parameter/opacity = 1.0
shader_parameter/emission = 6.0
shader_parameter/tex_1 = ExtResource("26_fxwkx")
shader_parameter/tiling_1 = Vector2(1, 1)
shader_parameter/offset_1 = Vector2(0, 0)
shader_parameter/speed_1 = Vector2(0, 0)
shader_parameter/num_horizontal = 2
shader_parameter/num_vertical = 2

[sub_resource type="QuadMesh" id="QuadMesh_n8fbk"]
material = SubResource("ShaderMaterial_os3lh")

[sub_resource type="Shader" id="Shader_a2vkx"]
code = "shader_type particles;
render_mode disable_velocity;

const float DEG_TO_RAD = PI / 180.0;
const uint AMOUNT = uint(24);

struct Particle {
	uint seed;
	float delta;
	float lifetime;
	uint index;
	uint number;
	float age;
	float normalized_age;
	float normalized_index;
	float normalized_number;
	vec3 initial_position;
	vec3 initial_scale;
	vec4 initial_color;
	vec3 initial_velocity;
	float radial_velocity;
	vec3 orbital_axis;
	float initial_orbital_speed;
	vec3 spawn_direction;
	vec3 position;
	vec3 scale;
	vec4 color;
	vec3 velocity;
	vec3 orbital_velocity;
	float orbital_speed;
	vec3 rotation_axis;
	float initial_rotation;
	float rotation;
	mat3 orientation;
	vec3 force;
	float animation_offset;
	float animation_speed;
	mat4 emission_transform;
	mat4 previous_transform;
	bool just_spawned;
	bool collided;
	vec3 collision_normal;
	float collision_depth;
};

float remap(float value, float input_min, float input_max, float output_min, float output_max) {
	float v = clamp(value, input_min, input_max);
	return (v - input_min) / (input_max - input_min) * (output_max - output_min) + output_min;
}

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}

uint hash(uint x) {
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = (x >> uint(16)) ^ x;
	return x;
}

vec3 normalize_or_zero(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 0.0, 0.0);
}

vec3 normalize_or_up(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 1.0, 0.0);
}

vec4 quat(vec3 axis, float angle) {
  float s = sin(angle / 2.0);
  return vec4(axis.x * s, axis.y * s, axis.z * s, cos(angle / 2.0));
}

vec4 quat_rotation_to(vec3 from, vec3 to) {
	vec3 xunit = vec3(1.0, 0.0, 0.0);
	vec3 yunit = vec3(0.0, 1.0, 0.0);
	float dot = dot(from, to);
	if (dot < -0.999999) {
	    vec3 axis = cross(xunit, from);
	    if (length(axis) < 0.000001) {
	    	axis = cross(yunit, from);
	    }
	    axis = normalize(axis);
	    return quat(axis, PI);
	} else if (dot > 0.999999) {
		return vec4(0.0, 0.0, 0.0, 1.0);
	} else {
		vec3 axis = cross(from, to);
		return normalize(vec4(axis, 1.0 + dot));
	}
}

vec3 rotated(vec3 v, vec3 axis, float angle) {
  vec4 q = quat(axis, angle);
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

vec3 rotated(vec3 v, vec4 q) {
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

mat3 rotated(mat3 m, vec4 q) {
  return mat3(rotated(m[0], q), rotated(m[1], q), rotated(m[2], q));
}

float normalized_by_amount(uint n) {
	return float(n) / float(max(AMOUNT, 1u));
}
void update_orbital_velocity(inout Particle particle, mat4 emission_transform, mat4 transform) {
	if (abs(particle.orbital_speed) < 0.01 || particle.delta < 0.001) { return; }
	vec3 local_pos = (inverse(emission_transform) * transform[3]).xyz;
	vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
	particle.position = (emission_transform * vec4(next, 0.0)).xyz;
	particle.orbital_velocity = (next - local_pos) / particle.delta;
}

void apply_radial_velocity(inout Particle particle) {
	vec3 d = particle.position;
	float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
	particle.position += normalize_or_zero(d) * mag * sign(particle.radial_velocity);
}

uniform vec4 u_m0_rgb : source_color;

void m0_initialize_color(inout Particle particle) {
	vec4 c = u_m0_rgb;
	float a = 1.0;
	particle.initial_color = vec4(c.rgb, a);
}

void m1_initialize_size(inout Particle particle) {
	vec3 s = vec3(2.000000, 2.000000, 2.000000);
	particle.initial_scale = s;
}

void m2_initialize_rotation(inout Particle particle) {
	float a = rand_range(particle.seed, 0.000000, 360.000000);
	particle.rotation_axis = vec3(0.000000, 1.000000, 0.000000);
	particle.initial_rotation = a * DEG_TO_RAD;
}

uniform sampler2D u_m3_scale : repeat_disable;

void m3_scale_size(inout Particle particle) {
	vec3 s = vec3(texture(u_m3_scale, vec2(particle.normalized_age)).r);
	particle.scale *= s;
}

uniform sampler2D u_m4_alpha : repeat_disable;

void m4_scale_color(inout Particle particle) {
	vec4 c = vec4(1.000000, 1.000000, 1.000000, 1.000000);
	float a = texture(u_m4_alpha, vec2(particle.normalized_age)).r;
	particle.color = particle.initial_color * vec4(c.rgb, a);
}


void initialize(inout Particle particle) {
	particle.initial_position = vec3(0.0);
	particle.initial_color = vec4(1.0);
	particle.initial_scale = vec3(1.0);
	particle.initial_velocity = vec3(0.0);
	particle.initial_orbital_speed = 0.0;
	particle.spawn_direction = vec3(0.0);
	particle.animation_offset = 0.0;
	particle.animation_speed = 1.0;
	particle.rotation_axis = vec3(1.0, 0.0, 0.0);
	particle.initial_rotation = 0.0;
	particle.radial_velocity = 0.0;
	particle.orbital_axis = vec3(0.0, 1.0, 0.0);
	particle.orbital_velocity = vec3(0.0);
	m0_initialize_color(particle);
	m1_initialize_size(particle);
	m2_initialize_rotation(particle);
	particle.scale = particle.initial_scale;
	particle.color = particle.initial_color;
	particle.orbital_speed = particle.initial_orbital_speed;
}

void start() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = vec3(0.0);
	initialize(particle);
	if (rand(particle.seed) > AMOUNT_RATIO) {
		ACTIVE = false;
	}
	if (RESTART_CUSTOM) {
		CUSTOM = vec4(0.0);
		CUSTOM.x = particle.initial_rotation;
	}
	if (RESTART_COLOR) {
		COLOR = particle.initial_color;
	}
	if (RESTART_ROT_SCALE) {
		TRANSFORM[0].xyz = rotated(vec3(1.0, 0.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.x;
		TRANSFORM[1].xyz = rotated(vec3(0.0, 1.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.y;
		TRANSFORM[2].xyz = rotated(vec3(0.0, 0.0, 1.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.z;
	}
	if (RESTART_POSITION) {
		TRANSFORM[3].xyz = particle.initial_position;
		TRANSFORM = EMISSION_TRANSFORM * TRANSFORM;
	}
	if (RESTART_VELOCITY) {
		VELOCITY = (EMISSION_TRANSFORM * vec4(particle.initial_velocity, 0.0)).xyz;
		VELOCITY += EMITTER_VELOCITY * 0.0;//inherit_emitter_velocity_ratio;
	}
}

void process() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.delta = DELTA;
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = TRANSFORM[3].xyz;
	particle.velocity = VELOCITY;
	particle.orientation = mat3(
		normalize(TRANSFORM[0].xyz),
		normalize(TRANSFORM[1].xyz),
		normalize(TRANSFORM[2].xyz));
	particle.force = vec3(0.0);
	particle.emission_transform = EMISSION_TRANSFORM;
	particle.previous_transform = TRANSFORM;
	particle.rotation = CUSTOM.x;
	particle.just_spawned = CUSTOM.y == 0.0;
	particle.collided = COLLIDED;
	particle.collision_normal = COLLISION_NORMAL;
	particle.collision_depth = COLLISION_DEPTH;
	initialize(particle);
	CUSTOM.y += DELTA / particle.lifetime;
	CUSTOM.y = mix(CUSTOM.y, 1.0, INTERPOLATE_TO_END);
	CUSTOM.y = clamp(CUSTOM.y, 0.0, 1.0);
	particle.age = CUSTOM.y * particle.lifetime;
	particle.normalized_age = CUSTOM.y;
	m3_scale_size(particle);
	m4_scale_color(particle);
	if (abs(particle.orbital_speed) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
		particle.orbital_velocity = (next - local_pos) / particle.delta;
	}
	if (abs(particle.radial_velocity) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 d = local_pos;
		float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
		vec3 next = local_pos + normalize_or_zero(d) * mag * sign(particle.radial_velocity);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
	}
	particle.position += particle.velocity * particle.delta;
	VELOCITY = particle.velocity;
	TRANSFORM[0].xyz = particle.orientation[0] * particle.scale.x;
	TRANSFORM[1].xyz = particle.orientation[1] * particle.scale.y;
	TRANSFORM[2].xyz = particle.orientation[2] * particle.scale.z;
	TRANSFORM[3].xyz = particle.position;
	COLOR = particle.color;
	CUSTOM.x = particle.rotation;
	
	if (particle.age >= LIFETIME) { ACTIVE = false;  }
}

"

[sub_resource type="Curve" id="Curve_6bxl7"]
_data = [Vector2(0, 0), 0.0, 1.628611, 0, 0, Vector2(1, 1), 0.52929837, 0.0, 0, 0]
point_count = 2

[sub_resource type="CurveTexture" id="CurveTexture_yte20"]
curve = SubResource("Curve_6bxl7")
metadata/uniform_name = "u_m3_scale"

[sub_resource type="Curve" id="Curve_l7e70"]
_data = [Vector2(0, 1), 0.0, -1.0, 0, 1, Vector2(1, 0), -1.0, 0.0, 1, 0]
point_count = 2

[sub_resource type="CurveTexture" id="CurveTexture_eu8jq"]
curve = SubResource("Curve_l7e70")
metadata/uniform_name = "u_m4_alpha"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_rxfon"]
shader = SubResource("Shader_a2vkx")
shader_parameter/u_m0_rgb = Color(0.94, 0.74855334, 0.49819997, 1)
shader_parameter/u_m3_scale = SubResource("CurveTexture_yte20")
shader_parameter/u_m4_alpha = SubResource("CurveTexture_eu8jq")

[sub_resource type="Shader" id="Shader_g40xs"]
code = "shader_type spatial;
render_mode unshaded, cull_disabled;

uniform float opacity = 1.0;
uniform float emission = 1.0;

uniform sampler2D tex_1 : source_color, filter_linear_mipmap, repeat_disable;
uniform vec2 tiling_1 = vec2(1.0);
uniform vec2 offset_1 = vec2(0.0);
uniform vec2 speed_1 = vec2(0.0);

uniform sampler2D dissolve : source_color, filter_linear_mipmap, repeat_disable;

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}


varying flat uint instance_id;
varying float age;

void vertex()
{
	instance_id = uint(INSTANCE_ID);
	age = INSTANCE_CUSTOM.y;
}

float remap(float value, float input_min, float input_max, float output_min, float output_max) {
	return (clamp(value, input_min, input_max) - input_min) / (input_max - input_min) * (output_max - output_min) + output_min;
}

void fragment()
{
	uint seed = instance_id;
	vec4 tex_sampled_1 = texture(tex_1, UV + speed_1 * tiling_1 * TIME);
	ALPHA = COLOR.a;
	ALPHA = ALPHA * tex_sampled_1.r;
	ALPHA = clamp(ALPHA * opacity, 0.0, 1.0);
	float d = texture(dissolve, UV).r;
	float feather = 0.05;
	float age_ = remap(age, 0.0, 1.0, -feather, 1.0);
	float erosion = smoothstep(age_, age_ + feather, d);
	//ALPHA *= erosion;
	ALBEDO = COLOR.rgb * emission;
	
}


"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_0vnmd"]
render_priority = 0
shader = SubResource("Shader_g40xs")
shader_parameter/opacity = 1.0
shader_parameter/emission = 4.0
shader_parameter/tex_1 = ExtResource("31_w4r8r")
shader_parameter/tiling_1 = Vector2(1, 1)
shader_parameter/offset_1 = Vector2(0, 0)
shader_parameter/speed_1 = Vector2(0, 0)
shader_parameter/dissolve = ExtResource("30_6be7q")

[sub_resource type="PlaneMesh" id="PlaneMesh_eaah3"]
material = SubResource("ShaderMaterial_0vnmd")

[sub_resource type="Shader" id="Shader_llm58"]
code = "shader_type particles;
render_mode disable_velocity;

const float DEG_TO_RAD = PI / 180.0;
const uint AMOUNT = uint(24);

struct Particle {
	uint seed;
	float delta;
	float lifetime;
	uint index;
	uint number;
	float age;
	float normalized_age;
	float normalized_index;
	float normalized_number;
	vec3 initial_position;
	vec3 initial_scale;
	vec4 initial_color;
	vec3 initial_velocity;
	float radial_velocity;
	vec3 orbital_axis;
	float initial_orbital_speed;
	vec3 spawn_direction;
	vec3 position;
	vec3 scale;
	vec4 color;
	vec3 velocity;
	vec3 orbital_velocity;
	float orbital_speed;
	vec3 rotation_axis;
	float initial_rotation;
	float rotation;
	mat3 orientation;
	vec3 force;
	float animation_offset;
	float animation_speed;
	mat4 emission_transform;
	mat4 previous_transform;
	bool just_spawned;
	bool collided;
	vec3 collision_normal;
	float collision_depth;
};

float remap(float value, float input_min, float input_max, float output_min, float output_max) {
	float v = clamp(value, input_min, input_max);
	return (v - input_min) / (input_max - input_min) * (output_max - output_min) + output_min;
}

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}

uint hash(uint x) {
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = (x >> uint(16)) ^ x;
	return x;
}

vec3 normalize_or_zero(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 0.0, 0.0);
}

vec3 normalize_or_up(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 1.0, 0.0);
}

vec4 quat(vec3 axis, float angle) {
  float s = sin(angle / 2.0);
  return vec4(axis.x * s, axis.y * s, axis.z * s, cos(angle / 2.0));
}

vec4 quat_rotation_to(vec3 from, vec3 to) {
	vec3 xunit = vec3(1.0, 0.0, 0.0);
	vec3 yunit = vec3(0.0, 1.0, 0.0);
	float dot = dot(from, to);
	if (dot < -0.999999) {
	    vec3 axis = cross(xunit, from);
	    if (length(axis) < 0.000001) {
	    	axis = cross(yunit, from);
	    }
	    axis = normalize(axis);
	    return quat(axis, PI);
	} else if (dot > 0.999999) {
		return vec4(0.0, 0.0, 0.0, 1.0);
	} else {
		vec3 axis = cross(from, to);
		return normalize(vec4(axis, 1.0 + dot));
	}
}

vec3 rotated(vec3 v, vec3 axis, float angle) {
  vec4 q = quat(axis, angle);
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

vec3 rotated(vec3 v, vec4 q) {
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

mat3 rotated(mat3 m, vec4 q) {
  return mat3(rotated(m[0], q), rotated(m[1], q), rotated(m[2], q));
}

float normalized_by_amount(uint n) {
	return float(n) / float(max(AMOUNT, 1u));
}
void update_orbital_velocity(inout Particle particle, mat4 emission_transform, mat4 transform) {
	if (abs(particle.orbital_speed) < 0.01 || particle.delta < 0.001) { return; }
	vec3 local_pos = (inverse(emission_transform) * transform[3]).xyz;
	vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
	particle.position = (emission_transform * vec4(next, 0.0)).xyz;
	particle.orbital_velocity = (next - local_pos) / particle.delta;
}

void apply_radial_velocity(inout Particle particle) {
	vec3 d = particle.position;
	float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
	particle.position += normalize_or_zero(d) * mag * sign(particle.radial_velocity);
}

uniform vec4 u_m0_rgb : source_color;

void m0_initialize_color(inout Particle particle) {
	vec4 c = u_m0_rgb;
	float a = 0.4;
	particle.initial_color = vec4(c.rgb, a);
}

void m1_initialize_rotation(inout Particle particle) {
	float a = rand_range(particle.seed, 0.000000, 360.000000);
	particle.rotation_axis = vec3(0.000000, 0.000000, 1.000000);
	particle.initial_rotation = a * DEG_TO_RAD;
}

void m2_initialize_size(inout Particle particle) {
	vec3 s = vec3(3.000000, 3.000000, 3.000000);
	particle.initial_scale = s;
}

uniform sampler2D u_m3_scale : repeat_disable;

void m3_scale_size(inout Particle particle) {
	vec3 s = vec3(texture(u_m3_scale, vec2(particle.normalized_age)).r);
	particle.scale *= s;
}

uniform sampler2D u_m4_alpha : repeat_disable;

void m4_scale_color(inout Particle particle) {
	vec4 c = vec4(1.000000, 1.000000, 1.000000, 1.000000);
	float a = texture(u_m4_alpha, vec2(particle.normalized_age)).r;
	particle.color = particle.initial_color * vec4(c.rgb, a);
}


void initialize(inout Particle particle) {
	particle.initial_position = vec3(0.0);
	particle.initial_color = vec4(1.0);
	particle.initial_scale = vec3(1.0);
	particle.initial_velocity = vec3(0.0);
	particle.initial_orbital_speed = 0.0;
	particle.spawn_direction = vec3(0.0);
	particle.animation_offset = 0.0;
	particle.animation_speed = 1.0;
	particle.rotation_axis = vec3(1.0, 0.0, 0.0);
	particle.initial_rotation = 0.0;
	particle.radial_velocity = 0.0;
	particle.orbital_axis = vec3(0.0, 1.0, 0.0);
	particle.orbital_velocity = vec3(0.0);
	m0_initialize_color(particle);
	m1_initialize_rotation(particle);
	m2_initialize_size(particle);
	particle.scale = particle.initial_scale;
	particle.color = particle.initial_color;
	particle.orbital_speed = particle.initial_orbital_speed;
}

void start() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = vec3(0.0);
	initialize(particle);
	if (rand(particle.seed) > AMOUNT_RATIO) {
		ACTIVE = false;
	}
	if (RESTART_CUSTOM) {
		CUSTOM = vec4(0.0);
		CUSTOM.x = particle.initial_rotation;
	}
	if (RESTART_COLOR) {
		COLOR = particle.initial_color;
	}
	if (RESTART_ROT_SCALE) {
		TRANSFORM[0].xyz = rotated(vec3(1.0, 0.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.x;
		TRANSFORM[1].xyz = rotated(vec3(0.0, 1.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.y;
		TRANSFORM[2].xyz = rotated(vec3(0.0, 0.0, 1.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.z;
	}
	if (RESTART_POSITION) {
		TRANSFORM[3].xyz = particle.initial_position;
		TRANSFORM = EMISSION_TRANSFORM * TRANSFORM;
	}
	if (RESTART_VELOCITY) {
		VELOCITY = (EMISSION_TRANSFORM * vec4(particle.initial_velocity, 0.0)).xyz;
		VELOCITY += EMITTER_VELOCITY * 0.0;//inherit_emitter_velocity_ratio;
	}
}

void process() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.delta = DELTA;
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = TRANSFORM[3].xyz;
	particle.velocity = VELOCITY;
	particle.orientation = mat3(
		normalize(TRANSFORM[0].xyz),
		normalize(TRANSFORM[1].xyz),
		normalize(TRANSFORM[2].xyz));
	particle.force = vec3(0.0);
	particle.emission_transform = EMISSION_TRANSFORM;
	particle.previous_transform = TRANSFORM;
	particle.rotation = CUSTOM.x;
	particle.just_spawned = CUSTOM.y == 0.0;
	particle.collided = COLLIDED;
	particle.collision_normal = COLLISION_NORMAL;
	particle.collision_depth = COLLISION_DEPTH;
	initialize(particle);
	CUSTOM.y += DELTA / particle.lifetime;
	CUSTOM.y = mix(CUSTOM.y, 1.0, INTERPOLATE_TO_END);
	CUSTOM.y = clamp(CUSTOM.y, 0.0, 1.0);
	particle.age = CUSTOM.y * particle.lifetime;
	particle.normalized_age = CUSTOM.y;
	m3_scale_size(particle);
	m4_scale_color(particle);
	if (abs(particle.orbital_speed) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
		particle.orbital_velocity = (next - local_pos) / particle.delta;
	}
	if (abs(particle.radial_velocity) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 d = local_pos;
		float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
		vec3 next = local_pos + normalize_or_zero(d) * mag * sign(particle.radial_velocity);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
	}
	particle.position += particle.velocity * particle.delta;
	VELOCITY = particle.velocity;
	TRANSFORM[0].xyz = particle.orientation[0] * particle.scale.x;
	TRANSFORM[1].xyz = particle.orientation[1] * particle.scale.y;
	TRANSFORM[2].xyz = particle.orientation[2] * particle.scale.z;
	TRANSFORM[3].xyz = particle.position;
	COLOR = particle.color;
	CUSTOM.x = particle.rotation;
	
	if (particle.age >= LIFETIME) { ACTIVE = false;  }
}

"

[sub_resource type="Curve" id="Curve_ypkhk"]
_data = [Vector2(0, 0), 0.0, 5.580388, 0, 0, Vector2(0.28378376, 1), 0.0, 0.0, 0, 0]
point_count = 2

[sub_resource type="CurveTexture" id="CurveTexture_pe8ma"]
curve = SubResource("Curve_ypkhk")
metadata/uniform_name = "u_m3_scale"

[sub_resource type="Curve" id="Curve_7fnxt"]
_data = [Vector2(0, 0), 0.0, 6.8837214, 0, 1, Vector2(0.14527026, 1), 6.8837214, -1.1552151, 1, 0, Vector2(1, 0), -1.173414, 0.0, 0, 0]
point_count = 3

[sub_resource type="CurveTexture" id="CurveTexture_bk8m1"]
curve = SubResource("Curve_7fnxt")
metadata/uniform_name = "u_m4_alpha"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_s764p"]
shader = SubResource("Shader_llm58")
shader_parameter/u_m0_rgb = Color(0.94, 0.74855334, 0.49819997, 1)
shader_parameter/u_m3_scale = SubResource("CurveTexture_pe8ma")
shader_parameter/u_m4_alpha = SubResource("CurveTexture_bk8m1")

[sub_resource type="Shader" id="Shader_sldnv"]
code = "shader_type spatial;
render_mode unshaded, blend_add, cull_disabled;

uniform float opacity = 1.0;
uniform float emission = 1.0;

uniform sampler2D tex_1 : source_color, filter_linear_mipmap, repeat_disable;
uniform vec2 tiling_1 = vec2(1.0);
uniform vec2 offset_1 = vec2(0.0);
uniform vec2 speed_1 = vec2(0.0);

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}


varying flat uint instance_id;


void vertex()
{
	instance_id = uint(INSTANCE_ID);
	vec3 pos = MODEL_MATRIX[3].xyz;
	pos += normalize(CAMERA_POSITION_WORLD - pos) * 2.5;
	mat4 model = mat4(
		normalize(MAIN_CAM_INV_VIEW_MATRIX[0]),
		normalize(MAIN_CAM_INV_VIEW_MATRIX[1]),
		normalize(MAIN_CAM_INV_VIEW_MATRIX[2]),
		vec4(pos, 1.0));
	model = model * mat4(
		vec4(cos(INSTANCE_CUSTOM.x), -sin(INSTANCE_CUSTOM.x), 0.0, 0.0),
		vec4(sin(INSTANCE_CUSTOM.x), cos(INSTANCE_CUSTOM.x), 0.0, 0.0),
		vec4(0.0, 0.0, 1.0, 0.0),
		vec4(0.0, 0.0, 0.0, 1.0));
	model = model * mat4(
		vec4(length(MODEL_MATRIX[0].xyz), 0.0, 0.0, 0.0),
		vec4(0.0, length(MODEL_MATRIX[1].xyz), 0.0, 0.0),
		vec4(0.0, 0.0, length(MODEL_MATRIX[2].xyz), 0.0),
		vec4(0.0, 0.0, 0.0, 1.0));
	MODELVIEW_MATRIX = VIEW_MATRIX * model;
	MODELVIEW_NORMAL_MATRIX = mat3(MODELVIEW_MATRIX);
}

void fragment()
{
	uint seed = instance_id;
	vec4 tex_sampled_1 = texture(tex_1, UV + speed_1 * tiling_1 * TIME);
	ALPHA = COLOR.a;
	ALPHA = ALPHA * tex_sampled_1.r;
	ALPHA = clamp(ALPHA * opacity, 0.0, 1.0);
	ALBEDO = COLOR.rgb * emission;
	ALBEDO *= tex_sampled_1.rgb;
	
}


"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_qcoli"]
render_priority = 2
shader = SubResource("Shader_sldnv")
shader_parameter/opacity = 1.0
shader_parameter/emission = 10.0
shader_parameter/tex_1 = ExtResource("32_jcfxp")
shader_parameter/tiling_1 = Vector2(1, 1)
shader_parameter/offset_1 = Vector2(0, 0)
shader_parameter/speed_1 = Vector2(0, 0)

[sub_resource type="QuadMesh" id="QuadMesh_1vk34"]
material = SubResource("ShaderMaterial_qcoli")

[sub_resource type="Shader" id="Shader_mh3kr"]
code = "shader_type particles;
render_mode disable_velocity;

const float DEG_TO_RAD = PI / 180.0;
const uint AMOUNT = uint(24);

struct Particle {
	uint seed;
	float delta;
	float lifetime;
	uint index;
	uint number;
	float age;
	float normalized_age;
	float normalized_index;
	float normalized_number;
	vec3 initial_position;
	vec3 initial_scale;
	vec4 initial_color;
	vec3 initial_velocity;
	float radial_velocity;
	vec3 orbital_axis;
	float initial_orbital_speed;
	vec3 spawn_direction;
	vec3 position;
	vec3 scale;
	vec4 color;
	vec3 velocity;
	vec3 orbital_velocity;
	float orbital_speed;
	vec3 rotation_axis;
	float initial_rotation;
	float rotation;
	mat3 orientation;
	vec3 force;
	float animation_offset;
	float animation_speed;
	mat4 emission_transform;
	mat4 previous_transform;
	bool just_spawned;
	bool collided;
	vec3 collision_normal;
	float collision_depth;
};

float remap(float value, float input_min, float input_max, float output_min, float output_max) {
	float v = clamp(value, input_min, input_max);
	return (v - input_min) / (input_max - input_min) * (output_max - output_min) + output_min;
}

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}

uint hash(uint x) {
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = (x >> uint(16)) ^ x;
	return x;
}

vec3 normalize_or_zero(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 0.0, 0.0);
}

vec3 normalize_or_up(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 1.0, 0.0);
}

vec4 quat(vec3 axis, float angle) {
  float s = sin(angle / 2.0);
  return vec4(axis.x * s, axis.y * s, axis.z * s, cos(angle / 2.0));
}

vec4 quat_rotation_to(vec3 from, vec3 to) {
	vec3 xunit = vec3(1.0, 0.0, 0.0);
	vec3 yunit = vec3(0.0, 1.0, 0.0);
	float dot = dot(from, to);
	if (dot < -0.999999) {
	    vec3 axis = cross(xunit, from);
	    if (length(axis) < 0.000001) {
	    	axis = cross(yunit, from);
	    }
	    axis = normalize(axis);
	    return quat(axis, PI);
	} else if (dot > 0.999999) {
		return vec4(0.0, 0.0, 0.0, 1.0);
	} else {
		vec3 axis = cross(from, to);
		return normalize(vec4(axis, 1.0 + dot));
	}
}

vec3 rotated(vec3 v, vec3 axis, float angle) {
  vec4 q = quat(axis, angle);
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

vec3 rotated(vec3 v, vec4 q) {
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

mat3 rotated(mat3 m, vec4 q) {
  return mat3(rotated(m[0], q), rotated(m[1], q), rotated(m[2], q));
}

float normalized_by_amount(uint n) {
	return float(n) / float(max(AMOUNT, 1u));
}
void update_orbital_velocity(inout Particle particle, mat4 emission_transform, mat4 transform) {
	if (abs(particle.orbital_speed) < 0.01 || particle.delta < 0.001) { return; }
	vec3 local_pos = (inverse(emission_transform) * transform[3]).xyz;
	vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
	particle.position = (emission_transform * vec4(next, 0.0)).xyz;
	particle.orbital_velocity = (next - local_pos) / particle.delta;
}

void apply_radial_velocity(inout Particle particle) {
	vec3 d = particle.position;
	float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
	particle.position += normalize_or_zero(d) * mag * sign(particle.radial_velocity);
}

uniform vec4 u_m0_rgb : source_color;

void m0_initialize_color(inout Particle particle) {
	vec4 c = u_m0_rgb;
	float a = 0.4;
	particle.initial_color = vec4(c.rgb, a);
}

void m1_initialize_size(inout Particle particle) {
	vec3 s = vec3(3.000000, 3.000000, 3.000000);
	particle.initial_scale = s;
}

uniform sampler2D u_m2_scale : repeat_disable;

void m2_scale_size(inout Particle particle) {
	vec3 s = vec3(texture(u_m2_scale, vec2(particle.normalized_age)).r);
	particle.scale *= s;
}

uniform sampler2D u_m3_alpha : repeat_disable;

void m3_scale_color(inout Particle particle) {
	vec4 c = vec4(1.000000, 1.000000, 1.000000, 1.000000);
	float a = texture(u_m3_alpha, vec2(particle.normalized_age)).r;
	particle.color = particle.initial_color * vec4(c.rgb, a);
}


void initialize(inout Particle particle) {
	particle.initial_position = vec3(0.0);
	particle.initial_color = vec4(1.0);
	particle.initial_scale = vec3(1.0);
	particle.initial_velocity = vec3(0.0);
	particle.initial_orbital_speed = 0.0;
	particle.spawn_direction = vec3(0.0);
	particle.animation_offset = 0.0;
	particle.animation_speed = 1.0;
	particle.rotation_axis = vec3(1.0, 0.0, 0.0);
	particle.initial_rotation = 0.0;
	particle.radial_velocity = 0.0;
	particle.orbital_axis = vec3(0.0, 1.0, 0.0);
	particle.orbital_velocity = vec3(0.0);
	m0_initialize_color(particle);
	m1_initialize_size(particle);
	particle.scale = particle.initial_scale;
	particle.color = particle.initial_color;
	particle.orbital_speed = particle.initial_orbital_speed;
}

void start() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = vec3(0.0);
	initialize(particle);
	if (rand(particle.seed) > AMOUNT_RATIO) {
		ACTIVE = false;
	}
	if (RESTART_CUSTOM) {
		CUSTOM = vec4(0.0);
		CUSTOM.x = particle.initial_rotation;
	}
	if (RESTART_COLOR) {
		COLOR = particle.initial_color;
	}
	if (RESTART_ROT_SCALE) {
		TRANSFORM[0].xyz = rotated(vec3(1.0, 0.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.x;
		TRANSFORM[1].xyz = rotated(vec3(0.0, 1.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.y;
		TRANSFORM[2].xyz = rotated(vec3(0.0, 0.0, 1.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.z;
	}
	if (RESTART_POSITION) {
		TRANSFORM[3].xyz = particle.initial_position;
		TRANSFORM = EMISSION_TRANSFORM * TRANSFORM;
	}
	if (RESTART_VELOCITY) {
		VELOCITY = (EMISSION_TRANSFORM * vec4(particle.initial_velocity, 0.0)).xyz;
		VELOCITY += EMITTER_VELOCITY * 0.0;//inherit_emitter_velocity_ratio;
	}
}

void process() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.delta = DELTA;
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = TRANSFORM[3].xyz;
	particle.velocity = VELOCITY;
	particle.orientation = mat3(
		normalize(TRANSFORM[0].xyz),
		normalize(TRANSFORM[1].xyz),
		normalize(TRANSFORM[2].xyz));
	particle.force = vec3(0.0);
	particle.emission_transform = EMISSION_TRANSFORM;
	particle.previous_transform = TRANSFORM;
	particle.rotation = CUSTOM.x;
	particle.just_spawned = CUSTOM.y == 0.0;
	particle.collided = COLLIDED;
	particle.collision_normal = COLLISION_NORMAL;
	particle.collision_depth = COLLISION_DEPTH;
	initialize(particle);
	CUSTOM.y += DELTA / particle.lifetime;
	CUSTOM.y = mix(CUSTOM.y, 1.0, INTERPOLATE_TO_END);
	CUSTOM.y = clamp(CUSTOM.y, 0.0, 1.0);
	particle.age = CUSTOM.y * particle.lifetime;
	particle.normalized_age = CUSTOM.y;
	m2_scale_size(particle);
	m3_scale_color(particle);
	if (abs(particle.orbital_speed) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
		particle.orbital_velocity = (next - local_pos) / particle.delta;
	}
	if (abs(particle.radial_velocity) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 d = local_pos;
		float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
		vec3 next = local_pos + normalize_or_zero(d) * mag * sign(particle.radial_velocity);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
	}
	particle.position += particle.velocity * particle.delta;
	VELOCITY = particle.velocity;
	TRANSFORM[0].xyz = particle.orientation[0] * particle.scale.x;
	TRANSFORM[1].xyz = particle.orientation[1] * particle.scale.y;
	TRANSFORM[2].xyz = particle.orientation[2] * particle.scale.z;
	TRANSFORM[3].xyz = particle.position;
	COLOR = particle.color;
	CUSTOM.x = particle.rotation;
	
	if (particle.age >= LIFETIME) { ACTIVE = false;  }
}

"

[sub_resource type="Curve" id="Curve_70nrf"]
_data = [Vector2(0, 0), 0.0, 7.9024663, 0, 0, Vector2(0.21283782, 1), 0.0, 0.0, 0, 0]
point_count = 2

[sub_resource type="CurveTexture" id="CurveTexture_tek2r"]
curve = SubResource("Curve_70nrf")
metadata/uniform_name = "u_m2_scale"

[sub_resource type="Curve" id="Curve_8fr6c"]
_data = [Vector2(0, 0), 0.0, 5.103448, 0, 1, Vector2(0.19594595, 1), 5.103448, -1.2473224, 1, 0, Vector2(1, 0), -1.2423377, 0.0, 0, 0]
point_count = 3

[sub_resource type="CurveTexture" id="CurveTexture_shc4o"]
curve = SubResource("Curve_8fr6c")
metadata/uniform_name = "u_m3_alpha"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_hddlj"]
shader = SubResource("Shader_mh3kr")
shader_parameter/u_m0_rgb = Color(0.94, 0.74855334, 0.49819997, 1)
shader_parameter/u_m2_scale = SubResource("CurveTexture_tek2r")
shader_parameter/u_m3_alpha = SubResource("CurveTexture_shc4o")

[sub_resource type="Shader" id="Shader_xhmx7"]
code = "shader_type spatial;
render_mode unshaded, blend_add, cull_disabled;

uniform float opacity = 1.0;
uniform float emission = 1.0;

uniform sampler2D tex_1 : source_color, filter_linear_mipmap, repeat_disable;
uniform vec2 tiling_1 = vec2(1.0);
uniform vec2 offset_1 = vec2(0.0);
uniform vec2 speed_1 = vec2(0.0);

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}


varying flat uint instance_id;


void vertex()
{
	instance_id = uint(INSTANCE_ID);
	vec3 pos = MODEL_MATRIX[3].xyz;
	pos += normalize(CAMERA_POSITION_WORLD - pos) * 2.5;
	mat4 model = mat4(
		normalize(MAIN_CAM_INV_VIEW_MATRIX[0]),
		normalize(MAIN_CAM_INV_VIEW_MATRIX[1]),
		normalize(MAIN_CAM_INV_VIEW_MATRIX[2]),
		vec4(pos, 1.0));
	model = model * mat4(
		vec4(cos(INSTANCE_CUSTOM.x), -sin(INSTANCE_CUSTOM.x), 0.0, 0.0),
		vec4(sin(INSTANCE_CUSTOM.x), cos(INSTANCE_CUSTOM.x), 0.0, 0.0),
		vec4(0.0, 0.0, 1.0, 0.0),
		vec4(0.0, 0.0, 0.0, 1.0));
	model = model * mat4(
		vec4(length(MODEL_MATRIX[0].xyz), 0.0, 0.0, 0.0),
		vec4(0.0, length(MODEL_MATRIX[1].xyz), 0.0, 0.0),
		vec4(0.0, 0.0, length(MODEL_MATRIX[2].xyz), 0.0),
		vec4(0.0, 0.0, 0.0, 1.0));
	MODELVIEW_MATRIX = VIEW_MATRIX * model;
	MODELVIEW_NORMAL_MATRIX = mat3(MODELVIEW_MATRIX);
}

void fragment()
{
	uint seed = instance_id;
	vec4 tex_sampled_1 = texture(tex_1, UV + speed_1 * tiling_1 * TIME);
	ALPHA = COLOR.a;
	ALPHA = ALPHA * tex_sampled_1.r;
	ALPHA = clamp(ALPHA * opacity, 0.0, 1.0);
	ALBEDO = COLOR.rgb * emission;
	ALBEDO *= tex_sampled_1.rgb;
	
}


"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_ldxie"]
render_priority = 0
shader = SubResource("Shader_xhmx7")
shader_parameter/opacity = 1.0
shader_parameter/emission = 2.0
shader_parameter/tex_1 = ExtResource("33_3jlxe")
shader_parameter/tiling_1 = Vector2(1, 1)
shader_parameter/offset_1 = Vector2(0, 0)
shader_parameter/speed_1 = Vector2(0, 0)

[sub_resource type="QuadMesh" id="QuadMesh_kwytq"]
material = SubResource("ShaderMaterial_ldxie")

[sub_resource type="Shader" id="Shader_3dayl"]
code = "shader_type particles;
render_mode disable_velocity;

const float DEG_TO_RAD = PI / 180.0;
const uint AMOUNT = uint(288);

struct Particle {
	uint seed;
	float delta;
	float lifetime;
	uint index;
	uint number;
	float age;
	float normalized_age;
	float normalized_index;
	float normalized_number;
	vec3 initial_position;
	vec3 initial_scale;
	vec4 initial_color;
	vec3 initial_velocity;
	float radial_velocity;
	vec3 orbital_axis;
	float initial_orbital_speed;
	vec3 spawn_direction;
	vec3 position;
	vec3 scale;
	vec4 color;
	vec3 velocity;
	vec3 orbital_velocity;
	float orbital_speed;
	vec3 rotation_axis;
	float initial_rotation;
	float rotation;
	mat3 orientation;
	vec3 force;
	float animation_offset;
	float animation_speed;
	mat4 emission_transform;
	mat4 previous_transform;
	bool just_spawned;
	bool collided;
	vec3 collision_normal;
	float collision_depth;
};

float remap(float value, float input_min, float input_max, float output_min, float output_max) {
	float v = clamp(value, input_min, input_max);
	return (v - input_min) / (input_max - input_min) * (output_max - output_min) + output_min;
}

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}

uint hash(uint x) {
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = (x >> uint(16)) ^ x;
	return x;
}

vec3 normalize_or_zero(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 0.0, 0.0);
}

vec3 normalize_or_up(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 1.0, 0.0);
}

vec4 quat(vec3 axis, float angle) {
  float s = sin(angle / 2.0);
  return vec4(axis.x * s, axis.y * s, axis.z * s, cos(angle / 2.0));
}

vec4 quat_rotation_to(vec3 from, vec3 to) {
	vec3 xunit = vec3(1.0, 0.0, 0.0);
	vec3 yunit = vec3(0.0, 1.0, 0.0);
	float dot = dot(from, to);
	if (dot < -0.999999) {
	    vec3 axis = cross(xunit, from);
	    if (length(axis) < 0.000001) {
	    	axis = cross(yunit, from);
	    }
	    axis = normalize(axis);
	    return quat(axis, PI);
	} else if (dot > 0.999999) {
		return vec4(0.0, 0.0, 0.0, 1.0);
	} else {
		vec3 axis = cross(from, to);
		return normalize(vec4(axis, 1.0 + dot));
	}
}

vec3 rotated(vec3 v, vec3 axis, float angle) {
  vec4 q = quat(axis, angle);
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

vec3 rotated(vec3 v, vec4 q) {
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

mat3 rotated(mat3 m, vec4 q) {
  return mat3(rotated(m[0], q), rotated(m[1], q), rotated(m[2], q));
}

float normalized_by_amount(uint n) {
	return float(n) / float(max(AMOUNT, 1u));
}
void update_orbital_velocity(inout Particle particle, mat4 emission_transform, mat4 transform) {
	if (abs(particle.orbital_speed) < 0.01 || particle.delta < 0.001) { return; }
	vec3 local_pos = (inverse(emission_transform) * transform[3]).xyz;
	vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
	particle.position = (emission_transform * vec4(next, 0.0)).xyz;
	particle.orbital_velocity = (next - local_pos) / particle.delta;
}

void apply_radial_velocity(inout Particle particle) {
	vec3 d = particle.position;
	float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
	particle.position += normalize_or_zero(d) * mag * sign(particle.radial_velocity);
}

void m0_shape_location_sphere(inout Particle particle) {
	float r = 1.0;
	float u = rand_range(particle.seed, 0.000000, 1.000000);
	float v = rand_range(particle.seed, 0.000000, 0.300000);
	vec3 n = vec3(1.0, 0.0, 0.0);
	n = rotated(n, vec3(0.0, 1.0, 0.0), u * 360.0 * DEG_TO_RAD);
	float h = 1.0 - 2.0 * v;
	n = normalize(n * (1.0 - abs(h)) + vec3(0.0, h, 0.0));
	particle.initial_position = n * r * 0.100000 + vec3(0.000000, 0.000000, 0.000000);
}

void m1_initialize_velocity_from_point(inout Particle particle) {
	float v = rand_range(particle.seed, 5.000000, 8.000000);
	vec3 front = vec3(0.0, 0.0, 1.0);
	vec3 to = -normalize(particle.initial_position);
	vec4 q = quat_rotation_to(front, to);
	vec3 right = rotated(vec3(1.0, 0.0, 0.0), q);
	particle.initial_velocity = normalize(particle.initial_position) * v;
}

uniform sampler2D u_m2_rgb : source_color, repeat_disable;

uniform sampler2D u_m2_alpha : repeat_disable;

void m2_scale_color(inout Particle particle) {
	vec4 c = vec4(texture(u_m2_rgb, vec2(particle.normalized_age)));
	float a = texture(u_m2_alpha, vec2(particle.normalized_age)).r;
	particle.color = particle.initial_color * vec4(c.rgb, a);
}

void m3_drag(inout Particle particle) {
	float limit = 1.0;
	float drag = 12.0;
	float speed = length(particle.velocity);
	float excess = max(0.0, speed - limit);
	vec3 dir = normalize_or_up(particle.velocity);
	particle.velocity = dir * (speed - particle.delta * excess * max(drag, 0.0));
}


void initialize(inout Particle particle) {
	particle.initial_position = vec3(0.0);
	particle.initial_color = vec4(1.0);
	particle.initial_scale = vec3(1.0);
	particle.initial_velocity = vec3(0.0);
	particle.initial_orbital_speed = 0.0;
	particle.spawn_direction = vec3(0.0);
	particle.animation_offset = 0.0;
	particle.animation_speed = 1.0;
	particle.rotation_axis = vec3(1.0, 0.0, 0.0);
	particle.initial_rotation = 0.0;
	particle.radial_velocity = 0.0;
	particle.orbital_axis = vec3(0.0, 1.0, 0.0);
	particle.orbital_velocity = vec3(0.0);
	m0_shape_location_sphere(particle);
	m1_initialize_velocity_from_point(particle);
	particle.scale = particle.initial_scale;
	particle.color = particle.initial_color;
	particle.orbital_speed = particle.initial_orbital_speed;
}

void start() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = vec3(0.0);
	initialize(particle);
	if (rand(particle.seed) > AMOUNT_RATIO) {
		ACTIVE = false;
	}
	if (RESTART_CUSTOM) {
		CUSTOM = vec4(0.0);
		CUSTOM.x = particle.initial_rotation;
	}
	if (RESTART_COLOR) {
		COLOR = particle.initial_color;
	}
	if (RESTART_ROT_SCALE) {
		TRANSFORM[0].xyz = rotated(vec3(1.0, 0.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.x;
		TRANSFORM[1].xyz = rotated(vec3(0.0, 1.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.y;
		TRANSFORM[2].xyz = rotated(vec3(0.0, 0.0, 1.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.z;
	}
	if (RESTART_POSITION) {
		TRANSFORM[3].xyz = particle.initial_position;
		TRANSFORM = EMISSION_TRANSFORM * TRANSFORM;
	}
	if (RESTART_VELOCITY) {
		VELOCITY = (EMISSION_TRANSFORM * vec4(particle.initial_velocity, 0.0)).xyz;
		VELOCITY += EMITTER_VELOCITY * 0.0;//inherit_emitter_velocity_ratio;
	}
}

void process() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.delta = DELTA;
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = TRANSFORM[3].xyz;
	particle.velocity = VELOCITY;
	particle.orientation = mat3(
		normalize(TRANSFORM[0].xyz),
		normalize(TRANSFORM[1].xyz),
		normalize(TRANSFORM[2].xyz));
	particle.force = vec3(0.0);
	particle.emission_transform = EMISSION_TRANSFORM;
	particle.previous_transform = TRANSFORM;
	particle.rotation = CUSTOM.x;
	particle.just_spawned = CUSTOM.y == 0.0;
	particle.collided = COLLIDED;
	particle.collision_normal = COLLISION_NORMAL;
	particle.collision_depth = COLLISION_DEPTH;
	initialize(particle);
	CUSTOM.y += DELTA / particle.lifetime;
	CUSTOM.y = mix(CUSTOM.y, 1.0, INTERPOLATE_TO_END);
	CUSTOM.y = clamp(CUSTOM.y, 0.0, 1.0);
	particle.age = CUSTOM.y * particle.lifetime;
	particle.normalized_age = CUSTOM.y;
	m2_scale_color(particle);
	m3_drag(particle);
	if (abs(particle.orbital_speed) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
		particle.orbital_velocity = (next - local_pos) / particle.delta;
	}
	if (abs(particle.radial_velocity) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 d = local_pos;
		float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
		vec3 next = local_pos + normalize_or_zero(d) * mag * sign(particle.radial_velocity);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
	}
	particle.position += particle.velocity * particle.delta;
	VELOCITY = particle.velocity;
	TRANSFORM[0].xyz = particle.orientation[0] * particle.scale.x;
	TRANSFORM[1].xyz = particle.orientation[1] * particle.scale.y;
	TRANSFORM[2].xyz = particle.orientation[2] * particle.scale.z;
	TRANSFORM[3].xyz = particle.position;
	COLOR = particle.color;
	CUSTOM.x = particle.rotation;
	
	if (particle.age >= LIFETIME) { ACTIVE = false;  }
}

"

[sub_resource type="Curve" id="Curve_usbqb"]
_data = [Vector2(0, 1), 0.0, -2.3558042, 0, 0, Vector2(0.9932433, 0), -0.32447237, 0.0, 0, 0]
point_count = 2

[sub_resource type="CurveTexture" id="CurveTexture_vlqbi"]
curve = SubResource("Curve_usbqb")
metadata/uniform_name = "u_m2_alpha"

[sub_resource type="Gradient" id="Gradient_ddmel"]
offsets = PackedFloat32Array(0, 0.7888889)
colors = PackedColorArray(0.95, 0.773775, 0.7505, 1, 0, 0, 0, 1)

[sub_resource type="GradientTexture1D" id="GradientTexture1D_dvclh"]
gradient = SubResource("Gradient_ddmel")
metadata/uniform_name = "u_m2_rgb"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_fxwkx"]
shader = SubResource("Shader_3dayl")
shader_parameter/u_m2_rgb = SubResource("GradientTexture1D_dvclh")
shader_parameter/u_m2_alpha = SubResource("CurveTexture_vlqbi")

[sub_resource type="Shader" id="Shader_yxp5i"]
code = "shader_type spatial;
render_mode unshaded, blend_premul_alpha, cull_disabled;

uniform float opacity = 1.0;
uniform float emission = 1.0;

uniform sampler2D tex_1 : source_color, filter_linear_mipmap, repeat_disable;
uniform vec2 tiling_1 = vec2(1.0);
uniform vec2 offset_1 = vec2(0.0);
uniform vec2 speed_1 = vec2(0.0);

uniform sampler2D tex_2 : source_color, filter_linear_mipmap, repeat_enable;
uniform vec2 tiling_2 = vec2(1.0);
uniform vec2 offset_2 = vec2(0.0);
uniform vec2 speed_2 = vec2(0.0);

uniform sampler2D tex_3 : source_color, filter_linear_mipmap, repeat_enable;
uniform vec2 tiling_3 = vec2(1.0);
uniform vec2 offset_3 = vec2(0.0);
uniform vec2 speed_3 = vec2(0.0);

uniform sampler2D tex_4 : source_color, filter_linear_mipmap, repeat_disable;
uniform vec2 tiling_4 = vec2(1.0);
uniform vec2 offset_4 = vec2(0.0);
uniform vec2 speed_4 = vec2(0.0);

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}


varying flat uint instance_id;


void vertex()
{
	instance_id = uint(INSTANCE_ID);
}

void fragment()
{
	uint seed = instance_id;
	vec4 tex_sampled_1 = texture(tex_1, UV + speed_1 * tiling_1 * TIME);
	vec4 tex_sampled_2 = texture(tex_2, UV * tiling_2 + offset_2 + vec2(rand_range(seed, 0.0, 1.0), rand_range(seed, 0.0, 1.0)) + speed_2 * tiling_2 * TIME);
	vec4 tex_sampled_3 = texture(tex_3, UV * tiling_3 + offset_3 + vec2(rand_range(seed, 0.0, 1.0), rand_range(seed, 0.0, 1.0)) + speed_3 * tiling_3 * TIME);
	vec4 tex_sampled_4 = texture(tex_4, UV + speed_4 * tiling_4 * TIME);
	ALPHA = COLOR.a;
	ALPHA = ALPHA * tex_sampled_1.r;
	//ALPHA = ALPHA * tex_sampled_2.r;
	//ALPHA = ALPHA * tex_sampled_3.r;
	ALPHA = ALPHA * tex_sampled_4.r;
	ALPHA = clamp(ALPHA * opacity, 0.0, 1.0);
	ALBEDO = COLOR.rgb * emission;
	ALBEDO *= tex_sampled_1.rgb;
	ALBEDO *= tex_sampled_2.rgb;
	ALBEDO *= tex_sampled_3.rgb;
	ALBEDO *= tex_sampled_4.rgb;
	
}


"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_64pd1"]
render_priority = 0
shader = SubResource("Shader_yxp5i")
shader_parameter/opacity = 1.0
shader_parameter/emission = 1.0
shader_parameter/tex_1 = ExtResource("34_2h2m6")
shader_parameter/tiling_1 = Vector2(1, 1)
shader_parameter/offset_1 = Vector2(0, 0)
shader_parameter/speed_1 = Vector2(0, 0)
shader_parameter/tex_2 = ExtResource("13_u53yx")
shader_parameter/tiling_2 = Vector2(0.1, 0.1)
shader_parameter/offset_2 = Vector2(0, 0)
shader_parameter/speed_2 = Vector2(0.5, 0.3)
shader_parameter/tex_3 = ExtResource("13_u53yx")
shader_parameter/tiling_3 = Vector2(0.2, 0.2)
shader_parameter/offset_3 = Vector2(0, 0)
shader_parameter/speed_3 = Vector2(0.1, -0.5)
shader_parameter/tex_4 = ExtResource("36_j7fib")
shader_parameter/tiling_4 = Vector2(1, 1)
shader_parameter/offset_4 = Vector2(0, 0)
shader_parameter/speed_4 = Vector2(0, 0)

[sub_resource type="QuadMesh" id="QuadMesh_6gdg1"]
material = SubResource("ShaderMaterial_64pd1")

[sub_resource type="Shader" id="Shader_ek454"]
code = "shader_type spatial;
render_mode unshaded, cull_disabled;

uniform float opacity = 1.0;
uniform float emission = 1.0;

uniform sampler2D tex_1 : source_color, filter_linear_mipmap, repeat_enable;
uniform vec2 tiling_1 = vec2(1.0);
uniform vec2 offset_1 = vec2(0.0);
uniform vec2 speed_1 = vec2(0.0);

uniform sampler2D tex_2 : source_color, filter_linear_mipmap, repeat_enable;
uniform vec2 tiling_2 = vec2(1.0);
uniform vec2 offset_2 = vec2(0.0);
uniform vec2 speed_2 = vec2(0.0);

uniform sampler2D mask : source_color, filter_linear_mipmap, repeat_disable;

uniform float distortion = 0.5;

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}


varying flat uint instance_id;
varying float age;


void vertex()
{
	instance_id = uint(INSTANCE_ID);
	age = INSTANCE_CUSTOM.y;
}

float remap(float value, float input_min, float input_max, float output_min, float output_max) {
	return (clamp(value, input_min, input_max) - input_min) / (input_max - input_min) * (output_max - output_min) + output_min;
}

void fragment()
{
	uint seed = instance_id;
	float m = texture(mask, UV).r;
	vec4 tex_sampled_2 = texture(tex_2, UV * tiling_2 + vec2(rand_range(seed, 0.0, 1.0), rand_range(seed, 0.0, 1.0)) + speed_2 * tiling_2 * TIME);
	vec4 tex_sampled_1 = texture(tex_1, UV + speed_1 * tiling_1 * age - vec2(0.0, 1.0) - tex_sampled_2.r * distortion);
	float feather = 0.2;
	float edge = remap(min(age + .3, .75), 0.0, 1.0, -feather, 1.0);
	float timed_mask = 1.0 - smoothstep(edge, edge + feather, 1.0 - UV.y);
	ALPHA = COLOR.a;
	ALPHA = ALPHA * tex_sampled_1.r * timed_mask * m;
	ALPHA = clamp(ALPHA * opacity, 0.0, 1.0);
	ALBEDO = COLOR.rgb * emission;
	ALBEDO *= tex_sampled_1.rgb;
	//ALPHA = timed_mask;
	//ALBEDO = vec3(timed_mask);
	
}


"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_r3tca"]
render_priority = 0
shader = SubResource("Shader_ek454")
shader_parameter/opacity = 1.0
shader_parameter/emission = 10.0
shader_parameter/tex_1 = ExtResource("38_qmhta")
shader_parameter/tiling_1 = Vector2(1, 1)
shader_parameter/offset_1 = Vector2(0, 0)
shader_parameter/speed_1 = Vector2(0, 1)
shader_parameter/tex_2 = ExtResource("17_u53yx")
shader_parameter/tiling_2 = Vector2(0.1, 0.1)
shader_parameter/offset_2 = Vector2(0, 0)
shader_parameter/speed_2 = Vector2(0, 0.055)
shader_parameter/mask = ExtResource("37_y7i7n")
shader_parameter/distortion = 0.5

[sub_resource type="Shader" id="Shader_qrchc"]
code = "shader_type particles;
render_mode disable_velocity;

const float DEG_TO_RAD = PI / 180.0;
const uint AMOUNT = uint(24);

struct Particle {
	uint seed;
	float delta;
	float lifetime;
	uint index;
	uint number;
	float age;
	float normalized_age;
	float normalized_index;
	float normalized_number;
	vec3 initial_position;
	vec3 initial_scale;
	vec4 initial_color;
	vec3 initial_velocity;
	float radial_velocity;
	vec3 orbital_axis;
	float initial_orbital_speed;
	vec3 spawn_direction;
	vec3 position;
	vec3 scale;
	vec4 color;
	vec3 velocity;
	vec3 orbital_velocity;
	float orbital_speed;
	vec3 rotation_axis;
	float initial_rotation;
	float rotation;
	mat3 orientation;
	vec3 force;
	float animation_offset;
	float animation_speed;
	mat4 emission_transform;
	mat4 previous_transform;
	bool just_spawned;
	bool collided;
	vec3 collision_normal;
	float collision_depth;
};

float remap(float value, float input_min, float input_max, float output_min, float output_max) {
	float v = clamp(value, input_min, input_max);
	return (v - input_min) / (input_max - input_min) * (output_max - output_min) + output_min;
}

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}

uint hash(uint x) {
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = (x >> uint(16)) ^ x;
	return x;
}

vec3 normalize_or_zero(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 0.0, 0.0);
}

vec3 normalize_or_up(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 1.0, 0.0);
}

vec4 quat(vec3 axis, float angle) {
  float s = sin(angle / 2.0);
  return vec4(axis.x * s, axis.y * s, axis.z * s, cos(angle / 2.0));
}

vec4 quat_rotation_to(vec3 from, vec3 to) {
	vec3 xunit = vec3(1.0, 0.0, 0.0);
	vec3 yunit = vec3(0.0, 1.0, 0.0);
	float dot = dot(from, to);
	if (dot < -0.999999) {
	    vec3 axis = cross(xunit, from);
	    if (length(axis) < 0.000001) {
	    	axis = cross(yunit, from);
	    }
	    axis = normalize(axis);
	    return quat(axis, PI);
	} else if (dot > 0.999999) {
		return vec4(0.0, 0.0, 0.0, 1.0);
	} else {
		vec3 axis = cross(from, to);
		return normalize(vec4(axis, 1.0 + dot));
	}
}

vec3 rotated(vec3 v, vec3 axis, float angle) {
  vec4 q = quat(axis, angle);
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

vec3 rotated(vec3 v, vec4 q) {
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

mat3 rotated(mat3 m, vec4 q) {
  return mat3(rotated(m[0], q), rotated(m[1], q), rotated(m[2], q));
}

float normalized_by_amount(uint n) {
	return float(n) / float(max(AMOUNT, 1u));
}
void update_orbital_velocity(inout Particle particle, mat4 emission_transform, mat4 transform) {
	if (abs(particle.orbital_speed) < 0.01 || particle.delta < 0.001) { return; }
	vec3 local_pos = (inverse(emission_transform) * transform[3]).xyz;
	vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
	particle.position = (emission_transform * vec4(next, 0.0)).xyz;
	particle.orbital_velocity = (next - local_pos) / particle.delta;
}

void apply_radial_velocity(inout Particle particle) {
	vec3 d = particle.position;
	float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
	particle.position += normalize_or_zero(d) * mag * sign(particle.radial_velocity);
}

void m0_initialize_rotation(inout Particle particle) {
	float a = -90.0;
	particle.rotation_axis = vec3(1.000000, 0.000000, 0.000000);
	particle.initial_rotation = a * DEG_TO_RAD;
}

uniform vec4 u_m1_rgb : source_color;

void m1_initialize_color(inout Particle particle) {
	vec4 c = u_m1_rgb;
	float a = 1.0;
	particle.initial_color = vec4(c.rgb, a);
}

void m2_initialize_size(inout Particle particle) {
	vec3 s = vec3(1.500000, 1.500000, 1.500000);
	particle.initial_scale = s;
}


void initialize(inout Particle particle) {
	particle.initial_position = vec3(0.0);
	particle.initial_color = vec4(1.0);
	particle.initial_scale = vec3(1.0);
	particle.initial_velocity = vec3(0.0);
	particle.initial_orbital_speed = 0.0;
	particle.spawn_direction = vec3(0.0);
	particle.animation_offset = 0.0;
	particle.animation_speed = 1.0;
	particle.rotation_axis = vec3(1.0, 0.0, 0.0);
	particle.initial_rotation = 0.0;
	particle.radial_velocity = 0.0;
	particle.orbital_axis = vec3(0.0, 1.0, 0.0);
	particle.orbital_velocity = vec3(0.0);
	m0_initialize_rotation(particle);
	m1_initialize_color(particle);
	m2_initialize_size(particle);
	particle.scale = particle.initial_scale;
	particle.color = particle.initial_color;
	particle.orbital_speed = particle.initial_orbital_speed;
}

void start() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = vec3(0.0);
	initialize(particle);
	if (rand(particle.seed) > AMOUNT_RATIO) {
		ACTIVE = false;
	}
	if (RESTART_CUSTOM) {
		CUSTOM = vec4(0.0);
		CUSTOM.x = particle.initial_rotation;
	}
	if (RESTART_COLOR) {
		COLOR = particle.initial_color;
	}
	if (RESTART_ROT_SCALE) {
		TRANSFORM[0].xyz = rotated(vec3(1.0, 0.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.x;
		TRANSFORM[1].xyz = rotated(vec3(0.0, 1.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.y;
		TRANSFORM[2].xyz = rotated(vec3(0.0, 0.0, 1.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.z;
	}
	if (RESTART_POSITION) {
		TRANSFORM[3].xyz = particle.initial_position;
		TRANSFORM = EMISSION_TRANSFORM * TRANSFORM;
	}
	if (RESTART_VELOCITY) {
		VELOCITY = (EMISSION_TRANSFORM * vec4(particle.initial_velocity, 0.0)).xyz;
		VELOCITY += EMITTER_VELOCITY * 0.0;//inherit_emitter_velocity_ratio;
	}
}

void process() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.delta = DELTA;
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = TRANSFORM[3].xyz;
	particle.velocity = VELOCITY;
	particle.orientation = mat3(
		normalize(TRANSFORM[0].xyz),
		normalize(TRANSFORM[1].xyz),
		normalize(TRANSFORM[2].xyz));
	particle.force = vec3(0.0);
	particle.emission_transform = EMISSION_TRANSFORM;
	particle.previous_transform = TRANSFORM;
	particle.rotation = CUSTOM.x;
	particle.just_spawned = CUSTOM.y == 0.0;
	particle.collided = COLLIDED;
	particle.collision_normal = COLLISION_NORMAL;
	particle.collision_depth = COLLISION_DEPTH;
	initialize(particle);
	CUSTOM.y += DELTA / particle.lifetime;
	CUSTOM.y = mix(CUSTOM.y, 1.0, INTERPOLATE_TO_END);
	CUSTOM.y = clamp(CUSTOM.y, 0.0, 1.0);
	particle.age = CUSTOM.y * particle.lifetime;
	particle.normalized_age = CUSTOM.y;
	if (abs(particle.orbital_speed) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
		particle.orbital_velocity = (next - local_pos) / particle.delta;
	}
	if (abs(particle.radial_velocity) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 d = local_pos;
		float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
		vec3 next = local_pos + normalize_or_zero(d) * mag * sign(particle.radial_velocity);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
	}
	particle.position += particle.velocity * particle.delta;
	VELOCITY = particle.velocity;
	TRANSFORM[0].xyz = particle.orientation[0] * particle.scale.x;
	TRANSFORM[1].xyz = particle.orientation[1] * particle.scale.y;
	TRANSFORM[2].xyz = particle.orientation[2] * particle.scale.z;
	TRANSFORM[3].xyz = particle.position;
	COLOR = particle.color;
	CUSTOM.x = particle.rotation;
	
	if (particle.age >= LIFETIME) { ACTIVE = false;  }
}

"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_35adv"]
shader = SubResource("Shader_qrchc")
shader_parameter/u_m1_rgb = Color(0.94, 0.74855334, 0.49819997, 1)

[sub_resource type="Shader" id="Shader_wp8sh"]
code = "shader_type particles;
render_mode disable_velocity;

const float DEG_TO_RAD = PI / 180.0;
const uint AMOUNT = uint(24);

struct Particle {
	uint seed;
	float delta;
	float lifetime;
	uint index;
	uint number;
	float age;
	float normalized_age;
	float normalized_index;
	float normalized_number;
	vec3 initial_position;
	vec3 initial_scale;
	vec4 initial_color;
	vec3 initial_velocity;
	float radial_velocity;
	vec3 orbital_axis;
	float initial_orbital_speed;
	vec3 spawn_direction;
	vec3 position;
	vec3 scale;
	vec4 color;
	vec3 velocity;
	vec3 orbital_velocity;
	float orbital_speed;
	vec3 rotation_axis;
	float initial_rotation;
	float rotation;
	mat3 orientation;
	vec3 force;
	float animation_offset;
	float animation_speed;
	mat4 emission_transform;
	mat4 previous_transform;
	bool just_spawned;
	bool collided;
	vec3 collision_normal;
	float collision_depth;
};

float remap(float value, float input_min, float input_max, float output_min, float output_max) {
	float v = clamp(value, input_min, input_max);
	return (v - input_min) / (input_max - input_min) * (output_max - output_min) + output_min;
}

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}

uint hash(uint x) {
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = (x >> uint(16)) ^ x;
	return x;
}

vec3 normalize_or_zero(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 0.0, 0.0);
}

vec3 normalize_or_up(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 1.0, 0.0);
}

vec4 quat(vec3 axis, float angle) {
  float s = sin(angle / 2.0);
  return vec4(axis.x * s, axis.y * s, axis.z * s, cos(angle / 2.0));
}

vec4 quat_rotation_to(vec3 from, vec3 to) {
	vec3 xunit = vec3(1.0, 0.0, 0.0);
	vec3 yunit = vec3(0.0, 1.0, 0.0);
	float dot = dot(from, to);
	if (dot < -0.999999) {
	    vec3 axis = cross(xunit, from);
	    if (length(axis) < 0.000001) {
	    	axis = cross(yunit, from);
	    }
	    axis = normalize(axis);
	    return quat(axis, PI);
	} else if (dot > 0.999999) {
		return vec4(0.0, 0.0, 0.0, 1.0);
	} else {
		vec3 axis = cross(from, to);
		return normalize(vec4(axis, 1.0 + dot));
	}
}

vec3 rotated(vec3 v, vec3 axis, float angle) {
  vec4 q = quat(axis, angle);
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

vec3 rotated(vec3 v, vec4 q) {
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

mat3 rotated(mat3 m, vec4 q) {
  return mat3(rotated(m[0], q), rotated(m[1], q), rotated(m[2], q));
}

float normalized_by_amount(uint n) {
	return float(n) / float(max(AMOUNT, 1u));
}
void update_orbital_velocity(inout Particle particle, mat4 emission_transform, mat4 transform) {
	if (abs(particle.orbital_speed) < 0.01 || particle.delta < 0.001) { return; }
	vec3 local_pos = (inverse(emission_transform) * transform[3]).xyz;
	vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
	particle.position = (emission_transform * vec4(next, 0.0)).xyz;
	particle.orbital_velocity = (next - local_pos) / particle.delta;
}

void apply_radial_velocity(inout Particle particle) {
	vec3 d = particle.position;
	float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
	particle.position += normalize_or_zero(d) * mag * sign(particle.radial_velocity);
}

void m0_initialize_rotation(inout Particle particle) {
	float a = rand_range(particle.seed, 0.000000, 360.000000);
	particle.rotation_axis = vec3(0.000000, 1.000000, 0.000000);
	particle.initial_rotation = a * DEG_TO_RAD;
}

uniform sampler2D u_m1_rgb : source_color, repeat_disable;

uniform sampler2D u_m1_alpha : repeat_disable;

void m1_scale_color(inout Particle particle) {
	vec4 c = vec4(texture(u_m1_rgb, vec2(particle.normalized_age)));
	float a = texture(u_m1_alpha, vec2(particle.normalized_age)).r;
	particle.color = particle.initial_color * vec4(c.rgb, a);
}


void initialize(inout Particle particle) {
	particle.initial_position = vec3(0.0);
	particle.initial_color = vec4(1.0);
	particle.initial_scale = vec3(1.0);
	particle.initial_velocity = vec3(0.0);
	particle.initial_orbital_speed = 0.0;
	particle.spawn_direction = vec3(0.0);
	particle.animation_offset = 0.0;
	particle.animation_speed = 1.0;
	particle.rotation_axis = vec3(1.0, 0.0, 0.0);
	particle.initial_rotation = 0.0;
	particle.radial_velocity = 0.0;
	particle.orbital_axis = vec3(0.0, 1.0, 0.0);
	particle.orbital_velocity = vec3(0.0);
	m0_initialize_rotation(particle);
	particle.scale = particle.initial_scale;
	particle.color = particle.initial_color;
	particle.orbital_speed = particle.initial_orbital_speed;
}

void start() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = vec3(0.0);
	initialize(particle);
	if (rand(particle.seed) > AMOUNT_RATIO) {
		ACTIVE = false;
	}
	if (RESTART_CUSTOM) {
		CUSTOM = vec4(0.0);
		CUSTOM.x = particle.initial_rotation;
	}
	if (RESTART_COLOR) {
		COLOR = particle.initial_color;
	}
	if (RESTART_ROT_SCALE) {
		TRANSFORM[0].xyz = rotated(vec3(1.0, 0.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.x;
		TRANSFORM[1].xyz = rotated(vec3(0.0, 1.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.y;
		TRANSFORM[2].xyz = rotated(vec3(0.0, 0.0, 1.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.z;
	}
	if (RESTART_POSITION) {
		TRANSFORM[3].xyz = particle.initial_position;
		TRANSFORM = EMISSION_TRANSFORM * TRANSFORM;
	}
	if (RESTART_VELOCITY) {
		VELOCITY = (EMISSION_TRANSFORM * vec4(particle.initial_velocity, 0.0)).xyz;
		VELOCITY += EMITTER_VELOCITY * 0.0;//inherit_emitter_velocity_ratio;
	}
}

void process() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.delta = DELTA;
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = TRANSFORM[3].xyz;
	particle.velocity = VELOCITY;
	particle.orientation = mat3(
		normalize(TRANSFORM[0].xyz),
		normalize(TRANSFORM[1].xyz),
		normalize(TRANSFORM[2].xyz));
	particle.force = vec3(0.0);
	particle.emission_transform = EMISSION_TRANSFORM;
	particle.previous_transform = TRANSFORM;
	particle.rotation = CUSTOM.x;
	particle.just_spawned = CUSTOM.y == 0.0;
	particle.collided = COLLIDED;
	particle.collision_normal = COLLISION_NORMAL;
	particle.collision_depth = COLLISION_DEPTH;
	initialize(particle);
	CUSTOM.y += DELTA / particle.lifetime;
	CUSTOM.y = mix(CUSTOM.y, 1.0, INTERPOLATE_TO_END);
	CUSTOM.y = clamp(CUSTOM.y, 0.0, 1.0);
	particle.age = CUSTOM.y * particle.lifetime;
	particle.normalized_age = CUSTOM.y;
	m1_scale_color(particle);
	if (abs(particle.orbital_speed) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
		particle.orbital_velocity = (next - local_pos) / particle.delta;
	}
	if (abs(particle.radial_velocity) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 d = local_pos;
		float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
		vec3 next = local_pos + normalize_or_zero(d) * mag * sign(particle.radial_velocity);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
	}
	particle.position += particle.velocity * particle.delta;
	VELOCITY = particle.velocity;
	TRANSFORM[0].xyz = particle.orientation[0] * particle.scale.x;
	TRANSFORM[1].xyz = particle.orientation[1] * particle.scale.y;
	TRANSFORM[2].xyz = particle.orientation[2] * particle.scale.z;
	TRANSFORM[3].xyz = particle.position;
	COLOR = particle.color;
	CUSTOM.x = particle.rotation;
	
	if (particle.age >= LIFETIME) { ACTIVE = false;  }
}

"

[sub_resource type="Curve" id="Curve_r22vs"]
_data = [Vector2(0, 1), 0.0, -1.0, 0, 1, Vector2(1, 0), -1.0, 0.0, 1, 0]
point_count = 2

[sub_resource type="CurveTexture" id="CurveTexture_6be7q"]
curve = SubResource("Curve_r22vs")
metadata/uniform_name = "u_m1_alpha"

[sub_resource type="Gradient" id="Gradient_yvumi"]
colors = PackedColorArray(0.97, 0.5160401, 0.21340004, 1, 0.16, 0.049706668, 0.0096, 1)

[sub_resource type="GradientTexture1D" id="GradientTexture1D_w4r8r"]
gradient = SubResource("Gradient_yvumi")
metadata/uniform_name = "u_m1_rgb"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_jcfxp"]
shader = SubResource("Shader_wp8sh")
shader_parameter/u_m1_rgb = SubResource("GradientTexture1D_w4r8r")
shader_parameter/u_m1_alpha = SubResource("CurveTexture_6be7q")

[sub_resource type="Shader" id="Shader_iept0"]
code = "shader_type spatial;
render_mode unshaded, cull_disabled;

uniform float opacity = 1.0;
uniform float emission = 1.0;

uniform sampler2D tex_1 : source_color, filter_linear_mipmap, repeat_disable;
uniform vec2 tiling_1 = vec2(1.0);
uniform vec2 offset_1 = vec2(0.0);
uniform vec2 speed_1 = vec2(0.0);

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}


varying flat uint instance_id;
varying float age;

void vertex()
{
	instance_id = uint(INSTANCE_ID);
	age = INSTANCE_CUSTOM.y;
}

float remap(float value, float input_min, float input_max, float output_min, float output_max) {
	return (clamp(value, input_min, input_max) - input_min) / (input_max - input_min) * (output_max - output_min) + output_min;
}

void fragment()
{
	uint seed = instance_id;
	vec4 tex_sampled_1 = texture(tex_1, UV + speed_1 * tiling_1 * TIME);
	ALPHA = COLOR.a;
	ALPHA = ALPHA * tex_sampled_1.r;
	ALPHA = clamp(ALPHA * opacity, 0.0, 1.0);
	float feather = 0.05;
	float age_ = remap(age, 0.0, 1.0, -feather, 1.0);
	float erosion = smoothstep(age_, age_ + feather, tex_sampled_1.r);
	ALPHA *= erosion;
	ALBEDO = COLOR.rgb * emission;
	ALBEDO *= tex_sampled_1.rgb;
	
}


"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_d6i82"]
render_priority = 0
shader = SubResource("Shader_iept0")
shader_parameter/opacity = 1.0
shader_parameter/emission = 2.0
shader_parameter/tex_1 = ExtResource("41_jluke")
shader_parameter/tiling_1 = Vector2(1, 1)
shader_parameter/offset_1 = Vector2(0, 0)
shader_parameter/speed_1 = Vector2(0, 0)

[sub_resource type="PlaneMesh" id="PlaneMesh_3i4eu"]
material = SubResource("ShaderMaterial_d6i82")

[sub_resource type="Shader" id="Shader_j5ofw"]
code = "shader_type spatial;
render_mode unshaded, cull_disabled;

uniform float opacity = 1.0;
uniform float emission = 1.0;

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}


varying flat uint instance_id;


void vertex()
{
	instance_id = uint(INSTANCE_ID);
}

void fragment()
{
	uint seed = instance_id;
	ALBEDO = COLOR.rgb * emission;
	
}


"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_xjq6u"]
render_priority = 0
shader = SubResource("Shader_j5ofw")
shader_parameter/opacity = 1.0
shader_parameter/emission = 2.0

[sub_resource type="Shader" id="Shader_3jlxe"]
code = "shader_type particles;
render_mode disable_velocity;

const float DEG_TO_RAD = PI / 180.0;
const uint AMOUNT = uint(96);

struct Particle {
	uint seed;
	float delta;
	float lifetime;
	uint index;
	uint number;
	float age;
	float normalized_age;
	float normalized_index;
	float normalized_number;
	vec3 initial_position;
	vec3 initial_scale;
	vec4 initial_color;
	vec3 initial_velocity;
	float radial_velocity;
	vec3 orbital_axis;
	float initial_orbital_speed;
	vec3 spawn_direction;
	vec3 position;
	vec3 scale;
	vec4 color;
	vec3 velocity;
	vec3 orbital_velocity;
	float orbital_speed;
	vec3 rotation_axis;
	float initial_rotation;
	float rotation;
	mat3 orientation;
	vec3 force;
	float animation_offset;
	float animation_speed;
	mat4 emission_transform;
	mat4 previous_transform;
	bool just_spawned;
	bool collided;
	vec3 collision_normal;
	float collision_depth;
};

float remap(float value, float input_min, float input_max, float output_min, float output_max) {
	float v = clamp(value, input_min, input_max);
	return (v - input_min) / (input_max - input_min) * (output_max - output_min) + output_min;
}

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}

uint hash(uint x) {
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = (x >> uint(16)) ^ x;
	return x;
}

vec3 normalize_or_zero(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 0.0, 0.0);
}

vec3 normalize_or_up(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 1.0, 0.0);
}

vec4 quat(vec3 axis, float angle) {
  float s = sin(angle / 2.0);
  return vec4(axis.x * s, axis.y * s, axis.z * s, cos(angle / 2.0));
}

vec4 quat_rotation_to(vec3 from, vec3 to) {
	vec3 xunit = vec3(1.0, 0.0, 0.0);
	vec3 yunit = vec3(0.0, 1.0, 0.0);
	float dot = dot(from, to);
	if (dot < -0.999999) {
	    vec3 axis = cross(xunit, from);
	    if (length(axis) < 0.000001) {
	    	axis = cross(yunit, from);
	    }
	    axis = normalize(axis);
	    return quat(axis, PI);
	} else if (dot > 0.999999) {
		return vec4(0.0, 0.0, 0.0, 1.0);
	} else {
		vec3 axis = cross(from, to);
		return normalize(vec4(axis, 1.0 + dot));
	}
}

vec3 rotated(vec3 v, vec3 axis, float angle) {
  vec4 q = quat(axis, angle);
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

vec3 rotated(vec3 v, vec4 q) {
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

mat3 rotated(mat3 m, vec4 q) {
  return mat3(rotated(m[0], q), rotated(m[1], q), rotated(m[2], q));
}

float normalized_by_amount(uint n) {
	return float(n) / float(max(AMOUNT, 1u));
}
void update_orbital_velocity(inout Particle particle, mat4 emission_transform, mat4 transform) {
	if (abs(particle.orbital_speed) < 0.01 || particle.delta < 0.001) { return; }
	vec3 local_pos = (inverse(emission_transform) * transform[3]).xyz;
	vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
	particle.position = (emission_transform * vec4(next, 0.0)).xyz;
	particle.orbital_velocity = (next - local_pos) / particle.delta;
}

void apply_radial_velocity(inout Particle particle) {
	vec3 d = particle.position;
	float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
	particle.position += normalize_or_zero(d) * mag * sign(particle.radial_velocity);
}

void m0_initialize_size(inout Particle particle) {
	float s_c = rand_range(particle.seed, 0.200000, 0.300000);
	vec3 s = vec3(s_c, s_c, s_c);
	particle.initial_scale = s;
}

void m1_shape_location_sphere(inout Particle particle) {
	float r = rand_range(particle.seed, 0.100000, 1.000000);
	float u = rand_range(particle.seed, 0.000000, 1.000000);
	float v = rand_range(particle.seed, 0.300000, 0.450000);
	vec3 n = vec3(1.0, 0.0, 0.0);
	n = rotated(n, vec3(0.0, 1.0, 0.0), u * 360.0 * DEG_TO_RAD);
	float h = 1.0 - 2.0 * v;
	n = normalize(n * (1.0 - abs(h)) + vec3(0.0, h, 0.0));
	particle.initial_position = n * r * 0.300000 + vec3(0.000000, 0.100000, 0.000000);
}

void m2_initialize_velocity_from_point(inout Particle particle) {
	float v = rand_range(particle.seed, 5.000000, 6.000000);
	vec3 front = vec3(0.0, 0.0, 1.0);
	vec3 to = -normalize(particle.initial_position);
	vec4 q = quat_rotation_to(front, to);
	vec3 right = rotated(vec3(1.0, 0.0, 0.0), q);
	particle.initial_velocity = normalize(particle.initial_position) * v;
}

void m3_set_rotation(inout Particle particle) {
	float x = 0.0;
	float y = 0.0;
	float z = 0.0;
	float ox = rand_range(particle.seed, 0.000000, 360.000000);
	float oy = rand_range(particle.seed, 0.000000, 360.000000);
	float oz = rand_range(particle.seed, 0.000000, 360.000000);
	mat3 mx = rotated(mat3(1.0), quat(vec3(1.0, 0.0, 0.0), ox * DEG_TO_RAD + x * DEG_TO_RAD));
	mat3 my = rotated(mat3(1.0), quat(vec3(0.0, 1.0, 0.0), oy * DEG_TO_RAD + y * DEG_TO_RAD));
	mat3 mz = rotated(mat3(1.0), quat(vec3(0.0, 0.0, 1.0), oz * DEG_TO_RAD + z * DEG_TO_RAD));
	particle.orientation = mx * my * mz;
}

void m4_drag(inout Particle particle) {
	float limit = 1.0;
	float drag = 3.0;
	float speed = length(particle.velocity);
	float excess = max(0.0, speed - limit);
	vec3 dir = normalize_or_up(particle.velocity);
	particle.velocity = dir * (speed - particle.delta * excess * max(drag, 0.0));
}

void m5_add_force(inout Particle particle) {
	vec3 f = vec3(0.000000, -12.000000, 0.000000);
	particle.force += f;
}

void m6_solve_forces_and_velocity(inout Particle particle) {
	particle.velocity += particle.force * particle.delta;
	if (abs(particle.orbital_speed) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
		particle.orbital_velocity = (next - local_pos) / particle.delta;
	}
	if (abs(particle.radial_velocity) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 d = local_pos;
		float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
		vec3 next = local_pos + normalize_or_zero(d) * mag * sign(particle.radial_velocity);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
	}
	particle.position += particle.velocity * particle.delta;
}

void m7_collision(inout Particle particle) {
	if (particle.collided) {
		float collision_response = dot(particle.collision_normal, particle.velocity);
		float slide_to_bounce_trigger = step(2.0 / clamp(0.500000 + 1.0, 1.0, 2.0), abs(collision_response));
		particle.position += particle.collision_normal * particle.collision_depth;
		// Remove all components of velocity that are not tangential to COLLISION_NORMAL.
		particle.velocity -= particle.collision_normal * collision_response;
		// Apply friction only to velocity across the surface (effectively decouples friction and bounce behavior).
		particle.velocity = mix(particle.velocity, vec3(0.0), clamp(0.200000, 0.0, 1.0));
		// Add bounce velocity to velocity.
		particle.velocity -= particle.collision_normal * collision_response * (0.500000 * slide_to_bounce_trigger);
	}
}

uniform sampler2D u_m8_rgb : source_color, repeat_disable;

void m8_scale_color(inout Particle particle) {
	vec4 c = vec4(texture(u_m8_rgb, vec2(particle.normalized_age)));
	float a = 1.0;
	particle.color = particle.initial_color * vec4(c.rgb, a);
}

uniform sampler2D u_m9_scale : repeat_disable;

void m9_scale_size(inout Particle particle) {
	vec3 s = vec3(texture(u_m9_scale, vec2(particle.normalized_age)).r);
	particle.scale *= s;
}


void initialize(inout Particle particle) {
	particle.initial_position = vec3(0.0);
	particle.initial_color = vec4(1.0);
	particle.initial_scale = vec3(1.0);
	particle.initial_velocity = vec3(0.0);
	particle.initial_orbital_speed = 0.0;
	particle.spawn_direction = vec3(0.0);
	particle.animation_offset = 0.0;
	particle.animation_speed = 1.0;
	particle.rotation_axis = vec3(1.0, 0.0, 0.0);
	particle.initial_rotation = 0.0;
	particle.radial_velocity = 0.0;
	particle.orbital_axis = vec3(0.0, 1.0, 0.0);
	particle.orbital_velocity = vec3(0.0);
	m0_initialize_size(particle);
	m1_shape_location_sphere(particle);
	m2_initialize_velocity_from_point(particle);
	particle.scale = particle.initial_scale;
	particle.color = particle.initial_color;
	particle.orbital_speed = particle.initial_orbital_speed;
}

void start() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = vec3(0.0);
	initialize(particle);
	if (rand(particle.seed) > AMOUNT_RATIO) {
		ACTIVE = false;
	}
	if (RESTART_CUSTOM) {
		CUSTOM = vec4(0.0);
		CUSTOM.x = particle.initial_rotation;
	}
	if (RESTART_COLOR) {
		COLOR = particle.initial_color;
	}
	if (RESTART_ROT_SCALE) {
		TRANSFORM[0].xyz = rotated(vec3(1.0, 0.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.x;
		TRANSFORM[1].xyz = rotated(vec3(0.0, 1.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.y;
		TRANSFORM[2].xyz = rotated(vec3(0.0, 0.0, 1.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.z;
	}
	if (RESTART_POSITION) {
		TRANSFORM[3].xyz = particle.initial_position;
		TRANSFORM = EMISSION_TRANSFORM * TRANSFORM;
	}
	if (RESTART_VELOCITY) {
		VELOCITY = (EMISSION_TRANSFORM * vec4(particle.initial_velocity, 0.0)).xyz;
		VELOCITY += EMITTER_VELOCITY * 0.0;//inherit_emitter_velocity_ratio;
	}
}

void process() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.delta = DELTA;
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = TRANSFORM[3].xyz;
	particle.velocity = VELOCITY;
	particle.orientation = mat3(
		normalize(TRANSFORM[0].xyz),
		normalize(TRANSFORM[1].xyz),
		normalize(TRANSFORM[2].xyz));
	particle.force = vec3(0.0);
	particle.emission_transform = EMISSION_TRANSFORM;
	particle.previous_transform = TRANSFORM;
	particle.rotation = CUSTOM.x;
	particle.just_spawned = CUSTOM.y == 0.0;
	particle.collided = COLLIDED;
	particle.collision_normal = COLLISION_NORMAL;
	particle.collision_depth = COLLISION_DEPTH;
	initialize(particle);
	CUSTOM.y += DELTA / particle.lifetime;
	CUSTOM.y = mix(CUSTOM.y, 1.0, INTERPOLATE_TO_END);
	CUSTOM.y = clamp(CUSTOM.y, 0.0, 1.0);
	particle.age = CUSTOM.y * particle.lifetime;
	particle.normalized_age = CUSTOM.y;
	m3_set_rotation(particle);
	m4_drag(particle);
	m5_add_force(particle);
	m6_solve_forces_and_velocity(particle);
	m7_collision(particle);
	m8_scale_color(particle);
	m9_scale_size(particle);
	VELOCITY = particle.velocity;
	TRANSFORM[0].xyz = particle.orientation[0] * particle.scale.x;
	TRANSFORM[1].xyz = particle.orientation[1] * particle.scale.y;
	TRANSFORM[2].xyz = particle.orientation[2] * particle.scale.z;
	TRANSFORM[3].xyz = particle.position;
	COLOR = particle.color;
	CUSTOM.x = particle.rotation;
	
	if (particle.age >= LIFETIME) { ACTIVE = false;  }
}

"

[sub_resource type="Gradient" id="Gradient_3filq"]
offsets = PackedFloat32Array(0.0037037036, 0.13703704)
colors = PackedColorArray(0.86, 0.4472, 0.17199999, 1, 0.11, 0.041470002, 0.012100002, 1)

[sub_resource type="GradientTexture1D" id="GradientTexture1D_2h2m6"]
gradient = SubResource("Gradient_3filq")
metadata/uniform_name = "u_m8_rgb"

[sub_resource type="Curve" id="Curve_0sqcb"]
_data = [Vector2(0, 0), 0.0, 11.128843, 0, 0, Vector2(0.13175675, 1), 0.0, 0.0, 0, 0, Vector2(1, 0), -1.9925797, 0.0, 0, 0]
point_count = 3

[sub_resource type="CurveTexture" id="CurveTexture_b86d1"]
curve = SubResource("Curve_0sqcb")
metadata/uniform_name = "u_m9_scale"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_j7fib"]
shader = SubResource("Shader_3jlxe")
shader_parameter/u_m8_rgb = SubResource("GradientTexture1D_2h2m6")
shader_parameter/u_m9_scale = SubResource("CurveTexture_b86d1")

[node name="Node3D" type="Node3D" unique_id=1487102816]
script = SubResource("GDScript_u53yx")

[node name="Collider" type="GPUParticlesCollisionBox3D" parent="." unique_id=628976746]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, -1, 0)
size = Vector3(40, 2, 40)

[node name="MainEmitters" type="Node3D" parent="." unique_id=1075348613]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 10, 0)

[node name="Core" type="GPUParticles3D" parent="MainEmitters" unique_id=137444905]
material_override = SubResource("ShaderMaterial_6vuv3")
cast_shadow = 0
emitting = false
amount = 24
sub_emitter = NodePath("../../SubEmitters/EmbersProjectile")
one_shot = true
use_fixed_seed = true
seed = 56465465
fixed_fps = 60
interpolate = false
fract_delta = false
visibility_aabb = AABB(-20, -20, -20, 40, 40, 40)
local_coords = true
process_material = SubResource("ShaderMaterial_21ym8")
draw_pass_1 = ExtResource("13_eu8jq")

[node name="Trail" type="GPUParticles3D" parent="MainEmitters" unique_id=122006036]
cast_shadow = 0
emitting = false
amount = 24
one_shot = true
use_fixed_seed = true
seed = 56465465
fixed_fps = 60
interpolate = false
fract_delta = false
visibility_aabb = AABB(-20, -20, -20, 40, 40, 40)
local_coords = true
trail_enabled = true
process_material = SubResource("ShaderMaterial_xjhfn")
draw_pass_1 = SubResource("RibbonTrailMesh_gk6rx")

[node name="Glow" type="GPUParticles3D" parent="MainEmitters" unique_id=1654340623]
cast_shadow = 0
emitting = false
amount = 24
sub_emitter = NodePath("../../SubEmitters/Shockwave")
one_shot = true
use_fixed_seed = true
seed = 56465465
fixed_fps = 60
interpolate = false
fract_delta = false
visibility_aabb = AABB(-20, -20, -20, 40, 40, 40)
local_coords = true
process_material = SubResource("ShaderMaterial_xjchu")
draw_pass_1 = SubResource("QuadMesh_83lj5")

[node name="SubSparks" type="GPUParticles3D" parent="MainEmitters" unique_id=884122849]
cast_shadow = 0
emitting = false
amount = 24
sub_emitter = NodePath("../../SubEmitters/Sparks")
one_shot = true
use_fixed_seed = true
seed = 56465465
fixed_fps = 60
interpolate = false
fract_delta = false
visibility_aabb = AABB(-20, -20, -20, 40, 40, 40)
local_coords = true
process_material = SubResource("ShaderMaterial_ry011")

[node name="SubFlare" type="GPUParticles3D" parent="MainEmitters" unique_id=1191518644]
cast_shadow = 0
emitting = false
amount = 24
sub_emitter = NodePath("../../SubEmitters/Flare")
one_shot = true
use_fixed_seed = true
seed = 56465465
fixed_fps = 60
interpolate = false
fract_delta = false
visibility_aabb = AABB(-20, -20, -20, 40, 40, 40)
local_coords = true
process_material = SubResource("ShaderMaterial_t8qsk")

[node name="SubGlow" type="GPUParticles3D" parent="MainEmitters" unique_id=1927394795]
cast_shadow = 0
emitting = false
amount = 24
sub_emitter = NodePath("../../SubEmitters/Glow")
one_shot = true
use_fixed_seed = true
seed = 56465465
fixed_fps = 60
interpolate = false
fract_delta = false
visibility_aabb = AABB(-20, -20, -20, 40, 40, 40)
local_coords = true
process_material = SubResource("ShaderMaterial_ubj0s")

[node name="SubEmbers" type="GPUParticles3D" parent="MainEmitters" unique_id=197335002]
cast_shadow = 0
emitting = false
amount = 24
sub_emitter = NodePath("../../SubEmitters/EmbersImpact")
one_shot = true
use_fixed_seed = true
seed = 56465465
fixed_fps = 60
interpolate = false
fract_delta = false
visibility_aabb = AABB(-20, -20, -20, 40, 40, 40)
local_coords = true
process_material = SubResource("ShaderMaterial_rxgj7")

[node name="SubSmoke" type="GPUParticles3D" parent="MainEmitters" unique_id=1558857700]
cast_shadow = 0
emitting = false
amount = 24
sub_emitter = NodePath("../../SubEmitters/Smoke")
one_shot = true
use_fixed_seed = true
seed = 56465465
fixed_fps = 60
interpolate = false
fract_delta = false
visibility_aabb = AABB(-20, -20, -20, 40, 40, 40)
local_coords = true
process_material = SubResource("ShaderMaterial_kuk0d")

[node name="SubCrater" type="GPUParticles3D" parent="MainEmitters" unique_id=1681809526]
cast_shadow = 0
emitting = false
amount = 24
sub_emitter = NodePath("../../SubEmitters/Crater")
one_shot = true
use_fixed_seed = true
seed = 56465465
fixed_fps = 60
interpolate = false
fract_delta = false
visibility_aabb = AABB(-20, -20, -20, 40, 40, 40)
local_coords = true
process_material = SubResource("ShaderMaterial_jkyy0")

[node name="SubDebris" type="GPUParticles3D" parent="MainEmitters" unique_id=852390567]
cast_shadow = 0
emitting = false
amount = 24
sub_emitter = NodePath("../../SubEmitters/Debris")
one_shot = true
use_fixed_seed = true
seed = 56465465
fixed_fps = 60
interpolate = false
fract_delta = false
visibility_aabb = AABB(-20, -20, -20, 40, 40, 40)
local_coords = true
process_material = SubResource("ShaderMaterial_8ogl4")

[node name="SubEmitters" type="Node3D" parent="." unique_id=529526734]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 10, 0)

[node name="Sparks" type="GPUParticles3D" parent="SubEmitters" unique_id=1092858614]
cast_shadow = 0
emitting = false
amount = 144
lifetime = 0.5
fixed_fps = 60
interpolate = false
fract_delta = false
visibility_aabb = AABB(-20, -20, -20, 40, 40, 40)
local_coords = true
process_material = SubResource("ShaderMaterial_2tpp2")
draw_pass_1 = SubResource("QuadMesh_ai3hr")

[node name="EmbersProjectile" type="GPUParticles3D" parent="SubEmitters" unique_id=1921792802]
cast_shadow = 0
emitting = false
amount = 256
lifetime = 0.4
fixed_fps = 60
interpolate = false
fract_delta = false
visibility_aabb = AABB(-20, -20, -20, 40, 40, 40)
local_coords = true
process_material = SubResource("ShaderMaterial_n8jf6")
draw_pass_1 = SubResource("QuadMesh_bdfux")

[node name="EmbersImpact" type="GPUParticles3D" parent="SubEmitters" unique_id=1343289789]
cast_shadow = 0
emitting = false
amount = 96
fixed_fps = 60
interpolate = false
fract_delta = false
visibility_aabb = AABB(-20, -20, -20, 40, 40, 40)
local_coords = true
process_material = SubResource("ShaderMaterial_irr66")
draw_pass_1 = SubResource("QuadMesh_n8fbk")

[node name="GroundImpact" type="GPUParticles3D" parent="SubEmitters" unique_id=1038078785]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 0.01, 0)
cast_shadow = 0
emitting = false
amount = 24
lifetime = 0.2
fixed_fps = 60
interpolate = false
fract_delta = false
visibility_aabb = AABB(-20, -20, -20, 40, 40, 40)
local_coords = true
process_material = SubResource("ShaderMaterial_rxfon")
draw_pass_1 = SubResource("PlaneMesh_eaah3")

[node name="Flare" type="GPUParticles3D" parent="SubEmitters" unique_id=993540756]
cast_shadow = 0
emitting = false
amount = 24
lifetime = 0.2
fixed_fps = 60
interpolate = false
fract_delta = false
visibility_aabb = AABB(-20, -20, -20, 40, 40, 40)
local_coords = true
process_material = SubResource("ShaderMaterial_s764p")
draw_pass_1 = SubResource("QuadMesh_1vk34")

[node name="Glow" type="GPUParticles3D" parent="SubEmitters" unique_id=1355481495]
cast_shadow = 0
emitting = false
amount = 24
lifetime = 0.2
fixed_fps = 60
interpolate = false
fract_delta = false
visibility_aabb = AABB(-20, -20, -20, 40, 40, 40)
local_coords = true
process_material = SubResource("ShaderMaterial_hddlj")
draw_pass_1 = SubResource("QuadMesh_kwytq")

[node name="Smoke" type="GPUParticles3D" parent="SubEmitters" unique_id=1822594597]
cast_shadow = 0
emitting = false
amount = 288
lifetime = 0.8
fixed_fps = 60
interpolate = false
fract_delta = false
visibility_aabb = AABB(-20, -20, -20, 40, 40, 40)
local_coords = true
transform_align = 1
process_material = SubResource("ShaderMaterial_fxwkx")
draw_pass_1 = SubResource("QuadMesh_6gdg1")

[node name="Shockwave" type="GPUParticles3D" parent="SubEmitters" unique_id=483113805]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 0.01, 0)
material_override = SubResource("ShaderMaterial_r3tca")
cast_shadow = 0
emitting = false
amount = 24
lifetime = 0.2
one_shot = true
fixed_fps = 60
interpolate = false
fract_delta = false
visibility_aabb = AABB(-20, -20, -20, 40, 40, 40)
local_coords = true
process_material = SubResource("ShaderMaterial_35adv")
draw_pass_1 = ExtResource("40_7vxbc")

[node name="Crater" type="GPUParticles3D" parent="SubEmitters" unique_id=307678510]
cast_shadow = 0
emitting = false
amount = 24
lifetime = 0.6
one_shot = true
fixed_fps = 60
interpolate = false
fract_delta = false
visibility_aabb = AABB(-20, -20, -20, 40, 40, 40)
local_coords = true
process_material = SubResource("ShaderMaterial_jcfxp")
draw_pass_1 = SubResource("PlaneMesh_3i4eu")

[node name="Debris" type="GPUParticles3D" parent="SubEmitters" unique_id=1743918303]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 0.01, 0)
material_override = SubResource("ShaderMaterial_xjq6u")
cast_shadow = 0
emitting = false
amount = 96
fixed_fps = 60
interpolate = false
fract_delta = false
visibility_aabb = AABB(-20, -20, -20, 40, 40, 40)
local_coords = true
process_material = SubResource("ShaderMaterial_j7fib")
draw_pass_1 = ExtResource("45_jlx0x")
